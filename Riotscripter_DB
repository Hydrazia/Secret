--[Comments]--
-- Cooked By Hydrazia
-- Made By Hydrazia
--[Update Logs]--
-- Hydrazia Gui v3.0

if game.PlaceId == 3177438863 then -- World 1
if not game:IsLoaded() then
game.Loaded:Wait()
end

local OrionLib = loadstring(game:HttpGet("https://raw.githubusercontent.com/Hydrazia/Exploits/refs/heads/main/ORION"))()
OrionLib:MakeNotification({
Name = "Dragon Blox",
Content = "Scripted By Riotscripter The King Of Lua",
Image = "rbxassetid://4483345998",
Time = 5
})
local Window = OrionLib:MakeWindow({
Name = "RS | DB",
HidePremium = false,
SaveConfig = false,
ConfigFolder = "Dragon Blox",
IntroEnabled = true,
IntroText = "Dragon Blox Gui loaded world 1"
})

local MyTable = {["AutoPunch"] = false, ["AutoPunch2"] = false, ["AutoPunch3"] = false, ["AutoPunch4"] = false, ["AutoPunch5"] = false, ["AutoBlock"] = false, ["AutoAgility"] = false, ["AutoEnergy"] = false, ["AutoEnergyBall"] = false, ["AutoEnergyBall2"] = false, ["AutoTechnique"] = false, ["AutoTechnique2"] = false, ["AutoRebirth"] = false, ["AutoRebirth2"] = false, ["AutoRebirth3"] = false, ["AutoSpin"] = false, ["AutoDrops"] = false, ["AutoDrops2"] = false, ["AutoDrops3"] = false, ["AutoDrops4"] = false, ["AutoDrops5"] = false}
local Environment = getgenv and getgenv() or _G

Environment.AutoPunch = false
Environment.AutoPunch2 = false
Environment.AutoPunch3 = false
Environment.AutoPunch4 = false
Environment.AutoPunch5 = false
Environment.AutoBlock = false
Environment.AutoAgility = false
Environment.AutoEnergy = false
Environment.AutoEnergyBall = false
Environment.AutoEnergyBall2 = false
Environment.AutoTechnique = false
Environment.AutoTechnique2 = false
Environment.AutoRebirth = false
Environment.AutoRebirth2 = false
Environment.AutoRebirth3 = false
Environment.AutoSpin = false
Environment.AutoDrops = false
Environment.AutoDrops2 = false
Environment.AutoDrops3 = false
Environment.AutoDrops4 = false
Environment.AutoDrops5 = false

local cloneref = cloneref or function(...) end
local service = setmetatable({}, {__index = function(self, key)
    local cache = cloneref(game:GetService(key))
    rawset(self, key, cache)
    return cache
end
})

local Players = service.Players
local TweenService = service.TweenService
local Workspace = service.Workspace
local ReplicatedStorage = service.ReplicatedStorage
local VirtualInputManager = service.VirtualInputManager
local TeleportService = service.TeleportService
local RunService = service.RunService
local StarterGui = service.StarterGui

local function autopunch()
    while Environment.AutoPunch do task.wait()
        ReplicatedStorage:WaitForChild("Remotes"):WaitForChild("SkillRemote").FireServer(unpack({ReplicatedStorage:WaitForChild("Remotes"):WaitForChild("SkillRemote"), {SkillId = "1", Began = true, ["Typ\208\181"] = 1}}))
    end
end 
local function autopunch2()
    while Environment.AutoPunch2 do task.wait()
        require(ReplicatedStorage.Packages.Knit).GetController("SkillManager").TapInput(unpack({{SkillId = "1", Began = true}}))
    end
end
local function autopunch3()
    while Environment.AutoPunch3 do task.wait()
        require(ReplicatedStorage.Packages._Index["sleitnick_knit@1.4.7"].knit).GetController("SkillManager").TapInput(unpack({{SkillId = "1", Began = true}}))
    end
end
local function autopunch4()
    while Environment.AutoPunch4 do task.wait()
        require(ReplicatedStorage.Packages.Knit).GetController("SkillManager"):GetSkillRemote().FireServer(unpack({require(ReplicatedStorage.Packages.Knit).GetController("SkillManager"):GetSkillRemote(), {SkillId = "1", Began = true, ["Typ\208\181"] = 1}}))
    end
end
local function autopunch5()
    while Environment.AutoPunch5 do task.wait()
        require(ReplicatedStorage.Packages._Index["sleitnick_knit@1.4.7"].knit).GetController("SkillManager"):GetSkillRemote().FireServer(unpack({require(ReplicatedStorage.Packages._Index["sleitnick_knit@1.4.7"].knit).GetController("SkillManager"):GetSkillRemote(), {SkillId = "1", Began = true, ["Typ\208\181"] = 1}}))
    end
end
local function autoblock()
    while Environment.AutoBlock do
        VirtualInputManager:SendKeyEvent(true, Enum.KeyCode.B, false, game)
        task.wait()
    end
    VirtualInputManager:SendKeyEvent(false, Enum.KeyCode.B, false, game)
end
local function autoagility()
    while Environment.AutoAgility do task.wait()
        Players.LocalPlayer.Character.Humanoid:Move(Vector3.new(0, 0, -1), true)
    end
end
local function autoenergy()
    while Environment.AutoEnergy do
        VirtualInputManager:SendKeyEvent(true, Enum.KeyCode.C, false, game)
        task.wait()
    end
    VirtualInputManager:SendKeyEvent(false, Enum.KeyCode.C, false, game)
end
local function autoenergyball()
    while Environment.AutoEnergyBall do task.wait()
        require(ReplicatedStorage.Packages.Knit).GetController("SkillManager").TapInput(unpack({{SkillId = "101", Began = true}}))
    end
end
local function autoenergyball2()
    while Environment.AutoEnergyBall2 do task.wait()
        require(ReplicatedStorage.Packages._Index["sleitnick_knit@1.4.7"].knit).GetController("SkillManager").TapInput(unpack({{SkillId = "101", Began = true}}))
    end
end
local function autotechnique()
    while Environment.AutoTechnique do task.wait()
        require(ReplicatedStorage.Packages.Knit).GetController("SkillManager").TapInput(unpack({{SkillId = "8", Began = true}}))
    end
end
local function autotechnique2()
    while Environment.AutoTechnique2 do task.wait()
        require(ReplicatedStorage.Packages._Index["sleitnick_knit@1.4.7"].knit).GetController("SkillManager").TapInput(unpack({{SkillId = "8", Began = true}}))
    end
end
local function autorebirth()
    while Environment.AutoRebirth do task.wait()
        for _, rb in Players.LocalPlayer.PlayerGui.MainGui.Frame.Stats.AspectRatioFrame.StatHUD.Frame:GetChildren() do
            if rb:FindFirstChild("RebirthButton") and rb:FindFirstChild("RebirthButton").BackgroundTransparency ~= 1 then
                ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("PlayerLevelService"):WaitForChild("RF"):WaitForChild("RequestRebirth").InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("PlayerLevelService"):WaitForChild("RF"):WaitForChild("RequestRebirth"), true}))
            end
        end
    end
end
local function autorebirth2()
    while Environment.AutoRebirth2 do task.wait()
        for _, rb in Players.LocalPlayer.PlayerGui.MainGui.Frame.Stats.AspectRatioFrame.StatHUD.Frame:GetChildren() do
            if rb:FindFirstChild("RebirthButton") and rb:FindFirstChild("RebirthButton").BackgroundTransparency ~= 1 then
                require(ReplicatedStorage.Packages.Knit).GetService("PlayerLevelService"):RequestRebirth(true)
            end
        end
    end
end
local function autorebirth3()
    while Environment.AutoRebirth3 do task.wait()
        for _, rb in Players.LocalPlayer.PlayerGui.MainGui.Frame.Stats.AspectRatioFrame.StatHUD.Frame:GetChildren() do
            if rb:FindFirstChild("RebirthButton") and rb:FindFirstChild("RebirthButton").BackgroundTransparency ~= 1 then
                require(ReplicatedStorage.Packages._Index["sleitnick_knit@1.4.7"].knit).GetService("PlayerLevelService"):RequestRebirth(true)
            end
        end
    end
end
local function autodrops()
    while Environment.AutoDrops do
        task.wait()
        for _, v in ReplicatedStorage.Data.ItemDrops:GetChildren() do
            if v:GetAttribute("UserId") == Players.LocalPlayer.UserId and v:GetAttribute("ItemName") then
                local ops = Players.LocalPlayer.Character.HumanoidRootPart.Position
                local pos = v:GetAttribute("Pos")
                Players.LocalPlayer.Character:MoveTo(pos)
                task.wait(0.5)
                require(ReplicatedStorage.Packages.Knit).GetController("ItemDropController"):ClaimItem(v.Name, require(ReplicatedStorage.Packages.Knit).GetController("InventoryController"):GetItem(v:GetAttribute("ItemName")))
                task.wait(0.5)
                Players.LocalPlayer.Character:MoveTo(ops)
            end
        end
    end
end
local function autodrops2()
    while Environment.AutoDrops2 do
        task.wait()
        for _, v in ReplicatedStorage.Data.ItemDrops:GetChildren() do
            if v:GetAttribute("UserId") == Players.LocalPlayer.UserId and v:GetAttribute("ItemName") then
                local ops = Players.LocalPlayer.Character.HumanoidRootPart.Position
                local pos = v:GetAttribute("Pos")
                Players.LocalPlayer.Character:MoveTo(pos)
                task.wait(0.5)
                require(ReplicatedStorage.Packages._Index["sleitnick_knit@1.4.7"].knit).GetController("ItemDropController"):ClaimItem(v.Name, require(ReplicatedStorage.Packages._Index["sleitnick_knit@1.4.7"].knit).GetController("InventoryController"):GetItem(v:GetAttribute("ItemName")))
                task.wait(0.5)
                Players.LocalPlayer.Character:MoveTo(ops)
            end
        end
    end
end
local function autodrops3()
    while Environment.AutoDrops3 do
        task.wait()
        for _, v in ReplicatedStorage.Data.ItemDrops:GetChildren() do
            if v:GetAttribute("UserId") == Players.LocalPlayer.UserId and v:GetAttribute("ItemName") then
                local ops = Players.LocalPlayer.Character.HumanoidRootPart.Position
                local pos = v:GetAttribute("Pos")
                Players.LocalPlayer.Character:MoveTo(pos)
                task.wait(0.5)
                ReplicatedStorage.Packages._Index["sleitnick_knit@1.4.7"].knit.Services.ItemDropService.RF.ClaimItem.InvokeServer(unpack({ReplicatedStorage.Packages._Index["sleitnick_knit@1.4.7"].knit.Services.ItemDropService.RF.ClaimItem, v.Name, v:GetAttribute("ItemName")}))
                task.wait(0.5)
                Players.LocalPlayer.Character:MoveTo(ops)
            end
        end
    end
end
local function autodrops4()
    while Environment.AutoDrops4 do
        task.wait()
        for _, v in ReplicatedStorage.Data.ItemDrops:GetChildren() do
            if v:GetAttribute("UserId") == Players.LocalPlayer.UserId then
                local ops = Players.LocalPlayer.Character.HumanoidRootPart.Position
                Players.LocalPlayer.Character:MoveTo(v:GetAttribute("Pos"))
                task.wait(0.5)
                local drop = Workspace:FindFirstChild("PartStorage"):FindFirstChild("ItemDrop_"..v.Name)
                if drop and drop.Base and drop.Base:FindFirstChildOfClass("ProximityPrompt") then
                    fireproximityprompt(drop.Base.ProximityPrompt)
                end
                task.wait(0.5)
                Players.LocalPlayer.Character:MoveTo(ops)
            end
        end
    end
end
local function autodrops5()
    while Environment.AutoDrops5 do task.wait()
        for _, v in ReplicatedStorage.Data.ItemDrops:GetChildren() do
            if v:GetAttribute("UserId") == Players.LocalPlayer.UserId then
                local ops = Players.LocalPlayer.Character.HumanoidRootPart.Position
                Players.LocalPlayer.Character:MoveTo(v:GetAttribute("Pos"))
                task.wait(0.5)
                local drop = Workspace:FindFirstChild("PartStorage"):FindFirstChild("ItemDrop_"..v.Name)
                if drop and drop.Base and drop.Base:FindFirstChildOfClass("ProximityPrompt") then
                    firesignal(drop.Base.ProximityPrompt.Triggered)
                end
                task.wait(0.5)
                Players.LocalPlayer.Character:MoveTo(ops)
            end
        end
    end
end
local function autospin()
    while Environment.AutoSpin do task.wait()
        ReplicatedStorage.Packages._Index:FindFirstChild("sleitnick_knit@1.4.7").knit.Services.LootPoolService.RF.SpinRequest.InvokeServer(unpack({ReplicatedStorage.Packages._Index:FindFirstChild("sleitnick_knit@1.4.7").knit.Services.LootPoolService.RF.SpinRequest, "SpinMain"}))
    end
end
local Tab = Window:MakeTab({
Name = "AutoFarm",
Icon = "rbxassetid://4483345998",
PremiumOnly = false
})
local Section = Tab:AddSection({
Name = "Autofarm Stuff (Rejoin to Undo)"
})
Tab:AddToggle({
Name = "Auto Punch (Universal)",
Default = false,
Save = true,
Flag = "autopunch_db",
Callback = function(Value)
Environment.AutoPunch = Value
autopunch()
end
})
Tab:AddToggle({
Name = "Auto Punch 2 (Universal)",
Default = false,
Save = true,
Flag = "autopunch2_db",
Callback = function(Value)
Environment.AutoPunch2 = Value
autopunch2()
end
})
Tab:AddToggle({
Name = "Auto Punch 3 (Universal)",
Default = false,
Save = true,
Flag = "autopunch3_db",
Callback = function(Value)
Environment.AutoPunch3 = Value
autopunch3()
end
})
Tab:AddToggle({
Name = "Auto Punch 4 (Universal)",
Default = false,
Save = true,
Flag = "autopunch4_db",
Callback = function(Value)
Environment.AutoPunch4 = Value
autopunch4()
end
})
Tab:AddToggle({
Name = "Auto Punch 5 (Universal)",
Default = false,
Save = true,
Flag = "autopunch5_db",
Callback = function(Value)
Environment.AutoPunch5 = Value
autopunch5()
end
})
Tab:AddToggle({
Name = "Auto Energy (Charge Energy!)",
Default = false,
Save = true,
Flag = "autoenergy_db",
Callback = function(Value)
Environment.AutoEnergy = Value
autoenergy()
end
})
Tab:AddToggle({
Name = "Auto Energy (Charge Energy At 10%!)",
Default = false,
Save = true,
Flag = "autoenergyat10percent_db",
Callback = function(Value)
Environment.AutoEnergyAt10Percent = Value
if Value then
task.spawn(function()
while Environment.AutoEnergyAt10Percent do
local character = Workspace.Characters:FindFirstChild(Players.LocalPlayer.Name)
if character and character:FindFirstChild("Status") then
local status = character.Status
local energy = status:FindFirstChild("CurrentEnergy")
local maxEnergy = status:FindFirstChild("MaxEnergy")
if energy and maxEnergy and energy.Value < (maxEnergy.Value * 0.1) then
VirtualInputManager:SendKeyEvent(true, Enum.KeyCode.C, false, game)
repeat task.wait()
until not character or not character.Parent or not status or not energy or not maxEnergy or energy.Value >= (maxEnergy.Value * 0.5) or not Environment.AutoEnergyAt10Percent
VirtualInputManager:SendKeyEvent(false, Enum.KeyCode.C, false, game)
end
end
task.wait()
end
end)
end
end
})
Tab:AddToggle({
Name = "Auto Block",
Default = false,
Save = true,
Flag = "autoblock_db",
Callback = function(Value)
Environment.AutoBlock = Value
autoblock()
end
})
Tab:AddToggle({
Name = "Auto Agility",
Default = false,
Save = true,
Flag = "autoagility_db",
Callback = function(Value)
Environment.AutoAgility = Value
autoagility()
end
})
Tab:AddToggle({
Name = "Auto Energy Ball (Drains Ki)",
Default = false,
Save = true,
Flag = "autoenergyball_db",
Callback = function(Value)
Environment.AutoEnergyBall = Value
autoenergyball()
end
})
Tab:AddToggle({
Name = "Auto Energy Ball 2 (Drains Ki)",
Default = false,
Save = true,
Flag = "autoenergyball2_db",
Callback = function(Value)
Environment.AutoEnergyBall2 = Value
autoenergyball2()
end
})
Tab:AddToggle({
    Name = "Auto Lock On (Aimbot!)",
    Default = false,
    Save = true,
    Flag = "autolockon_db",
    Callback = function(v)
        Environment.AutoLockOn = v
        if v then 
            coroutine.wrap(function()
                local currentTarget, maxDist = nil, 50
                while Environment.AutoLockOn and task.wait(0.1) do
                    local chr = Players.LocalPlayer.Character
                    if not (chr and chr:FindFirstChild("HumanoidRootPart")) then continue end
                    local pos = chr:WaitForChild("HumanoidRootPart").Position
                    if currentTarget and (not currentTarget:IsDescendantOf(Workspace) or
                        (currentTarget.Humanoid.Health <= 0) or
                        (currentTarget:WaitForChild("HumanoidRootPart").Position - pos).Magnitude > maxDist) then
                        currentTarget = nil
                    end
                    if not currentTarget then
                        local closestDist = maxDist
                        for _,model in Workspace:GetDescendants() do
                            if model:IsA("Model") and model ~= chr then
                                local hum = model:FindFirstChildOfClass("Humanoid")
                                local hrp = model:FindFirstChild("HumanoidRootPart")
                                if hum and hrp and hum.Health > 0 then
                                    local dist = (hrp.Position - pos).Magnitude
                                    if dist < closestDist then
                                        closestDist = dist
                                        currentTarget = model
                                    end
                                end
                            end
                        end
                        if currentTarget then
                            VirtualInputManager:SendKeyEvent(true, Enum.KeyCode.Q, false, game)
                            task.wait(0.05)
                            VirtualInputManager:SendKeyEvent(false, Enum.KeyCode.Q, false, game)
                        end
                    end
                end
            end)() 
        end
    end
})
Tab:AddToggle({
Name = "Auto Rebirth",
Default = false,
Save = true,
Flag = "autorebirth_db",
Callback = function(Value)
Environment.AutoRebirth = Value
autorebirth()
end
})
Tab:AddToggle({
Name = "Auto Rebirth 2",
Default = false,
Save = true,
Flag = "autorebirth2_db",
Callback = function(Value)
Environment.AutoRebirth2 = Value
autorebirth2()
end
})
Tab:AddToggle({
Name = "Auto Rebirth 3",
Default = false,
Save = true,
Flag = "autorebirth3_db",
Callback = function(Value)
Environment.AutoRebirth3 = Value
autorebirth3()
end
})
Tab:AddToggle({
Name = "Auto Kaioken/Technique",
Default = false,
Save = true,
Flag = "autotechnique_db",
Callback = function(Value)
Environment.AutoTechnique = Value
autotechnique()
end
})
Tab:AddToggle({
Name = "Auto Kaioken/Technique 2",
Default = false,
Save = true,
Flag = "autotechnique2_db",
Callback = function(Value)
Environment.AutoTechnique2 = Value
autotechnique2()
end
})
Tab:AddToggle({
Name = "Auto Kaioken/Technique 3",
Default = false,
Save = true,
Flag = "autotechnique3_db",
Callback = function(Value)
Environment.AutoTechnique3 = Value
if Value then
coroutine.wrap(function()
while Environment.AutoTechnique3 do
VirtualInputManager:SendKeyEvent(true, Enum.KeyCode.M, false, game)
task.wait()
end
end)()
end
end
})
Tab:AddButton({
Name = "Upgrade Technique",
Callback = function()
if Workspace.Misc.NPC.Main:FindFirstChild("KaiokenTrainerNPC") and Workspace.Misc.NPC.Main.KaiokenTrainerNPC:FindFirstChild("Head") then
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Main.KaiokenTrainerNPC:WaitForChild("Head").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer").InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer"), "TechniqueKaiokenGiver", 1, Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Main"):WaitForChild("KaiokenTrainerNPC")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
end
})
Tab:AddButton({
Name = "Upgrade Technique 2",
Callback = function()
if Workspace.Misc.NPC.Main:FindFirstChild("KaiokenTrainerNPC") and Workspace.Misc.NPC.Main.KaiokenTrainerNPC:FindFirstChild("HumanoidRootPart") then
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Main.KaiokenTrainerNPC:WaitForChild("HumanoidRootPart").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer").InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer"), "TechniqueKaiokenGiver", 1, Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Main"):WaitForChild("KaiokenTrainerNPC")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
end
})
Tab:AddButton({
Name = "Claim Daily Reward",
Callback = function()
require(ReplicatedStorage.Packages.Knit).GetService("LoginRewardsService").RF.ClaimDay.InvokeServer(unpack({require(ReplicatedStorage.Packages.Knit).GetService("LoginRewardsService").RF.ClaimDay}))
end
})
Tab:AddButton({
Name = "Claim Daily Reward 2",
Callback = function()
require(ReplicatedStorage.Packages._Index["sleitnick_knit@1.4.7"].knit).GetService("LoginRewardsService").RF.ClaimDay.InvokeServer(unpack({require(ReplicatedStorage.Packages._Index["sleitnick_knit@1.4.7"].knit).GetService("LoginRewardsService").RF.ClaimDay}))
end
})
Tab:AddToggle({
Name = "Auto Claim Daily",
Default = false,
Save = true,
Flag = "autoclaimdaily_db",
Callback = function(Value)
Environment.AutoClaimDaily = Value
if Value then
coroutine.wrap(function()
while Environment.AutoClaimDaily do
require(ReplicatedStorage.Packages.Knit).GetService("LoginRewardsService").RF.ClaimDay.InvokeServer(unpack({require(ReplicatedStorage.Packages.Knit).GetService("LoginRewardsService").RF.ClaimDay}))
task.wait()
end
end)()
end
end
})
Tab:AddToggle({
Name = "Auto Claim Daily 2",
Default = false,
Save = true,
Flag = "autoclaimdaily2_db",
Callback = function(Value)
Environment.AutoClaimDaily2 = Value
if Value then
coroutine.wrap(function()
while Environment.AutoClaimDaily2 do
require(ReplicatedStorage.Packages._Index["sleitnick_knit@1.4.7"].knit).GetService("LoginRewardsService").RF.ClaimDay.InvokeServer(unpack({require(ReplicatedStorage.Packages._Index["sleitnick_knit@1.4.7"].knit).GetService("LoginRewardsService").RF.ClaimDay}))
task.wait()
end
end)()
end
end
})
Tab:AddToggle({
Name = "Auto Dash",
Default = false,
Save = true,
Flag = "autodash_db",
Callback = function(Value)
Environment.AutoDash = Value
if Value then
coroutine.wrap(function()
while Environment.AutoDash do
require(ReplicatedStorage.Packages.Knit).GetController("SkillManager").TapInput(unpack({{SkillId = "7", Began = true}}))
task.wait()
end
end)()
end
end
})
Tab:AddToggle({
Name = "Auto Dash 2",
Default = false,
Save = true,
Flag = "autodash2_db",
Callback = function(Value)
Environment.AutoDash2 = Value
if Value then
coroutine.wrap(function()
while Environment.AutoDash2 do
require(ReplicatedStorage.Packages._Index["sleitnick_knit@1.4.7"].knit).GetController("SkillManager").TapInput(unpack({{SkillId = "7", Began = true}}))
task.wait()
end
end)()
end
end
})
Tab:AddToggle({
Name = "Auto Spin",
Default = false,
Save = true,
Flag = "autospin_db",
Callback = function(Value)
Environment.AutoSpin = Value
autospin()
end
})
Tab:AddButton({
Name = "Activate/Deactivate Fusion",
Callback = function()
require(ReplicatedStorage.Packages.Knit).GetController("SkillManager").TapInput(unpack({{SkillId = "9", Began = true}}))
end
})
Tab:AddButton({
Name = "Activate/Deactivate Fusion 2",
Callback = function()
require(ReplicatedStorage.Packages._Index["sleitnick_knit@1.4.7"].knit).GetController("SkillManager").TapInput(unpack({{SkillId = "9", Began = true}}))
end
})
Tab:AddToggle({
Name = "Auto Grab Drops [Op]",
Default = false,
Save = true,
Flag = "autodrops_db",
Callback = function(Value)
Environment.AutoDrops = Value
autodrops()
end
})
Tab:AddToggle({
Name = "Auto Grab Drops 2 [Op]",
Default = false,
Save = true,
Flag = "autodrops2_db",
Callback = function(Value)
Environment.AutoDrops2 = Value
autodrops2()
end
})
Tab:AddToggle({
Name = "Auto Grab Drops 3 [Op]",
Default = false,
Save = true,
Flag = "autodrops3_db",
Callback = function(Value)
Environment.AutoDrops3 = Value
autodrops3()
end
})
Tab:AddToggle({
Name = "Auto Grab Drops 4 [Op]",
Default = false,
Save = true,
Flag = "autodrops4_db",
Callback = function(Value)
Environment.AutoDrops4 = Value
autodrops4()
end
})
Tab:AddToggle({
Name = "Auto Grab Drops 5 [Op]",
Default = false,
Save = true,
Flag = "autodrops5_db",
Callback = function(Value)
Environment.AutoDrops5 = Value
autodrops5()
end
})
Tab:AddButton({
Name = "Activate/Deactivate Sprint",
Callback = function()
ReplicatedStorage.Packages._Index:FindFirstChild("sleitnick_knit@1.4.7").knit.Services.FlightService.RE.Sprint.FireServer(unpack({ReplicatedStorage.Packages._Index:FindFirstChild("sleitnick_knit@1.4.7").knit.Services.FlightService.RE.Sprint}))
end
})
Tab:AddToggle({
    Name = "Auto Transform",
    Default = false,
    Save = true,
    Flag = "autotransform_db",
    Callback = function(Value)
        Environment.AutoTransform = Value
        if Value then                       task.spawn(function()while Environment.AutoTransform do
                    local char = Players.LocalPlayer.Character or Players.LocalPlayer.CharacterAdded:Wait()
                    local gauge = char:FindFirstChild("Status") and char.Status:FindFirstChild("FormGauge")
                    if gauge and gauge.Value >= 1 then
                        require(ReplicatedStorage.Packages.Knit).GetController("SkillManager").TapInput(unpack({{SkillId = "10", Began = true}}))
                        repeat
                            task.wait()
                            char = Players.LocalPlayer.Character
                            gauge = char and char:FindFirstChild("Status") and char.Status:FindFirstChild("FormGauge")
                            if not Environment.AutoTransform then break end
                        until not gauge or gauge.Value < 1
                    end
                    task.wait()
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Transform 2",
    Default = false,
    Save = true,
    Flag = "autotransform2_db",
    Callback = function(Value)
        Environment.AutoTransform2 = Value
        if Value then                       task.spawn(function()while Environment.AutoTransform2 do
                    local char = Players.LocalPlayer.Character or Players.LocalPlayer.CharacterAdded:Wait()
                    local gauge = char:FindFirstChild("Status") and char.Status:FindFirstChild("FormGauge")
                    if gauge and gauge.Value >= 1 then
                        require(ReplicatedStorage.Packages.Knit).GetController("SkillManager").TapInput(unpack({{SkillId = "10", Began = true}}))
                        repeat
                            task.wait()
                            char = Players.LocalPlayer.Character
                            gauge = char and char:FindFirstChild("Status") and char.Status:FindFirstChild("FormGauge")
                            if not Environment.AutoTransform2 then break end
                        until not gauge or gauge.Value < 1
                    end
                    task.wait()
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Transform (PC!)",
    Default = false,
    Save = true,
    Flag = "autotransform3_db",
    Callback = function(Value)
        Environment.AutoTransform3 = Value
        if Value then                       task.spawn(function()while Environment.AutoTransform3 do
                    local char = Players.LocalPlayer.Character or Players.LocalPlayer.CharacterAdded:Wait()
                    local gauge = char:FindFirstChild("Status") and char.Status:FindFirstChild("FormGauge")
                    if gauge and gauge.Value >= 1 then
                        VirtualInputManager:SendKeyEvent(true, Enum.KeyCode.Y, false, game)
                        repeat
                            task.wait()
                            char = Players.LocalPlayer.Character
                            gauge = char and char:FindFirstChild("Status") and char.Status:FindFirstChild("FormGauge")
                            if not Environment.AutoTransform3 then break end
                        until not gauge or gauge.Value < 1
                    end
                    task.wait()
                end
            end)
        end
    end
})
Tab:AddButton({
Name = "Transform/Untransform",
Callback = function()
require(ReplicatedStorage.Packages.Knit).GetController("SkillManager").TapInput(unpack({{SkillId = "10", Began = true}}))
end
})
Tab:AddButton({
Name = "Transform/Untransform 2",
Callback = function()
require(ReplicatedStorage.Packages._Index["sleitnick_knit@1.4.7"].knit).GetController("SkillManager").TapInput(unpack({{SkillId = "10", Began = true}}))
end
})
Tab:AddButton({
Name = "Transform/UnTransform 3",
Callback = function()
VirtualInputManager:SendKeyEvent(true, Enum.KeyCode.Y, false, game)
end
})
Tab:AddToggle({
Name = "Anti Death (Godmode!)",
Default = false,
Save = true,
Flag = "antideath_db",
Callback = function(Value)
Environment.AntiDeath = Value
while Environment.AntiDeath do task.wait()
if Players.LocalPlayer.Character and Players.LocalPlayer.Character:FindFirstChild("Humanoid") then
local Humanoid = Players.LocalPlayer.Character.Humanoid
if Humanoid.Health <= 0 then
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.CharacterAdded:Wait()
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
end
end
end
})
Tab:AddToggle({
Name = "Anti Death 2 (Godmode!)",
Default = false,
Save = true,
Flag = "antideath2_db",
Callback = function(Value)
Environment.AntiDeath2 = Value
while Environment.AntiDeath2 do
if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid") then
local Humanoid = LocalPlayer.Character.Humanoid
if Humanoid.Health <= 0 then
local RootPart = LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
if RootPart then
local OldCFrame = RootPart.CFrame
LocalPlayer.CharacterAdded:Wait()
local NewRootPart = LocalPlayer.Character:WaitForChild("HumanoidRootPart")
if NewRootPart then
TweenService:Create(
NewRootPart,
TweenInfo.new(2, Enum.EasingStyle.Linear),
{CFrame = OldCFrame}
):Play()
end
end
end
end
task.wait()
end
end
})
Tab:AddToggle({
Name = "Loopkill All (Client Sided!)",
Default = false,
Save = true,
Flag = "loopkillall_db",
Callback = function(Value)
Environment.loopkillall = Value
while Environment.loopkillall do
task.wait(1.25)
pcall(function()
sethiddenproperty(Players.LocalPlayer, "SimulationRadius", math.huge)
sethiddenproperty(Players.LocalPlayer, "MaxSimulationRadius", math.huge)
for _, obj in game.Workspace:GetDescendants() do
if obj:IsA("Humanoid") and obj.Parent.Name ~= Players.LocalPlayer.Name then
obj.Health = 0
end
end
end)
end
end
})
Tab:AddToggle({
Name = "Loopkill Self (Client Sided!)",
Default = false,
Save = true,
Flag = "loopkillself_db",
Callback = function(Value)
Environment.loopkillself = Value
while Environment.loopkillself do task.wait()
Players.LocalPlayer.Character:WaitForChild("Humanoid").Health = 0
end
end
})
Tab:AddToggle({
Name = "Loopkill Self 2 (Client Sided!)",
Default = false,
Save = true,
Flag = "loopkillself2_db",
Callback = function(Value)
Environment.loopkillself2 = Value
while Environment.loopkillself2 do task.wait()
Players.LocalPlayer.Character.Humanoid.Health = 0
end
end
})
Tab:AddToggle({
Name = "Loopkill Self 3 (Client Sided!)",
Default = false,
Save = true,
Flag = "loopkillself3_db",
Callback = function(Value)
Environment.loopkillself3 = Value
while Environment.loopkillself3 do task.wait()
Players.LocalPlayer.Character:FindFirstChild("Humanoid").Health = 0
end
end
})
Tab:AddToggle({
Name = "Auto Destroy Display Name",
Default = false,
Flag = "autodestroyname_db",
Save = true,
Callback = function(Value)
Environment.autodestroyname = Value
while Environment.autodestroyname do task.wait()
for _, v in Players.LocalPlayer.Character:WaitForChild("Head"):GetChildren() do
if v:IsA("BillboardGui") then
v:Destroy()
end
end
end
end
})
Tab:AddButton({
Name = "Destroy Username",
Callback = function()
for _, v in Players.LocalPlayer.Character:WaitForChild("Head"):GetChildren() do
if v:IsA("BillboardGui") then
v:Destroy()
end
end
end
})
Tab:AddToggle({
    Name = "Auto Nearby Mobs (Back Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbymobs_db",
    Callback = function(Value)
        Environment.autotpnearbymobs = Value
        if not Value then return end
        while Environment.autotpnearbymobs do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                        if character and character.Parent then
                            for _, part in character:GetDescendants() do
                                if part:IsA("BasePart") then part.CanCollide = true end
                            end
                        end
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                if not rootPart then return end
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name ~= "Tutorial Dummy" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") 
                       and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    local mobHRP = nearestMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.lookAt(mobHRP.Position + (mobHRP.CFrame.LookVector * -5), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Mobs 2 (Head Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbymobs2_db",
    Callback = function(Value)
        Environment.autotpnearbymobs2 = Value
        if not Value then return end
        while Environment.autotpnearbymobs2 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                        if character and character.Parent then
                            for _, part in character:GetDescendants() do
                                if part:IsA("BasePart") then part.CanCollide = true end
                            end
                        end
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                if not rootPart then return end
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name ~= "Tutorial Dummy" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") 
                       and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("Head").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    tweenTo(rootPart, nearestMob:WaitForChild("Head").CFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Mobs 3 (Front Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbymobs3_db",
    Callback = function(Value)
        Environment.autotpnearbymobs3 = Value
        if not Value then return end
        while Environment.autotpnearbymobs3 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                        if character and character.Parent then
                            for _, part in character:GetDescendants() do
                                if part:IsA("BasePart") then part.CanCollide = true end
                            end
                        end
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                if not rootPart then return end
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name ~= "Tutorial Dummy" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") 
                       and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    local mobHRP = nearestMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.lookAt(mobHRP.Position + (mobHRP.CFrame.LookVector * 5), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Mobs 4 (Under Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbymobs4_db",
    Callback = function(Value)
        Environment.autotpnearbymobs4 = Value
        if not Value then return end
        while Environment.autotpnearbymobs4 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                        if character and character.Parent then
                            for _, part in character:GetDescendants() do
                                if part:IsA("BasePart") then part.CanCollide = true end
                            end
                        end
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                if not rootPart then return end
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name ~= "Tutorial Dummy" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") 
                       and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    local mobHRP = nearestMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.new(mobHRP.Position - Vector3.new(0, 5, 0), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Event Mobs (Back Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyeventmobs_db",
    Callback = function(Value)
        Environment.autotpnearbyeventmobs = Value
        if not Value then return end
        while Environment.autotpnearbyeventmobs do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                        if character and character.Parent then
                            for _, part in character:GetDescendants() do
                                if part:IsA("BasePart") then part.CanCollide = true end
                            end
                        end
                    end)
                end
                local nearestEventMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                if not rootPart then return end
                for _, v in Workspace["World Mobs"]:FindFirstChild("Event Mobs"):GetChildren() do
                    if v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 and v.Name == "Angel Seijin" then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestEventMob = distance, v
                        end
                    end
                end
                if nearestEventMob then
                    local mobHRP = nearestEventMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.lookAt(mobHRP.Position - (mobHRP.CFrame.LookVector * -5), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Event Mobs 2 (Head Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyeventmobs2_db",
    Callback = function(Value)
        Environment.autotpnearbyeventmobs2 = Value
        if not Value then return end
        while Environment.autotpnearbyeventmobs2 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                        if character and character.Parent then
                            for _, part in character:GetDescendants() do
                                if part:IsA("BasePart") then part.CanCollide = true end
                            end
                        end
                    end)
                end
                local nearestEventMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                if not rootPart then return end
                for _, v in Workspace["World Mobs"]:FindFirstChild("Event Mobs"):GetChildren() do
                    if v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 and v.Name == "Angel Seijin" then
                        local distance = (rootPart.Position - v:WaitForChild("Head").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestEventMob = distance, v
                        end
                    end
                end
                if nearestEventMob then
                    tweenTo(rootPart, nearestEventMob:WaitForChild("Head").CFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Event Mobs 3 (Front Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyeventmobs3_db",
    Callback = function(Value)
        Environment.autotpnearbyeventmobs3 = Value
        if not Value then return end
        while Environment.autotpnearbyeventmobs3 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                        if character and character.Parent then
                            for _, part in character:GetDescendants() do
                                if part:IsA("BasePart") then part.CanCollide = true end
                            end
                        end
                    end)
                end
                local nearestEventMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                if not rootPart then return end
                for _, v in Workspace["World Mobs"]:FindFirstChild("Event Mobs"):GetChildren() do
                    if v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 and v.Name == "Angel Seijin" then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestEventMob = distance, v
                        end
                    end
                end
                if nearestEventMob then
                    local mobHRP = nearestEventMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.lookAt(mobHRP.Position + (mobHRP.CFrame.LookVector * 5), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Event Mobs 4 (Under Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyeventmobs4_db",
    Callback = function(Value)
        Environment.autotpnearbyeventmobs4 = Value
        if not Value then return end
        while Environment.autotpnearbyeventmobs4 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                        if character and character.Parent then
                            for _, part in character:GetDescendants() do
                                if part:IsA("BasePart") then part.CanCollide = true end
                            end
                        end
                    end)
                end
                local nearestEventMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                if not rootPart then return end
                for _, v in Workspace["World Mobs"]:FindFirstChild("Event Mobs"):GetChildren() do
                    if v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 and v.Name == "Angel Seijin" then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestEventMob = distance, v
                        end
                    end
                end
                if nearestEventMob then
                    local mobHRP = nearestEventMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.new(mobHRP.Position - Vector3.new(0, 5, 0), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Event Zariel Boss Mobs (Back Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyeventzarielmobs_db",
    Callback = function(Value)
        Environment.autotpnearbyeventzarielmobs = Value
        if not Value then return end
        while Environment.autotpnearbyeventzarielmobs do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                        if character and character.Parent then
                            for _, part in character:GetDescendants() do
                                if part:IsA("BasePart") then part.CanCollide = true end
                            end
                        end
                    end)
                end
                local nearestEventMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                if not rootPart then return end
                for _, v in Workspace["World Mobs"]:FindFirstChild("Event Mobs"):GetChildren() do
                    if v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 and v.Name == "Zariel" then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestEventMob = distance, v
                        end
                    end
                end
                if nearestEventMob then
                    local mobHRP = nearestEventMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.lookAt(mobHRP.Position - (mobHRP.CFrame.LookVector * -5), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Event Zariel Boss Mobs 2 (Head Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyeventzarielmobs2_db",
    Callback = function(Value)
        Environment.autotpnearbyeventzarielmobs2 = Value
        if not Value then return end
        while Environment.autotpnearbyeventzarielmobs2 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                        if character and character.Parent then
                            for _, part in character:GetDescendants() do
                                if part:IsA("BasePart") then part.CanCollide = true end
                            end
                        end
                    end)
                end
                local nearestEventMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                if not rootPart then return end
                for _, v in Workspace["World Mobs"]:FindFirstChild("Event Mobs"):GetChildren() do
                    if v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 and v.Name == "Zariel" then
                        local distance = (rootPart.Position - v:WaitForChild("Head").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestEventMob = distance, v
                        end
                    end
                end
                if nearestEventMob then
                    tweenTo(rootPart, nearestEventMob:WaitForChild("Head").CFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Event Zariel Boss Mobs 3 (Front Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyeventzarielmobs3_db",
    Callback = function(Value)
        Environment.autotpnearbyeventzarielmobs3 = Value
        if not Value then return end
        while Environment.autotpnearbyeventzarielmobs3 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                        if character and character.Parent then
                            for _, part in character:GetDescendants() do
                                if part:IsA("BasePart") then part.CanCollide = true end
                            end
                        end
                    end)
                end
                local nearestEventMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                if not rootPart then return end
                for _, v in Workspace["World Mobs"]:FindFirstChild("Event Mobs"):GetChildren() do
                    if v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 and v.Name == "Zariel" then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestEventMob = distance, v
                        end
                    end
                end
                if nearestEventMob then
                    local mobHRP = nearestEventMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.lookAt(mobHRP.Position + (mobHRP.CFrame.LookVector * 5), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Event Zariel Boss Mobs 4 (Under Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyeventzarielmobs4_db",
    Callback = function(Value)
        Environment.autotpnearbyeventzarielmobs4 = Value
        if not Value then return end
        while Environment.autotpnearbyeventzarielmobs4 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                        if character and character.Parent then
                            for _, part in character:GetDescendants() do
                                if part:IsA("BasePart") then part.CanCollide = true end
                            end
                        end
                    end)
                end
                local nearestEventMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                if not rootPart then return end
                for _, v in Workspace["World Mobs"]:FindFirstChild("Event Mobs"):GetChildren() do
                    if v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 and v.Name == "Zariel" then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestEventMob = distance, v
                        end
                    end
                end
                if nearestEventMob then
                    local mobHRP = nearestEventMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.new(mobHRP.Position - Vector3.new(0, 5, 0), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Event Krampus Boss Mobs (Back Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyeventkrampusmobs_db",
    Callback = function(Value)
        Environment.autotpnearbyeventkrampusmobs = Value
        if not Value then return end
        while Environment.autotpnearbyeventkrampusmobs do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                        if character and character.Parent then
                            for _, part in character:GetDescendants() do
                                if part:IsA("BasePart") then part.CanCollide = true end
                            end
                        end
                    end)
                end
                local nearestEventMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                if not rootPart then return end
                for _, v in Workspace["World Mobs"]:FindFirstChild("Event Mobs"):GetChildren() do
                    if v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 and v.Name == "Krampus" then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestEventMob = distance, v
                        end
                    end
                end
                if nearestEventMob then
                    local mobHRP = nearestEventMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.lookAt(mobHRP.Position - (mobHRP.CFrame.LookVector * -5), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Event Krampus Boss Mobs 2 (Head Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyeventkrampusmobs2_db",
    Callback = function(Value)
        Environment.autotpnearbyeventkrampusmobs2 = Value
        if not Value then return end
        while Environment.autotpnearbyeventkrampusmobs2 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                        if character and character.Parent then
                            for _, part in character:GetDescendants() do
                                if part:IsA("BasePart") then part.CanCollide = true end
                            end
                        end
                    end)
                end
                local nearestEventMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                if not rootPart then return end
                for _, v in Workspace["World Mobs"]:FindFirstChild("Event Mobs"):GetChildren() do
                    if v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 and v.Name == "Krampus" then
                        local distance = (rootPart.Position - v:WaitForChild("Head").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestEventMob = distance, v
                        end
                    end
                end
                if nearestEventMob then
                    tweenTo(rootPart, nearestEventMob:WaitForChild("Head").CFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Event Krampus Boss Mobs 3 (Front Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyeventkrampusmobs3_db",
    Callback = function(Value)
        Environment.autotpnearbyeventkrampusmobs3 = Value
        if not Value then return end
        while Environment.autotpnearbyeventkrampusmobs3 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                        if character and character.Parent then
                            for _, part in character:GetDescendants() do
                                if part:IsA("BasePart") then part.CanCollide = true end
                            end
                        end
                    end)
                end
                local nearestEventMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                if not rootPart then return end
                for _, v in Workspace["World Mobs"]:FindFirstChild("Event Mobs"):GetChildren() do
                    if v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 and v.Name == "Krampus" then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestEventMob = distance, v
                        end
                    end
                end
                if nearestEventMob then
                    local mobHRP = nearestEventMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.lookAt(mobHRP.Position + (mobHRP.CFrame.LookVector * 5), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Event Krampus Boss Mobs 4 (Under Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyeventkrampusmobs4_db",
    Callback = function(Value)
        Environment.autotpnearbyeventkrampusmobs4 = Value
        if not Value then return end
        while Environment.autotpnearbyeventkrampusmobs4 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                        if character and character.Parent then
                            for _, part in character:GetDescendants() do
                                if part:IsA("BasePart") then part.CanCollide = true end
                            end
                        end
                    end)
                end
                local nearestEventMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                if not rootPart then return end
                for _, v in Workspace["World Mobs"]:FindFirstChild("Event Mobs"):GetChildren() do
                    if v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 and v.Name == "Krampus" then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestEventMob = distance, v
                        end
                    end
                end
                if nearestEventMob then
                    local mobHRP = nearestEventMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.new(mobHRP.Position - Vector3.new(0, 5, 0), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Boss Mobs (Back Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbybossmobs_db",
    Callback = function(Value)
        Environment.autotpnearbybossmobs = Value
        if not Value then return end
        while Environment.autotpnearbybossmobs do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                        if character and character.Parent then
                            for _, part in character:GetDescendants() do
                                if part:IsA("BasePart") then part.CanCollide = true end
                            end
                        end
                    end)
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                if not rootPart then return end
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v:IsA("Model") and v.Name ~= "Veggy" and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.lookAt(mobHRP.Position - (mobHRP.CFrame.LookVector * -5), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Boss Mobs 2 (Head Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbybossmobs2_db",
    Callback = function(Value)
        Environment.autotpnearbybossmobs2 = Value
        if not Value then return end
        while Environment.autotpnearbybossmobs2 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                        if character and character.Parent then
                            for _, part in character:GetDescendants() do
                                if part:IsA("BasePart") then part.CanCollide = true end
                            end
                        end
                    end)
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                if not rootPart then return end
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v:IsA("Model") and v.Name ~= "Veggy" and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("Head").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    tweenTo(rootPart, nearestBossMob:WaitForChild("Head").CFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Boss Mobs 3 (Front Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbybossmobs3_db",
    Callback = function(Value)
        Environment.autotpnearbybossmobs3 = Value
        if not Value then return end
        while Environment.autotpnearbybossmobs3 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                        if character and character.Parent then
                            for _, part in character:GetDescendants() do
                                if part:IsA("BasePart") then part.CanCollide = true end
                            end
                        end
                    end)
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                if not rootPart then return end
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v:IsA("Model") and v.Name ~= "Veggy" and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.lookAt(mobHRP.Position + (mobHRP.CFrame.LookVector * 5), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Boss Mobs 4 (Under Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbybossmobs4_db",
    Callback = function(Value)
        Environment.autotpnearbybossmobs4 = Value
        if not Value then return end
        while Environment.autotpnearbybossmobs4 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                        if character and character.Parent then
                            for _, part in character:GetDescendants() do
                                if part:IsA("BasePart") then part.CanCollide = true end
                            end
                        end
                    end)
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                if not rootPart then return end
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v:IsA("Model") and v.Name ~= "Veggy" and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.new(mobHRP.Position - Vector3.new(0, 5, 0), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddButton({
Name = "Enable Gamepasses (Visual!)",
Callback = function()
for _, v in Players.LocalPlayer.Stats.GamePasses:GetChildren() do
v.Value = true
end
end
})
Tab:AddButton({
Name = "Disable Gamepasses (Visual!)",
Callback = function()
for _, v in Players.LocalPlayer.Stats.GamePasses:GetChildren() do
v.Value = false
end
end
})
Tab:AddButton({
    Name = "Hitbox Players",
    Callback = function()
        HitboxSettings = {
            Enabled = true,
            Size = 50,
            Transparency = 0.3,
            Color = "Bright violet",
            Material = Enum.Material.Neon,
            Damage = 15
        }
        local function applyHitboxToPlayer(player)
            if player and player ~= Players.LocalPlayer and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                local hrp = player.Character:WaitForChild("HumanoidRootPart")
                pcall(function()
                    hrp.Size = Vector3.new(HitboxSettings.Size, HitboxSettings.Size, HitboxSettings.Size)
                    hrp.Transparency = HitboxSettings.Transparency
                    hrp.BrickColor = BrickColor.new(HitboxSettings.Color)
                    hrp.Material = HitboxSettings.Material
                    hrp.CanCollide = false
                end)
            end
        end
        for _, player in Players:GetPlayers() do
            applyHitboxToPlayer(player)
        end
        Players.PlayerAdded:Connect(function(player)
            player.CharacterAdded:Connect(function(character)
                applyHitboxToPlayer(player)
            end)
        end)
        Players.PlayerRemoving:Connect(function(player)
            if player and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                local hrp = player.Character:WaitForChild("HumanoidRootPart")
                pcall(function()
                    hrp.Size = Vector3.new(2, 2, 1)
                    hrp.Transparency = 0
                    hrp.BrickColor = BrickColor.new("Medium stone grey")
                    hrp.Material = Enum.Material.Plastic
                    hrp.CanCollide = true
                end)
            end
        end)
        StarterGui:SetCore("SendNotification", {
            Title = "Hitbox Script Loaded",
            Text = "Unique hitbox script activated.",
            Duration = 5
        })
        Players.LocalPlayer.CharacterAdded:Connect(function(character)
            character.ChildAdded:Connect(function(child)
                if child:IsA("Tool") or child:IsA("Accessory") then
                    if HitboxSettings.Enabled and child:FindFirstChild("Handle") then
                        local handle = child.Handle
                        handle.Touched:Connect(function(hit)
                            if hit.Parent and hit.Parent:FindFirstChild("Humanoid") and hit.Parent.Name ~= Players.LocalPlayer.Name then
                                hit.Parent.Humanoid:TakeDamage(HitboxSettings.Damage)
                            end
                        end)
                    end
                end
            end)
        end)
    end
})
Tab:AddButton({
Name = "Server Hop",
Callback = function()
TeleportService:Teleport(3177438863, Players.LocalPlayer);
end
})
Tab:AddButton({
Name = "Server Hop To Dead Server",
Callback =function()
local Http = game:GetService("HttpService")
local TPS = game:GetService("TeleportService")
local Api = "https://games.roblox.com/v1/games/"
local _place = game.PlaceId
local _servers = Api.._place.."/servers/Public?sortOrder=Asc&limit=100"
function ListServers(cursor)
local Raw = game:HttpGet(_servers .. ((cursor and "&cursor="..cursor) or ""))
return Http:JSONDecode(Raw)
end
local Server, Next; repeat
local Servers = ListServers(Next)
Server = Servers.data[1]
Next = Servers.nextPageCursor
until Server
TPS:TeleportToPlaceInstance(_place,Server.id,Players.LocalPlayer)
end
})
Tab:AddButton({
Name = "Server Hop If You Die",
Callback = function()
task.wait(0.75)
Players.LocalPlayer.Character.Humanoid.Died:Connect(function()
local x = {}
for _, v in game:GetService("HttpService"):JSONDecode(game:HttpGetAsync("https://games.roblox.com/v1/games/" .. game.PlaceId .. "/servers/Public?sortOrder=Asc&limit=100")).data do
if type(v) == "table" and v.maxPlayers > v.playing and v.id ~= game.JobId then
x[#x + 1] = v.id
end
end
if #x > 0 then
TeleportService:TeleportToPlaceInstance(game.PlaceId, x[math.random(1, #x)])
else
print("Server not found.")
end
end)
end
})
Tab:AddButton({
Name = "Leave Server",
Callback = function()
game:Shutdown();
end
})
Tab:AddButton({
Name = "Rejoin Server",
Callback = function()
TeleportService:TeleportToPlaceInstance(game.PlaceId, game.JobId, Players.LocalPlayer)
end
})
Tab:AddButton({
Name = "Unload Gui",
Callback = function()
OrionLib:Destroy()
end
})
local Tab = Window:MakeTab({
Name = "Mobs",
Icon = "rbxassetid://4483345998",
PremiumOnly = false
})
local Section = Tab:AddSection({
Name = "Mobs Select Tab (Only Select 1 At A Time!)"
})
Tab:AddLabel("Just Get The Quest To Farm Your Mob")
Tab:AddLabel("Bandit")
Tab:AddButton({
Name = "Get Bandit Quest",
Callback = function()
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCMain1_WestCity:WaitForChild("Head").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer").InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer"), "NPCQuest_WestCity", 2, Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Quests"):WaitForChild("QuestNPCMain1_WestCity")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
})
Tab:AddButton({
Name = "Get Bandit Quest 2",
Callback = function()
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCMain1_WestCity:WaitForChild("HumanoidRootPart").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer").InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer"), "NPCQuest_WestCity", 2, Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Quests"):WaitForChild("QuestNPCMain1_WestCity")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
})
Tab:AddToggle({
    Name = "Auto Nearby Bandit Mobs (Back Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbybanditmobs_db",
    Callback = function(Value)
        Environment.autotpnearbybanditmobs = Value
        if not Value then return end
        while Environment.autotpnearbybanditmobs do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Bandit" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    local mobHRP = nearestMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.lookAt(mobHRP.Position + (mobHRP.CFrame.LookVector * -5), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Bandit Mobs (Back Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbybanditmobs_db",
    Callback = function(Value)
        Environment.autotpnearbybanditmobs = Value
        if not Value then return end
        while Environment.autotpnearbybanditmobs do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Bandit" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    local mobHRP = nearestMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.lookAt(mobHRP.Position + (mobHRP.CFrame.LookVector * -5), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Bandit Mobs 2 (Head Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbybanditmobs2_db",
    Callback = function(Value)
        Environment.autotpnearbybanditmobs2 = Value
        if not Value then return end
        while Environment.autotpnearbybanditmobs2 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Bandit" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("Head").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    tweenTo(rootPart, nearestMob:WaitForChild("Head").CFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Bandit Mobs 3 (Front Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbybanditmobs3_db",
    Callback = function(Value)
        Environment.autotpnearbybanditmobs3 = Value
        if not Value then return end
        while Environment.autotpnearbybanditmobs3 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Bandit" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    local mobHRP = nearestMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.lookAt(mobHRP.Position + (mobHRP.CFrame.LookVector * 5), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Bandit Mobs 4 (Under Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbybanditmobs4_db",
    Callback = function(Value)
        Environment.autotpnearbybanditmobs4 = Value
        if not Value then return end
        while Environment.autotpnearbybanditmobs4 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Bandit" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    local mobHRP = nearestMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.new(mobHRP.Position - Vector3.new(0, 5, 0), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddLabel("Mad Bandit")
Tab:AddButton({
Name = "Get Mad Bandit Quest",
Callback = function()
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCMain1_WestCity:WaitForChild("Head").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer").InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer"), "NPCQuest_WestCity", 7, Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Quests"):WaitForChild("QuestNPCMain1_WestCity")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
})
Tab:AddButton({
Name = "Get Mad Bandit Quest 2",
Callback = function()
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCMain1_WestCity:WaitForChild("HumanoidRootPart").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer").InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer"), "NPCQuest_WestCity", 7, Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Quests"):WaitForChild("QuestNPCMain1_WestCity")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
})
Tab:AddToggle({
    Name = "Auto Nearby Mad Bandit Mobs (Back Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbymadbanditmobs_db",
    Callback = function(Value)
        Environment.autotpnearbymadbanditmobs = Value
        if not Value then return end
        while Environment.autotpnearbymadbanditmobs do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Mad Bandit" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    local mobHRP = nearestMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.lookAt(mobHRP.Position + (mobHRP.CFrame.LookVector * -5), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Mad Bandit Mobs 2 (Head Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbymadbanditmobs2_db",
    Callback = function(Value)
        Environment.autotpnearbymadbanditmobs2 = Value
        if not Value then return end
        while Environment.autotpnearbymadbanditmobs2 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Mad Bandit" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("Head").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    tweenTo(rootPart, nearestMob:WaitForChild("Head").CFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Mad Bandit Mobs 3 (Front Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbymadbanditmobs3_db",
    Callback = function(Value)
        Environment.autotpnearbymadbanditmobs3 = Value
        if not Value then return end
        while Environment.autotpnearbymadbanditmobs3 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Mad Bandit" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    local mobHRP = nearestMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.lookAt(mobHRP.Position + (mobHRP.CFrame.LookVector * 5), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Mad Bandit Mobs 4 (Under Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbymadbanditmobs4_db",
    Callback = function(Value)
        Environment.autotpnearbymadbanditmobs4 = Value
        if not Value then return end
        while Environment.autotpnearbymadbanditmobs4 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Mad Bandit" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    local mobHRP = nearestMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.new(mobHRP.Position - Vector3.new(0, 5, 0), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddLabel("Evil Thug")
Tab:AddButton({
Name = "Get Evil Thug Quest",
Callback = function()
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCMain1_WestCity:WaitForChild("Head").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer").InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer"), "NPCQuest_WestCity", 12, Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Quests"):WaitForChild("QuestNPCMain1_WestCity")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
})
Tab:AddButton({
Name = "Get Evil Thug Quest",
Callback = function()
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCMain1_WestCity:WaitForChild("HumanoidRootPart").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer").InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer"), "NPCQuest_WestCity", 12, Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Quests"):WaitForChild("QuestNPCMain1_WestCity")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
})
Tab:AddToggle({
    Name = "Auto Nearby Evil Thug Mobs (Back Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyevilthugmobs_db",
    Callback = function(Value)
        Environment.autotpnearbyevilthugmobs = Value
        if not Value then return end
        while Environment.autotpnearbyevilthugmobs do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Evil Thug" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    local mobHRP = nearestMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.lookAt(mobHRP.Position + (mobHRP.CFrame.LookVector * -5), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Evil Thug Mobs 2 (Head Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyevilthugmobs2_db",
    Callback = function(Value)
        Environment.autotpnearbyevilthugmobs2 = Value
        if not Value then return end
        while Environment.autotpnearbyevilthugmobs2 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Evil Thug" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("Head").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    tweenTo(rootPart, nearestMob:WaitForChild("Head").CFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Evil Thug Mobs 3 (Front Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyevilthugmobs3_db",
    Callback = function(Value)
        Environment.autotpnearbyevilthugmobs3 = Value
        if not Value then return end
        while Environment.autotpnearbyevilthugmobs3 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Evil Thug" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    local mobHRP = nearestMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.lookAt(mobHRP.Position + (mobHRP.CFrame.LookVector * 5), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Evil Thug Mobs 4 (Under Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyevilthugmobs4_db",
    Callback = function(Value)
        Environment.autotpnearbyevilthugmobs4 = Value
        if not Value then return end
        while Environment.autotpnearbyevilthugmobs4 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Evil Thug" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    local mobHRP = nearestMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.new(mobHRP.Position - Vector3.new(0, 5, 0), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddLabel("Tigreal Tribe")
Tab:AddButton({
Name = "Get Tigreal Tribe Quest",
Callback = function()
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCMain1_WestCity:WaitForChild("Head").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer").InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer"), "NPCQuest_WestCity", 17, Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Quests"):WaitForChild("QuestNPCMain1_WestCity")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
})
Tab:AddButton({
Name = "Get Tigreal Tribe Quest 2",
Callback = function()
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCMain1_WestCity:WaitForChild("HumanoidRootPart").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer").InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer"), "NPCQuest_WestCity", 17, Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Quests"):WaitForChild("QuestNPCMain1_WestCity")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
})
Tab:AddToggle({
    Name = "Auto Nearby Tigreal Tribe Mobs (Back Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbytigrealtribemobs_db",
    Callback = function(Value)
        Environment.autotpnearbytigrealtribemobs = Value
        if not Value then return end
        while Environment.autotpnearbytigrealtribemobs do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Tigreal Tribe" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    local mobHRP = nearestMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.lookAt(mobHRP.Position + (mobHRP.CFrame.LookVector * -5), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Tigreal Tribe Mobs 2 (Head Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbytigrealtribemobs2_db",
    Callback = function(Value)
        Environment.autotpnearbytigrealtribemobs2 = Value
        if not Value then return end
        while Environment.autotpnearbytigrealtribemobs2 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Tigreal Tribe" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("Head").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    tweenTo(rootPart, nearestMob:WaitForChild("Head").CFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Tigreal Tribe Mobs 3 (Front Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbytigrealtribemobs3_db",
    Callback = function(Value)
        Environment.autotpnearbytigrealtribemobs3 = Value
        if not Value then return end
        while Environment.autotpnearbytigrealtribemobs3 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Tigreal Tribe" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    local mobHRP = nearestMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.lookAt(mobHRP.Position + (mobHRP.CFrame.LookVector * 5), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Tigreal Tribe Mobs 4 (Under Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbytigrealtribemobs4_db",
    Callback = function(Value)
        Environment.autotpnearbytigrealtribemobs4 = Value
        if not Value then return end
        while Environment.autotpnearbytigrealtribemobs4 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Tigreal Tribe" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    local mobHRP = nearestMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.new(mobHRP.Position - Vector3.new(0, 5, 0), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddLabel("Gorilla Tribe")
Tab:AddButton({
Name = "Get Gorilla Tribe Quest",
Callback = function()
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCMain1_WestCity:WaitForChild("Head").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer").InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer"), "NPCQuest_WestCity", 22, Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Quests"):WaitForChild("QuestNPCMain1_WestCity")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
})
Tab:AddButton({
Name = "Get Gorilla Tribe Quest 2",
Callback = function()
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCMain1_WestCity:WaitForChild("HumanoidRootPart").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer").InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer"), "NPCQuest_WestCity", 22, Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Quests"):WaitForChild("QuestNPCMain1_WestCity")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
})
Tab:AddToggle({
    Name = "Auto Nearby Gorilla Tribe Mobs (Back Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbygorillatribemobs_db",
    Callback = function(Value)
        Environment.autotpnearbygorillatribemobs = Value
        if not Value then return end
        while Environment.autotpnearbygorillatribemobs do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Gorilla Tribe" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    local mobHRP = nearestMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.lookAt(mobHRP.Position + (mobHRP.CFrame.LookVector * -5), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Gorilla Tribe Mobs 2 (Head Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbygorillatribemobs2_db",
    Callback = function(Value)
        Environment.autotpnearbygorillatribemobs2 = Value
        if not Value then return end
        while Environment.autotpnearbygorillatribemobs2 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Gorilla Tribe" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("Head").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    tweenTo(rootPart, nearestMob:WaitForChild("Head").CFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Gorilla Tribe Mobs 3 (Front Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbygorillatribemobs3_db",
    Callback = function(Value)
        Environment.autotpnearbygorillatribemobs3 = Value
        if not Value then return end
        while Environment.autotpnearbygorillatribemobs3 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Gorilla Tribe" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    local mobHRP = nearestMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.lookAt(mobHRP.Position + (mobHRP.CFrame.LookVector * 5), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Gorilla Tribe Mobs 4 (Under Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbygorillatribemobs4_db",
    Callback = function(Value)
        Environment.autotpnearbygorillatribemobs4 = Value
        if not Value then return end
        while Environment.autotpnearbygorillatribemobs4 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Gorilla Tribe" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    local mobHRP = nearestMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.new(mobHRP.Position - Vector3.new(0, 5, 0), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddLabel("Puriza Minion")
Tab:AddButton({
Name = "Get Puriza Minion Quest",
Callback = function()
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.Misc.NPC.Quests.QuestNPCMain2_PurizaArea:WaitForChild("Head").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7").knit.Services.DialogService.RF.Answer.InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7").knit.Services.DialogService.RF.Answer, "NPCQuest_PurizaArea", 7, Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Quests"):WaitForChild("QuestNPCMain2_PurizaArea")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
})
Tab:AddButton({
Name = "Get Puriza Minion Quest 2",
Callback = function()
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.Misc.NPC.Quests.QuestNPCMain2_PurizaArea:WaitForChild("HumanoidRootPart").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7").knit.Services.DialogService.RF.Answer.InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7").knit.Services.DialogService.RF.Answer, "NPCQuest_PurizaArea", 7, Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Quests"):WaitForChild("QuestNPCMain2_PurizaArea")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
})
Tab:AddToggle({
    Name = "Auto Nearby Puriza Minion Mobs (Back Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbypurizaminionmobs_db",
    Callback = function(Value)
        Environment.autotpnearbypurizaminionmobs = Value
        if not Value then return end
        while Environment.autotpnearbypurizaminionmobs do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Puriza Minion" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    local mobHRP = nearestMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.lookAt(mobHRP.Position + (mobHRP.CFrame.LookVector * -5), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Puriza Minion Mobs 2 (Head Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbypurizaminionmobs2_db",
    Callback = function(Value)
        Environment.autotpnearbypurizaminionmobs2 = Value
        if not Value then return end
        while Environment.autotpnearbypurizaminionmobs2 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Puriza Minion" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("Head").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    tweenTo(rootPart, nearestMob:WaitForChild("Head").CFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Puriza Minion Mobs 3 (Front Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbypurizaminionmobs3_db",
    Callback = function(Value)
        Environment.autotpnearbypurizaminionmobs3 = Value
        if not Value then return end
        while Environment.autotpnearbypurizaminionmobs3 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Puriza Minion" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    local mobHRP = nearestMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.lookAt(mobHRP.Position + (mobHRP.CFrame.LookVector * 5), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Puriza Minion Mobs 4 (Under Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbypurizaminionmobs4_db",
    Callback = function(Value)
        Environment.autotpnearbypurizaminionmobs4 = Value
        if not Value then return end
        while Environment.autotpnearbypurizaminionmobs4 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Puriza Minion" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    local mobHRP = nearestMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.new(mobHRP.Position - Vector3.new(0, 5, 0), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddLabel("Coolest Minion")
Tab:AddButton({
Name = "Get Coolest Minion Quest",
Callback = function()
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCMain3_CoolestArea:WaitForChild("Head").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7").knit.Services.DialogService.RF.Answer.InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7").knit.Services.DialogService.RF.Answer, "NPCQuest_CoolestArea", 2, Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Quests"):WaitForChild("QuestNPCMain3_CoolestArea")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
})
Tab:AddButton({
Name = "Get Coolest Minion Quest 2",
Callback = function()
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCMain3_CoolestArea:WaitForChild("HumanoidRootPart").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7").knit.Services.DialogService.RF.Answer.InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7").knit.Services.DialogService.RF.Answer, "NPCQuest_CoolestArea", 2, Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Quests"):WaitForChild("QuestNPCMain3_CoolestArea")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
})
Tab:AddToggle({
    Name = "Auto Nearby Coolest Minion Mobs (Back Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbycoolestminionmobs_db",
    Callback = function(Value)
        Environment.autotpnearbycoolestminionmobs = Value
        if not Value then return end
        while Environment.autotpnearbycoolestminionmobs do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Coolest Minion" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    local mobHRP = nearestMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.lookAt(mobHRP.Position + (mobHRP.CFrame.LookVector * -5), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Coolest Minion Mobs 2 (Head Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbycoolestminionmobs2_db",
    Callback = function(Value)
        Environment.autotpnearbycoolestminionmobs2 = Value
        if not Value then return end
        while Environment.autotpnearbycoolestminionmobs2 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Coolest Minion" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("Head").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    tweenTo(rootPart, nearestMob:WaitForChild("Head").CFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Coolest Minion Mobs 3 (Front Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbycoolestminionmobs3_db",
    Callback = function(Value)
        Environment.autotpnearbycoolestminionmobs3 = Value
        if not Value then return end
        while Environment.autotpnearbycoolestminionmobs3 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Coolest Minion" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    local mobHRP = nearestMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.lookAt(mobHRP.Position + (mobHRP.CFrame.LookVector * 5), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Coolest Minion Mobs 4 (Under Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbycoolestminionmobs4_db",
    Callback = function(Value)
        Environment.autotpnearbycoolestminionmobs4 = Value
        if not Value then return end
        while Environment.autotpnearbycoolestminionmobs4 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Coolest Minion" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    local mobHRP = nearestMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.new(mobHRP.Position - Vector3.new(0, 5, 0), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddLabel("Droid 17/Droid 18 Minion")
Tab:AddButton({
Name = "Get Droid 18 Minion Quest",
Callback = function()
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCMain4_Droid1718Area:WaitForChild("Head").CFrame
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCMain4_Droid1718Area:WaitForChild("HumanoidRootPart").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7").knit.Services.DialogService.RF.Answer.InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7").knit.Services.DialogService.RF.Answer, "NPCQuest_Droid1718Area", 2, Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Quests"):WaitForChild("QuestNPCMain4_Droid1718Area")}))
end
})
Tab:AddButton({
Name = "Get Droid 18 Minion Quest 2",
Callback = function()
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCMain4_Droid1718Area:WaitForChild("HumanoidRootPart").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7").knit.Services.DialogService.RF.Answer.InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7").knit.Services.DialogService.RF.Answer, "NPCQuest_Droid1718Area", 2, Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Quests"):WaitForChild("QuestNPCMain4_Droid1718Area")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
})
Tab:AddButton({
Name = "Get Droid 17 Minion Quest",
Callback = function()
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCMain4_Droid1718Area:WaitForChild("Head").CFrame
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCMain4_Droid1718Area:WaitForChild("HumanoidRootPart").CFrame
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
})
Tab:AddButton({
Name = "Get Droid 17 Minion Quest 2",
Callback = function()
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCMain4_Droid1718Area:WaitForChild("HumanoidRootPart").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7").knit.Services.DialogService.RF.Answer.InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7").knit.Services.DialogService.RF.Answer, "NPCQuest_Droid1718Area", 7, Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Quests"):WaitForChild("QuestNPCMain4_Droid1718Area")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
})
Tab:AddToggle({
    Name = "Auto Nearby Droid 18 Minion Mobs (Back Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbydroid18minionmobs_db",
    Callback = function(Value)
        Environment.autotpnearbydroid18minionmobs = Value
        if not Value then return end
        while Environment.autotpnearbydroid18minionmobs do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Droid 18 Minion" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    local mobHRP = nearestMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.lookAt(mobHRP.Position + (mobHRP.CFrame.LookVector * -5), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Droid 18 Minion Mobs 2 (Head Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbydroid18minionmobs2_db",
    Callback = function(Value)
        Environment.autotpnearbydroid18minionmobs2 = Value
        if not Value then return end
        while Environment.autotpnearbydroid18minionmobs2 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Droid 18 Minion" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("Head").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    tweenTo(rootPart, nearestMob:WaitForChild("Head").CFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Droid 18 Minion Mobs 3 (Front Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbydroid18minionmobs3_db",
    Callback = function(Value)
        Environment.autotpnearbydroid18minionmobs3 = Value
        if not Value then return end
        while Environment.autotpnearbydroid18minionmobs3 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Droid 18 Minion" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    local mobHRP = nearestMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.lookAt(mobHRP.Position + (mobHRP.CFrame.LookVector * 5), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Droid 18 Minion Mobs 4 (Under Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbydroid18minionmobs4_db",
    Callback = function(Value)
        Environment.autotpnearbydroid18minionmobs4 = Value
        if not Value then return end
        while Environment.autotpnearbydroid18minionmobs4 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Droid 18 Minion" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    local mobHRP = nearestMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.new(mobHRP.Position - Vector3.new(0, 5, 0), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Droid 17 Minion Mobs (Back Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbydroid17minionmobs_db",
    Callback = function(Value)
        Environment.autotpnearbydroid17minionmobs = Value
        if not Value then return end
        while Environment.autotpnearbydroid17minionmobs do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Droid 17 Minion" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    local mobHRP = nearestMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.lookAt(mobHRP.Position + (mobHRP.CFrame.LookVector * -5), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Droid 17 Minion Mobs 2 (Head Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbydroid17minionmobs2_db",
    Callback = function(Value)
        Environment.autotpnearbydroid17minionmobs2 = Value
        if not Value then return end
        while Environment.autotpnearbydroid17minionmobs2 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Droid 17 Minion" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("Head").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    tweenTo(rootPart, nearestMob:WaitForChild("Head").CFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Droid 17 Minion Mobs 3 (Front Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbydroid17minionmobs3_db",
    Callback = function(Value)
        Environment.autotpnearbydroid17minionmobs3 = Value
        if not Value then return end
        while Environment.autotpnearbydroid17minionmobs3 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Droid 17 Minion" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    local mobHRP = nearestMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.lookAt(mobHRP.Position + (mobHRP.CFrame.LookVector * 5), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Droid 17 Minion Mobs 4 (Under Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbydroid17minionmobs4_db",
    Callback = function(Value)
        Environment.autotpnearbydroid17minionmobs4 = Value
        if not Value then return end
        while Environment.autotpnearbydroid17minionmobs4 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Droid 17 Minion" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    local mobHRP = nearestMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.new(mobHRP.Position - Vector3.new(0, 5, 0), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddLabel("Atom Minion")
Tab:AddButton({
Name = "Get Atom Minion Quest",
Callback = function()
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCMain5_AtomArea:WaitForChild("Head").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7").knit.Services.DialogService.RF.Answer.InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7").knit.Services.DialogService.RF.Answer, "NPCQuest_AtomArea", 2, Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Quests"):WaitForChild("QuestNPCMain5_AtomArea")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
})
Tab:AddButton({
Name = "Get Atom Minion Quest 2",
Callback = function()
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCMain5_AtomArea:WaitForChild("HumanoidRootPart").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7").knit.Services.DialogService.RF.Answer.InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7").knit.Services.DialogService.RF.Answer, "NPCQuest_AtomArea", 2, Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Quests"):WaitForChild("QuestNPCMain5_AtomArea")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
})
Tab:AddToggle({
    Name = "Auto Nearby Atom Minion Mobs (Back Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyatomminionmobs_db",
    Callback = function(Value)
        Environment.autotpnearbyatomminionmobs = Value
        if not Value then return end
        while Environment.autotpnearbyatomminionmobs do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Atom Minion" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    local mobHRP = nearestMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.lookAt(mobHRP.Position + (mobHRP.CFrame.LookVector * -5), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Atom Minion Mobs 2 (Head Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyatomminionmobs2_db",
    Callback = function(Value)
        Environment.autotpnearbyatomminionmobs2 = Value
        if not Value then return end
        while Environment.autotpnearbyatomminionmobs2 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Atom Minion" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("Head").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    tweenTo(rootPart, nearestMob:WaitForChild("Head").CFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Atom Minion Mobs 3 (Front Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyatomminionmobs3_db",
    Callback = function(Value)
        Environment.autotpnearbyatomminionmobs3 = Value
        if not Value then return end
        while Environment.autotpnearbyatomminionmobs3 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Atom Minion" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    local mobHRP = nearestMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.lookAt(mobHRP.Position + (mobHRP.CFrame.LookVector * 5), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Atom Minion Mobs 4 (Under Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyatomminionmobs4_db",
    Callback = function(Value)
        Environment.autotpnearbyatomminionmobs4 = Value
        if not Value then return end
        while Environment.autotpnearbyatomminionmobs4 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Atom Minion" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    local mobHRP = nearestMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.new(mobHRP.Position - Vector3.new(0, 5, 0), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddLabel("Jinbu Minion")
Tab:AddButton({
Name = "Get Jinbu Minion Quest",
Callback = function()
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCMain6_JinbuArea:WaitForChild("Head").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7").knit.Services.DialogService.RF.Answer.InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7").knit.Services.DialogService.RF.Answer, "NPCQuest_JinbuArea", 2, Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Quests"):WaitForChild("QuestNPCMain6_JinbuArea")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
})
Tab:AddButton({
Name = "Get Jinbu Minion Quest 2",
Callback = function()
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCMain6_JinbuArea:WaitForChild("HumanoidRootPart").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7").knit.Services.DialogService.RF.Answer.InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7").knit.Services.DialogService.RF.Answer, "NPCQuest_JinbuArea", 2, Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Quests"):WaitForChild("QuestNPCMain6_JinbuArea")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
})
Tab:AddToggle({
    Name = "Auto Nearby Jinbu Minion Mobs (Back Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyjinbuminionmobs_db",
    Callback = function(Value)
        Environment.autotpnearbyjinbuminionmobs = Value
        if not Value then return end
        while Environment.autotpnearbyjinbuminionmobs do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Jinbu Minion" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    local mobHRP = nearestMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.lookAt(mobHRP.Position + (mobHRP.CFrame.LookVector * -5), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Jinbu Minion Mobs 2 (Head Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyjinbuminionmobs2_db",
    Callback = function(Value)
        Environment.autotpnearbyjinbuminionmobs2 = Value
        if not Value then return end
        while Environment.autotpnearbyjinbuminionmobs2 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Jinbu Minion" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("Head").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    tweenTo(rootPart, nearestMob:WaitForChild("Head").CFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Jinbu Minion Mobs 3 (Front Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyjinbuminionmobs3_db",
    Callback = function(Value)
        Environment.autotpnearbyjinbuminionmobs3 = Value
        if not Value then return end
        while Environment.autotpnearbyjinbuminionmobs3 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Jinbu Minion" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    local mobHRP = nearestMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.lookAt(mobHRP.Position + (mobHRP.CFrame.LookVector * 5), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Jinbu Minion Mobs 4 (Under Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyjinbuminionmobs4_db",
    Callback = function(Value)
        Environment.autotpnearbyjinbuminionmobs4 = Value
        if not Value then return end
        while Environment.autotpnearbyjinbuminionmobs4 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Jinbu Minion" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    local mobHRP = nearestMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.new(mobHRP.Position - Vector3.new(0, 5, 0), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddLabel("BabyVeggy Minion")
Tab:AddButton({
Name = "Get BabyVeggy Minion Quest",
Callback = function()
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCMain7_VeggyArea:WaitForChild("Head").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7").knit.Services.DialogService.RF.Answer.InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7").knit.Services.DialogService.RF.Answer, "NPCQuest_VeggyArea", 2, Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Quests"):WaitForChild("QuestNPCMain7_VeggyArea")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
})
Tab:AddButton({
Name = "Get BabyVeggy Minion Quest 2",
Callback = function()
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCMain7_VeggyArea:WaitForChild("HumanoidRootPart").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7").knit.Services.DialogService.RF.Answer.InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7").knit.Services.DialogService.RF.Answer, "NPCQuest_VeggyArea", 2, Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Quests"):WaitForChild("QuestNPCMain7_VeggyArea")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
})
Tab:AddToggle({
    Name = "Auto Nearby BabyVeggy Minion Mobs (Back Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbybabyveggyminionmobs_db",
    Callback = function(Value)
        Environment.autotpnearbybabyveggyminionmobs = Value
        if not Value then return end
        while Environment.autotpnearbybabyveggyminionmobs do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "BabyVeggy Minion" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    local mobHRP = nearestMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.lookAt(mobHRP.Position + (mobHRP.CFrame.LookVector * -5), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby BabyVeggy Minion Mobs 2 (Head Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbybabyveggyminionmobs2_db",
    Callback = function(Value)
        Environment.autotpnearbybabyveggyminionmobs2 = Value
        if not Value then return end
        while Environment.autotpnearbybabyveggyminionmobs2 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "BabyVeggy Minion" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("Head").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    tweenTo(rootPart, nearestMob:WaitForChild("Head").CFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby BabyVeggy Minion Mobs 3 (Front Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbybabyveggyminionmobs3_db",
    Callback = function(Value)
        Environment.autotpnearbybabyveggyminionmobs3 = Value
        if not Value then return end
        while Environment.autotpnearbybabyveggyminionmobs3 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "BabyVeggy Minion" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    local mobHRP = nearestMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.lookAt(mobHRP.Position + (mobHRP.CFrame.LookVector * 5), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby BabyVeggy Minion Mobs 4 (Under Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbybabyveggyminionmobs4_db",
    Callback = function(Value)
        Environment.autotpnearbybabyveggyminionmobs4 = Value
        if not Value then return end
        while Environment.autotpnearbybabyveggyminionmobs4 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "BabyVeggy Minion" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    local mobHRP = nearestMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.new(mobHRP.Position - Vector3.new(0, 5, 0), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddLabel("BlackKarrot Minion")
Tab:AddButton({
Name = "Get BlackKarrot Minion Quest",
Callback = function()
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCMain8_BlackKarrotArea:WaitForChild("Head").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7").knit.Services.DialogService.RF.Answer.InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7").knit.Services.DialogService.RF.Answer, "NPCQuest_BlackKarrotArea", 2, Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Quests"):WaitForChild("QuestNPCMain8_BlackKarrotArea")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
})
Tab:AddButton({
Name = "Get BlackKarrot Minion Quest 2",
Callback = function()
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCMain8_BlackKarrotArea:WaitForChild("HumanoidRootPart").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7").knit.Services.DialogService.RF.Answer.InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7").knit.Services.DialogService.RF.Answer, "NPCQuest_BlackKarrotArea", 2, Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Quests"):WaitForChild("QuestNPCMain8_BlackKarrotArea")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
})
Tab:AddToggle({
    Name = "Auto Nearby BlackKarrot Minion Mobs (Back Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyblackkarrotminionmobs_db",
    Callback = function(Value)
        Environment.autotpnearbyblackkarrotminionmobs = Value
        if not Value then return end
        while Environment.autotpnearbyblackkarrotminionmobs do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "BlackKarrot Minion" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    local mobHRP = nearestMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.lookAt(mobHRP.Position + (mobHRP.CFrame.LookVector * -5), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby BlackKarrot Minion Mobs 2 (Head Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyblackkarrotminionmobs2_db",
    Callback = function(Value)
        Environment.autotpnearbyblackkarrotminionmobs2 = Value
        if not Value then return end
        while Environment.autotpnearbyblackkarrotminionmobs2 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "BlackKarrot Minion" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("Head").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    tweenTo(rootPart, nearestMob:WaitForChild("Head").CFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby BlackKarrot Minion Mobs 3 (Front Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyblackkarrotminionmobs3_db",
    Callback = function(Value)
        Environment.autotpnearbyblackkarrotminionmobs3 = Value
        if not Value then return end
        while Environment.autotpnearbyblackkarrotminionmobs3 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "BlackKarrot Minion" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    local mobHRP = nearestMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.lookAt(mobHRP.Position + (mobHRP.CFrame.LookVector * 5), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby BlackKarrot Minion Mobs 4 (Under Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyblackkarrotminionmobs4_db",
    Callback = function(Value)
        Environment.autotpnearbyblackkarrotminionmobs4 = Value
        if not Value then return end
        while Environment.autotpnearbyblackkarrotminionmobs4 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "BlackKarrot Minion" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    local mobHRP = nearestMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.new(mobHRP.Position - Vector3.new(0, 5, 0), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddLabel("Brawly Minion")
Tab:AddButton({
Name = "Get Brawly Minion Quest",
Callback = function()
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCMain9_BrawlyArea:WaitForChild("Head").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7").knit.Services.DialogService.RF.Answer.InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7").knit.Services.DialogService.RF.Answer, "NPCQuest_BrawlyArea", 2, Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Quests"):WaitForChild("QuestNPCMain9_BrawlyArea")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
})
Tab:AddButton({
Name = "Get Brawly Minion Quest 2",
Callback = function()
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCMain9_BrawlyArea:WaitForChild("HumanoidRootPart").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7").knit.Services.DialogService.RF.Answer.InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7").knit.Services.DialogService.RF.Answer, "NPCQuest_BrawlyArea", 2, Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Quests"):WaitForChild("QuestNPCMain9_BrawlyArea")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
})
Tab:AddToggle({
    Name = "Auto Nearby Brawly Minion Mobs (Back Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbybrawlyminionmobs_db",
    Callback = function(Value)
        Environment.autotpnearbybrawlyminionmobs = Value
        if not Value then return end
        while Environment.autotpnearbybrawlyminionmobs do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Brawly Minion" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    local mobHRP = nearestMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.lookAt(mobHRP.Position + (mobHRP.CFrame.LookVector * -5), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Brawly Minion Mobs 2 (Head Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbybrawlyminionmobs2_db",
    Callback = function(Value)
        Environment.autotpnearbybrawlyminionmobs2 = Value
        if not Value then return end
        while Environment.autotpnearbybrawlyminionmobs2 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Brawly Minion" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("Head").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    tweenTo(rootPart, nearestMob:WaitForChild("Head").CFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Brawly Minion Mobs 3 (Front Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbybrawlyminionmobs3_db",
    Callback = function(Value)
        Environment.autotpnearbybrawlyminionmobs3 = Value
        if not Value then return end
        while Environment.autotpnearbybrawlyminionmobs3 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Brawly Minion" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    local mobHRP = nearestMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.lookAt(mobHRP.Position + (mobHRP.CFrame.LookVector * 5), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Brawly Minion Mobs 4 (Under Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbybrawlyminionmobs4_db",
    Callback = function(Value)
        Environment.autotpnearbybrawlyminionmobs4 = Value
        if not Value then return end
        while Environment.autotpnearbybrawlyminionmobs4 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Brawly Minion" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    local mobHRP = nearestMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.new(mobHRP.Position - Vector3.new(0, 5, 0), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddLabel("Jigray Minion")
Tab:AddButton({
Name = "Get Jigray Minion Quest",
Callback = function()
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCMain10_JigrayArea:WaitForChild("Head").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer").InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer"), "NPCQuest_JigrayArea", 2, Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Quests"):WaitForChild("QuestNPCMain10_JigrayArea")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
})
Tab:AddButton({
Name = "Get Jigray Minion Quest 2",
Callback = function()
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCMain10_JigrayArea:WaitForChild("HumanoidRootPart").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer").InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer"), "NPCQuest_JigrayArea", 2, Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Quests"):WaitForChild("QuestNPCMain10_JigrayArea")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
})
Tab:AddToggle({
    Name = "Auto Nearby Jigray Mobs (Back Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyjigraymobs_db",
    Callback = function(Value)
        Environment.autotpnearbyjigraymobs = Value
        if not Value then return end
        while Environment.autotpnearbyjigraymobs do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Jigray Minion" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    local mobHRP = nearestMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.lookAt(mobHRP.Position + (mobHRP.CFrame.LookVector * -5), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Jigray Mobs 2 (Head Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyjigraymobs2_db",
    Callback = function(Value)
        Environment.autotpnearbyjigraymobs2 = Value
        if not Value then return end
        while Environment.autotpnearbyjigraymobs2 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Jigray Minion" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("Head").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    tweenTo(rootPart, nearestMob:WaitForChild("Head").CFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Jigray Mobs 3 (Front Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyjigraymobs3_db",
    Callback = function(Value)
        Environment.autotpnearbyjigraymobs3 = Value
        if not Value then return end
        while Environment.autotpnearbyjigraymobs3 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Jigray Minion" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    local mobHRP = nearestMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.lookAt(mobHRP.Position + (mobHRP.CFrame.LookVector * 5), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Jigray Mobs 4 (Under Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyjigraymobs4_db",
    Callback = function(Value)
        Environment.autotpnearbyjigraymobs4 = Value
        if not Value then return end
        while Environment.autotpnearbyjigraymobs4 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Jigray Minion" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    local mobHRP = nearestMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.new(mobHRP.Position - Vector3.new(0, 5, 0), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddLabel("Just Get The Quest To Farm Your Boss Mob")
Tab:AddLabel("Puriza")
Tab:AddButton({
Name = "Get Puriza Quest",
Callback = function()
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCMain2_PurizaArea:WaitForChild("Head").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer").InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer"), "NPCQuest_PurizaArea", 2, Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Quests"):WaitForChild("QuestNPCMain2_PurizaArea")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
})
Tab:AddButton({
Name = "Get Puriza Quest 2",
Callback = function()
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCMain2_PurizaArea:WaitForChild("HumanoidRootPart").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer").InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer"), "NPCQuest_PurizaArea", 2, Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Quests"):WaitForChild("QuestNPCMain2_PurizaArea")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
})
Tab:AddToggle({
    Name = "Auto Nearby Puriza Boss Mobs (Back Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbypurizabossmobs_db",
    Callback = function(Value)
        Environment.autotpnearbypurizabossmobs = Value
        if not Value then return end
        while Environment.autotpnearbypurizabossmobs do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local originalCanCollide = rootPart.CanCollide
                    rootPart.CanCollide = false
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                    rootPart.CanCollide = originalCanCollide
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Puriza" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local offsetPosition = mobHRP.Position - (mobHRP.CFrame.LookVector * -5)
                    local targetCFrame = CFrame.new(offsetPosition, mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Puriza Boss Mobs 2 (Head Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbypurizabossmobs2_db",
    Callback = function(Value)
        Environment.autotpnearbypurizabossmobs2 = Value
        if not Value then return end
        while Environment.autotpnearbypurizabossmobs2 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local originalCanCollide = rootPart.CanCollide
                    rootPart.CanCollide = false
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                    rootPart.CanCollide = originalCanCollide
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Puriza" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("Head").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    tweenTo(rootPart, nearestBossMob:WaitForChild("Head").CFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Puriza Boss Mobs 3 (Front Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbypurizabossmobs3_db",
    Callback = function(Value)
        Environment.autotpnearbypurizabossmobs3 = Value
        if not Value then return end
        while Environment.autotpnearbypurizabossmobs3 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local originalCanCollide = rootPart.CanCollide
                    rootPart.CanCollide = false
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                    rootPart.CanCollide = originalCanCollide
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Puriza" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local offsetPosition = mobHRP.Position + (mobHRP.CFrame.LookVector * 5)
                    local targetCFrame = CFrame.new(offsetPosition, mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Puriza Boss Mobs 4 (Under Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbypurizabossmobs4_db",
    Callback = function(Value)
        Environment.autotpnearbypurizabossmobs4 = Value
        if not Value then return end
        while Environment.autotpnearbypurizabossmobs4 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local originalCanCollide = rootPart.CanCollide
                    rootPart.CanCollide = false
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                    rootPart.CanCollide = originalCanCollide
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Puriza" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.new(mobHRP.Position - Vector3.new(0, 5, 0), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddLabel("Coolest")
Tab:AddButton({
Name = "Get Coolest Quest",
Callback = function()
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCMain3_CoolestArea:WaitForChild("Head").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer").InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer"), "NPCQuest_CoolestArea", 7, Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Quests"):WaitForChild("QuestNPCMain3_CoolestArea")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
})
Tab:AddButton({
Name = "Get Coolest Quest 2",
Callback = function()
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCMain3_CoolestArea:WaitForChild("HumanoidRootPart").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer").InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer"), "NPCQuest_CoolestArea", 7, Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Quests"):WaitForChild("QuestNPCMain3_CoolestArea")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
})
Tab:AddToggle({
    Name = "Auto Nearby Coolest Boss Mobs (Back Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbycoolestbossmobs_db",
    Callback = function(Value)
        Environment.autotpnearbycoolestbossmobs = Value
        if not Value then return end
        while Environment.autotpnearbycoolestbossmobs do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local originalCanCollide = rootPart.CanCollide
                    rootPart.CanCollide = false
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                    rootPart.CanCollide = originalCanCollide
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Coolest" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local offsetPosition = mobHRP.Position + (mobHRP.CFrame.LookVector * 5) -- Simplified: - (mobHRP.CFrame.LookVector * -5) = + (mobHRP.CFrame.LookVector * 5)
                    local targetCFrame = CFrame.new(offsetPosition, mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Coolest Boss Mobs 2 (Head Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbycoolestbossmobs2_db",
    Callback = function(Value)
        Environment.autotpnearbycoolestbossmobs2 = Value
        if not Value then return end
        while Environment.autotpnearbycoolestbossmobs2 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local originalCanCollide = rootPart.CanCollide
                    rootPart.CanCollide = false
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                    rootPart.CanCollide = originalCanCollide
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Coolest" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("Head").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    tweenTo(rootPart, nearestBossMob:WaitForChild("Head").CFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Coolest Boss Mobs 3 (Front Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbycoolestbossmobs3_db",
    Callback = function(Value)
        Environment.autotpnearbycoolestbossmobs3 = Value
        if not Value then return end
        while Environment.autotpnearbycoolestbossmobs3 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local originalCanCollide = rootPart.CanCollide
                    rootPart.CanCollide = false
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                    rootPart.CanCollide = originalCanCollide
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Coolest" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local offsetPosition = mobHRP.Position + (mobHRP.CFrame.LookVector * 5)
                    local targetCFrame = CFrame.new(offsetPosition, mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Coolest Boss Mobs 4 (Under Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbycoolestbossmobs4_db",
    Callback = function(Value)
        Environment.autotpnearbycoolestbossmobs4 = Value
        if not Value then return end
        while Environment.autotpnearbycoolestbossmobs4 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local originalCanCollide = rootPart.CanCollide
                    rootPart.CanCollide = false
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                    rootPart.CanCollide = originalCanCollide
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Coolest" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.new(mobHRP.Position - Vector3.new(0, 5, 0), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddLabel("Droid 17/18")
Tab:AddButton({
Name = "Get Droid 18 Quest",
Callback = function()
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCMain4_Droid1718Area:WaitForChild("Head").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer").InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer"), "NPCQuest_Droid1718Area", 12, Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Quests"):WaitForChild("QuestNPCMain4_Droid1718Area")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
})
Tab:AddButton({
Name = "Get Droid 18 Quest 2",
Callback = function()
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCMain4_Droid1718Area:WaitForChild("HumanoidRootPart").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer").InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer"), "NPCQuest_Droid1718Area", 12, Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Quests"):WaitForChild("QuestNPCMain4_Droid1718Area")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
})
Tab:AddButton({
Name = "Get Droid 17 Quest",
Callback = function()
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCMain4_Droid1718Area:WaitForChild("Head").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer").InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer"), "NPCQuest_Droid1718Area", 17, Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Quests"):WaitForChild("QuestNPCMain4_Droid1718Area")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
})
Tab:AddButton({
Name = "Get Droid 17 Quest 2",
Callback = function()
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCMain4_Droid1718Area:WaitForChild("HumanoidRootPart").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer").InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer"), "NPCQuest_Droid1718Area", 17, Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Quests"):WaitForChild("QuestNPCMain4_Droid1718Area")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
})
Tab:AddToggle({
    Name = "Auto Nearby Droid 18 Boss Mobs (Back Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbydroid18bossmobs_db",
    Callback = function(Value)
        Environment.autotpnearbydroid18bossmobs = Value
        if not Value then return end
        while Environment.autotpnearbydroid18bossmobs do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local originalCanCollide = rootPart.CanCollide
                    rootPart.CanCollide = false
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                    rootPart.CanCollide = originalCanCollide
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Droid 18" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local offsetPosition = mobHRP.Position - (mobHRP.CFrame.LookVector * 5)
                    local targetCFrame = CFrame.new(offsetPosition, mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Droid 18 Boss Mobs 2 (Head Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbydroid18bossmobs2_db",
    Callback = function(Value)
        Environment.autotpnearbydroid18bossmobs2 = Value
        if not Value then return end
        while Environment.autotpnearbydroid18bossmobs2 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local originalCanCollide = rootPart.CanCollide
                    rootPart.CanCollide = false
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                    rootPart.CanCollide = originalCanCollide
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Droid 18" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("Head").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    tweenTo(rootPart, nearestBossMob:WaitForChild("Head").CFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Droid 18 Boss Mobs 3 (Front Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbydroid18bossmobs3_db",
    Callback = function(Value)
        Environment.autotpnearbydroid18bossmobs3 = Value
        if not Value then return end
        while Environment.autotpnearbydroid18bossmobs3 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local originalCanCollide = rootPart.CanCollide
                    rootPart.CanCollide = false
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                    rootPart.CanCollide = originalCanCollide
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Droid 18" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local offsetPosition = mobHRP.Position + (mobHRP.CFrame.LookVector * 5)
                    local targetCFrame = CFrame.new(offsetPosition, mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Droid 18 Boss Mobs 4 (Under Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbydroid18bossmobs4_db",
    Callback = function(Value)
        Environment.autotpnearbydroid18bossmobs4 = Value
        if not Value then return end
        while Environment.autotpnearbydroid18bossmobs4 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local originalCanCollide = rootPart.CanCollide
                    rootPart.CanCollide = false
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                    rootPart.CanCollide = originalCanCollide
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Droid 18" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.new(mobHRP.Position - Vector3.new(0, 5, 0), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Droid 17 Boss Mobs (Back Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbydroid17bossmobs_db",
    Callback = function(Value)
        Environment.autotpnearbydroid17bossmobs = Value
        if not Value then return end
        while Environment.autotpnearbydroid17bossmobs do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local originalCanCollide = rootPart.CanCollide
                    rootPart.CanCollide = false
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                    rootPart.CanCollide = originalCanCollide
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Droid 17" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local offsetPosition = mobHRP.Position - (mobHRP.CFrame.LookVector * 5)
                    local targetCFrame = CFrame.new(offsetPosition, mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Droid 17 Boss Mobs 2 (Head Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbydroid17bossmobs2_db",
    Callback = function(Value)
        Environment.autotpnearbydroid17bossmobs2 = Value
        if not Value then return end
        while Environment.autotpnearbydroid17bossmobs2 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local originalCanCollide = rootPart.CanCollide
                    rootPart.CanCollide = false
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                    rootPart.CanCollide = originalCanCollide
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Droid 17" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("Head").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    tweenTo(rootPart, nearestBossMob:WaitForChild("Head").CFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Droid 17 Boss Mobs 3 (Front Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbydroid17bossmobs3_db",
    Callback = function(Value)
        Environment.autotpnearbydroid17bossmobs3 = Value
        if not Value then return end
        while Environment.autotpnearbydroid17bossmobs3 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local originalCanCollide = rootPart.CanCollide
                    rootPart.CanCollide = false
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                    rootPart.CanCollide = originalCanCollide
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Droid 17" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local offsetPosition = mobHRP.Position + (mobHRP.CFrame.LookVector * 5)
                    local targetCFrame = CFrame.new(offsetPosition, mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Droid 17 Boss Mobs 4 (Under Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbydroid17bossmobs4_db",
    Callback = function(Value)
        Environment.autotpnearbydroid17bossmobs4 = Value
        if not Value then return end
        while Environment.autotpnearbydroid17bossmobs4 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local originalCanCollide = rootPart.CanCollide
                    rootPart.CanCollide = false
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                    rootPart.CanCollide = originalCanCollide
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Droid 17" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.new(mobHRP.Position - Vector3.new(0, 5, 0), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddLabel("Atom")
Tab:AddButton({
Name = "Get Atom Quest",
Callback = function()
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCMain5_AtomArea:WaitForChild("Head").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer").InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer"), "NPCQuest_AtomArea", 7, Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Quests"):WaitForChild("QuestNPCMain5_AtomArea")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
})
Tab:AddButton({
Name = "Get Atom Quest 2",
Callback = function()
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCMain5_AtomArea:WaitForChild("HumanoidRootPart").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer").InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer"), "NPCQuest_AtomArea", 7, Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Quests"):WaitForChild("QuestNPCMain5_AtomArea")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
})
Tab:AddToggle({
    Name = "Auto Nearby Atom Boss Mobs (Back Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyatombossmobs_db",
    Callback = function(Value)
        Environment.autotpnearbyatombossmobs = Value
        if not Value then return end
        while Environment.autotpnearbyatombossmobs do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local originalCanCollide = rootPart.CanCollide
                    rootPart.CanCollide = false
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                    rootPart.CanCollide = originalCanCollide
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Atom" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local offsetPosition = mobHRP.Position - (mobHRP.CFrame.LookVector * 5)
                    local targetCFrame = CFrame.new(offsetPosition, mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Atom Boss Mobs 2 (Head Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyatombossmobs2_db",
    Callback = function(Value)
        Environment.autotpnearbyatombossmobs2 = Value
        if not Value then return end
        while Environment.autotpnearbyatombossmobs2 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local originalCanCollide = rootPart.CanCollide
                    rootPart.CanCollide = false
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                    rootPart.CanCollide = originalCanCollide
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Atom" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("Head").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    tweenTo(rootPart, nearestBossMob:WaitForChild("Head").CFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Atom Boss Mobs 3 (Front Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyatombossmobs3_db",
    Callback = function(Value)
        Environment.autotpnearbyatombossmobs3 = Value
        if not Value then return end
        while Environment.autotpnearbyatombossmobs3 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local originalCanCollide = rootPart.CanCollide
                    rootPart.CanCollide = false
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                    rootPart.CanCollide = originalCanCollide
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Atom" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local offsetPosition = mobHRP.Position + (mobHRP.CFrame.LookVector * 5)
                    local targetCFrame = CFrame.new(offsetPosition, mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Atom Boss Mobs 4 (Under Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyatombossmobs4_db",
    Callback = function(Value)
        Environment.autotpnearbyatombossmobs4 = Value
        if not Value then return end
        while Environment.autotpnearbyatombossmobs4 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local originalCanCollide = rootPart.CanCollide
                    rootPart.CanCollide = false
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                    rootPart.CanCollide = originalCanCollide
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Atom" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.new(mobHRP.Position - Vector3.new(0, 5, 0), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddLabel("Jinbu")
Tab:AddButton({
Name = "Get Jinbu Quest",
Callback = function()
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCMain6_JinbuArea:WaitForChild("Head").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer").InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer"), "NPCQuest_JinbuArea", 7, Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Quests"):WaitForChild("QuestNPCMain6_JinbuArea")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
})
Tab:AddButton({
Name = "Get Jinbu Quest 2",
Callback = function()
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCMain6_JinbuArea:WaitForChild("HumanoidRootPart").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer").InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer"), "NPCQuest_JinbuArea", 7, Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Quests"):WaitForChild("QuestNPCMain6_JinbuArea")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
})
Tab:AddToggle({
    Name = "Auto Nearby Jinbu Boss Mobs (Back Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyjinbubossmobs_db",
    Callback = function(Value)
        Environment.autotpnearbyjinbubossmobs = Value
        if not Value then return end
        while Environment.autotpnearbyjinbubossmobs do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local originalCanCollide = rootPart.CanCollide
                    rootPart.CanCollide = false
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                    rootPart.CanCollide = originalCanCollide
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Jinbu" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local offsetPosition = mobHRP.Position - (mobHRP.CFrame.LookVector * 5)
                    local targetCFrame = CFrame.new(offsetPosition, mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Jinbu Boss Mobs 2 (Head Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyjinbubossmobs2_db",
    Callback = function(Value)
        Environment.autotpnearbyjinbubossmobs2 = Value
        if not Value then return end
        while Environment.autotpnearbyjinbubossmobs2 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local originalCanCollide = rootPart.CanCollide
                    rootPart.CanCollide = false
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                    rootPart.CanCollide = originalCanCollide
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Jinbu" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("Head").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    tweenTo(rootPart, nearestBossMob:WaitForChild("Head").CFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Jinbu Boss Mobs 3 (Front Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyjinbubossmobs3_db",
    Callback = function(Value)
        Environment.autotpnearbyjinbubossmobs3 = Value
        if not Value then return end
        while Environment.autotpnearbyjinbubossmobs3 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local originalCanCollide = rootPart.CanCollide
                    rootPart.CanCollide = false
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                    rootPart.CanCollide = originalCanCollide
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Jinbu" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local offsetPosition = mobHRP.Position + (mobHRP.CFrame.LookVector * 5)
                    local targetCFrame = CFrame.new(offsetPosition, mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Jinbu Boss Mobs 4 (Under Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyjinbubossmobs4_db",
    Callback = function(Value)
        Environment.autotpnearbyjinbubossmobs4 = Value
        if not Value then return end
        while Environment.autotpnearbyjinbubossmobs4 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local originalCanCollide = rootPart.CanCollide
                    rootPart.CanCollide = false
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                    rootPart.CanCollide = originalCanCollide
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Jinbu" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.new(mobHRP.Position - Vector3.new(0, 5, 0), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddLabel("Baby Veggy")
Tab:AddButton({
Name = "Get Baby Veggy Quest",
Callback = function()
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCMain7_VeggyArea:WaitForChild("Head").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer").InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer"), "NPCQuest_VeggyArea", 7, Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Quests"):WaitForChild("QuestNPCMain7_VeggyArea")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
})
Tab:AddButton({
Name = "Get Baby Veggy Quest 2",
Callback = function()
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCMain7_VeggyArea:WaitForChild("HumanoidRootPart").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer").InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer"), "NPCQuest_VeggyArea", 7, Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Quests"):WaitForChild("QuestNPCMain7_VeggyArea")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
})
Tab:AddToggle({
    Name = "Auto Nearby Baby Veggy Boss Mobs (Back Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbybabyveggybossmobs_db",
    Callback = function(Value)
        Environment.autotpnearbybabyveggybossmobs = Value
        if not Value then return end
        while Environment.autotpnearbybabyveggybossmobs do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local originalCanCollide = rootPart.CanCollide
                    rootPart.CanCollide = false
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                    rootPart.CanCollide = originalCanCollide
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Baby Veggy" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local offsetPosition = mobHRP.Position - (mobHRP.CFrame.LookVector * 5)
                    local targetCFrame = CFrame.new(offsetPosition, mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Baby Veggy Boss Mobs 2 (Head Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbybabyveggybossmobs2_db",
    Callback = function(Value)
        Environment.autotpnearbybabyveggybossmobs2 = Value
        if not Value then return end
        while Environment.autotpnearbybabyveggybossmobs2 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local originalCanCollide = rootPart.CanCollide
                    rootPart.CanCollide = false
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                    rootPart.CanCollide = originalCanCollide
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Baby Veggy" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("Head").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    tweenTo(rootPart, nearestBossMob:WaitForChild("Head").CFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Baby Veggy Boss Mobs 3 (Front Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbybabyveggybossmobs3_db",
    Callback = function(Value)
        Environment.autotpnearbybabyveggybossmobs3 = Value
        if not Value then return end
        while Environment.autotpnearbybabyveggybossmobs3 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local originalCanCollide = rootPart.CanCollide
                    rootPart.CanCollide = false
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                    rootPart.CanCollide = originalCanCollide
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Baby Veggy" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local offsetPosition = mobHRP.Position + (mobHRP.CFrame.LookVector * 5)
                    local targetCFrame = CFrame.new(offsetPosition, mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Baby Veggy Boss Mobs 4 (Under Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbybabyveggybossmobs4_db",
    Callback = function(Value)
        Environment.autotpnearbybabyveggybossmobs4 = Value
        if not Value then return end
        while Environment.autotpnearbybabyveggybossmobs4 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local originalCanCollide = rootPart.CanCollide
                    rootPart.CanCollide = false
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                    rootPart.CanCollide = originalCanCollide
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Baby Veggy" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.new(mobHRP.Position - Vector3.new(0, 5, 0), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddLabel("Black Karrot")
Tab:AddButton({
Name = "Get Black Karrot Quest",
Callback = function()
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCMain8_BlackKarrotArea:WaitForChild("Head").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer").InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer"), "NPCQuest_BlackKarrotArea", 7, Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Quests"):WaitForChild("QuestNPCMain8_BlackKarrotArea")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
})
Tab:AddButton({
Name = "Get Black Karrot Quest 2",
Callback = function()
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCMain8_BlackKarrotArea:WaitForChild("HumanoidRootPart").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer").InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer"), "NPCQuest_BlackKarrotArea", 7, Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Quests"):WaitForChild("QuestNPCMain8_BlackKarrotArea")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
})
Tab:AddToggle({
    Name = "Auto Nearby Black Karrot Boss Mobs (Back Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyblackkarrotbossmobs_db",
    Callback = function(Value)
        Environment.autotpnearbyblackkarrotbossmobs = Value
        if not Value then return end
        while Environment.autotpnearbyblackkarrotbossmobs do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local originalCanCollide = rootPart.CanCollide
                    rootPart.CanCollide = false
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                    rootPart.CanCollide = originalCanCollide
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Black Karrot" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local offsetPosition = mobHRP.Position - (mobHRP.CFrame.LookVector * 5)
                    local targetCFrame = CFrame.new(offsetPosition, mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Black Karrot Boss Mobs 2 (Head Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyblackkarrotbossmobs2_db",
    Callback = function(Value)
        Environment.autotpnearbyblackkarrotbossmobs2 = Value
        if not Value then return end
        while Environment.autotpnearbyblackkarrotbossmobs2 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local originalCanCollide = rootPart.CanCollide
                    rootPart.CanCollide = false
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                    rootPart.CanCollide = originalCanCollide
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Black Karrot" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("Head").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    tweenTo(rootPart, nearestBossMob:WaitForChild("Head").CFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Black Karrot Boss Mobs 3 (Front Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyblackkarrotbossmobs3_db",
    Callback = function(Value)
        Environment.autotpnearbyblackkarrotbossmobs3 = Value
        if not Value then return end
        while Environment.autotpnearbyblackkarrotbossmobs3 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local originalCanCollide = rootPart.CanCollide
                    rootPart.CanCollide = false
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                    rootPart.CanCollide = originalCanCollide
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Black Karrot" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local offsetPosition = mobHRP.Position + (mobHRP.CFrame.LookVector * 5)
                    local targetCFrame = CFrame.new(offsetPosition, mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Black Karrot Boss Mobs 4 (Under Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyblackkarrotbossmobs4_db",
    Callback = function(Value)
        Environment.autotpnearbyblackkarrotbossmobs4 = Value
        if not Value then return end
        while Environment.autotpnearbyblackkarrotbossmobs4 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local originalCanCollide = rootPart.CanCollide
                    rootPart.CanCollide = false
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                    rootPart.CanCollide = originalCanCollide
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Black Karrot" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.new(mobHRP.Position - Vector3.new(0, 5, 0), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddLabel("Brawly")
Tab:AddButton({
Name = "Get Brawly Quest",
Callback = function()
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCMain9_BrawlyArea:WaitForChild("Head").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer").InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer"), "NPCQuest_BrawlyArea", 7, Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Quests"):WaitForChild("QuestNPCMain9_BrawlyArea")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
})
Tab:AddButton({
Name = "Get Brawly Quest 2",
Callback = function()
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCMain9_BrawlyArea:WaitForChild("HumanoidRootPart").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer").InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer"), "NPCQuest_BrawlyArea", 7, Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Quests"):WaitForChild("QuestNPCMain9_BrawlyArea")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
})
Tab:AddToggle({
    Name = "Auto Nearby Brawly Boss Mobs (Back Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbybrawlybossmobs_db",
    Callback = function(Value)
        Environment.autotpnearbybrawlybossmobs = Value
        if not Value then return end
        while Environment.autotpnearbybrawlybossmobs do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local originalCanCollide = rootPart.CanCollide
                    rootPart.CanCollide = false
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                    rootPart.CanCollide = originalCanCollide
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Brawly" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local offsetPosition = mobHRP.Position - (mobHRP.CFrame.LookVector * 5)
                    local targetCFrame = CFrame.new(offsetPosition, mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Brawly Boss Mobs 2 (Head Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbybrawlybossmobs2_db",
    Callback = function(Value)
        Environment.autotpnearbybrawlybossmobs2 = Value
        if not Value then return end
        while Environment.autotpnearbybrawlybossmobs2 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local originalCanCollide = rootPart.CanCollide
                    rootPart.CanCollide = false
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                    rootPart.CanCollide = originalCanCollide
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Brawly" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("Head").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    tweenTo(rootPart, nearestBossMob:WaitForChild("Head").CFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Brawly Boss Mobs 3 (Front Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbybrawlybossmobs3_db",
    Callback = function(Value)
        Environment.autotpnearbybrawlybossmobs3 = Value
        if not Value then return end
        while Environment.autotpnearbybrawlybossmobs3 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local originalCanCollide = rootPart.CanCollide
                    rootPart.CanCollide = false
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                    rootPart.CanCollide = originalCanCollide
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Brawly" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local offsetPosition = mobHRP.Position + (mobHRP.CFrame.LookVector * 5)
                    local targetCFrame = CFrame.new(offsetPosition, mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Brawly Boss Mobs 4 (Under Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbybrawlybossmobs4_db",
    Callback = function(Value)
        Environment.autotpnearbybrawlybossmobs4 = Value
        if not Value then return end
        while Environment.autotpnearbybrawlybossmobs4 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local originalCanCollide = rootPart.CanCollide
                    rootPart.CanCollide = false
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                    rootPart.CanCollide = originalCanCollide
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Brawly" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.new(mobHRP.Position - Vector3.new(0, 5, 0), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddLabel("Jigray")
Tab:AddButton({
Name = "Get Jigray Quest",
Callback = function()
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCMain10_JigrayArea:WaitForChild("Head").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer").InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer"), "NPCQuest_JigrayArea", 7, Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Quests"):WaitForChild("QuestNPCMain10_JigrayArea")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
})
Tab:AddButton({
Name = "Get Jigray Quest 2",
Callback = function()
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCMain10_JigrayArea:WaitForChild("HumanoidRootPart").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer").InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer"), "NPCQuest_JigrayArea", 7, Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Quests"):WaitForChild("QuestNPCMain10_JigrayArea")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
})
Tab:AddToggle({
    Name = "Auto Nearby Jigray Boss Mobs (Back Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyjigraybossmobs_db",
    Callback = function(Value)
        Environment.autotpnearbyjigraybossmobs = Value
        if not Value then return end
        while Environment.autotpnearbyjigraybossmobs do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local originalCanCollide = rootPart.CanCollide
                    rootPart.CanCollide = false
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                    rootPart.CanCollide = originalCanCollide
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Jigray" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local offsetPosition = mobHRP.Position - (mobHRP.CFrame.LookVector * 5)
                    local targetCFrame = CFrame.new(offsetPosition, mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Jigray Boss Mobs 2 (Head Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyjigraybossmobs2_db",
    Callback = function(Value)
        Environment.autotpnearbyjigraybossmobs2 = Value
        if not Value then return end
        while Environment.autotpnearbyjigraybossmobs2 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local originalCanCollide = rootPart.CanCollide
                    rootPart.CanCollide = false
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                    rootPart.CanCollide = originalCanCollide
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Jigray" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("Head").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    tweenTo(rootPart, nearestBossMob:WaitForChild("Head").CFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Jigray Boss Mobs 3 (Front Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyjigraybossmobs3_db",
    Callback = function(Value)
        Environment.autotpnearbyjigraybossmobs3 = Value
        if not Value then return end
        while Environment.autotpnearbyjigraybossmobs3 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local originalCanCollide = rootPart.CanCollide
                    rootPart.CanCollide = false
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                    rootPart.CanCollide = originalCanCollide
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Jigray" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local offsetPosition = mobHRP.Position + (mobHRP.CFrame.LookVector * 5)
                    local targetCFrame = CFrame.new(offsetPosition, mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Jigray Boss Mobs 4 (Under Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyjigraybossmobs4_db",
    Callback = function(Value)
        Environment.autotpnearbyjigraybossmobs4 = Value
        if not Value then return end
        while Environment.autotpnearbyjigraybossmobs4 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local originalCanCollide = rootPart.CanCollide
                    rootPart.CanCollide = false
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                    rootPart.CanCollide = originalCanCollide
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Jigray" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.new(mobHRP.Position - Vector3.new(0, 5, 0), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddLabel("Garrett I, Garrett II, Garrett III")
Tab:AddToggle({
    Name = "Auto Nearby Garrett I Boss Mobs (Back Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbygarrettibossmobs_db",
    Callback = function(Value)
        Environment.autotpnearbygarrettibossmobs = Value
        if not Value then return end
        while Environment.autotpnearbygarrettibossmobs do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local originalCanCollide = rootPart.CanCollide
                    rootPart.CanCollide = false
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                    rootPart.CanCollide = originalCanCollide
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Garrett I" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local offsetPosition = mobHRP.Position - (mobHRP.CFrame.LookVector * 5)
                    local targetCFrame = CFrame.new(offsetPosition, mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Garrett I Boss Mobs 2 (Head Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbygarrettibossmobs2_db",
    Callback = function(Value)
        Environment.autotpnearbygarrettibossmobs2 = Value
        if not Value then return end
        while Environment.autotpnearbygarrettibossmobs2 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local originalCanCollide = rootPart.CanCollide
                    rootPart.CanCollide = false
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                    rootPart.CanCollide = originalCanCollide
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Garrett I" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("Head").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    tweenTo(rootPart, nearestBossMob:WaitForChild("Head").CFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Garrett I Boss Mobs 3 (Front Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbygarrettibossmobs3_db",
    Callback = function(Value)
        Environment.autotpnearbygarrettibossmobs3 = Value
        if not Value then return end
        while Environment.autotpnearbygarrettibossmobs3 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local originalCanCollide = rootPart.CanCollide
                    rootPart.CanCollide = false
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                    rootPart.CanCollide = originalCanCollide
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Garrett I" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local offsetPosition = mobHRP.Position + (mobHRP.CFrame.LookVector * 5)
                    local targetCFrame = CFrame.new(offsetPosition, mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Garrett I Boss Mobs 4 (Under Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbygarrettibossmobs4_db",
    Callback = function(Value)
        Environment.autotpnearbygarrettibossmobs4 = Value
        if not Value then return end
        while Environment.autotpnearbygarrettibossmobs4 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local originalCanCollide = rootPart.CanCollide
                    rootPart.CanCollide = false
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                    rootPart.CanCollide = originalCanCollide
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Garrett I" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.new(mobHRP.Position - Vector3.new(0, 5, 0), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Garrett II Boss Mobs (Back Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbygarrettiibossmobs_db",
    Callback = function(Value)
        Environment.autotpnearbygarrettiibossmobs = Value
        if not Value then return end
        while Environment.autotpnearbygarrettiibossmobs do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local originalCanCollide = rootPart.CanCollide
                    rootPart.CanCollide = false
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                    rootPart.CanCollide = originalCanCollide
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Garrett II" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local offsetPosition = mobHRP.Position - (mobHRP.CFrame.LookVector * 5)
                    local targetCFrame = CFrame.new(offsetPosition, mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Garrett II Boss Mobs 2 (Head Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbygarrettiibossmobs2_db",
    Callback = function(Value)
        Environment.autotpnearbygarrettiibossmobs2 = Value
        if not Value then return end
        while Environment.autotpnearbygarrettiibossmobs2 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local originalCanCollide = rootPart.CanCollide
                    rootPart.CanCollide = false
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                    rootPart.CanCollide = originalCanCollide
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Garrett II" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("Head").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    tweenTo(rootPart, nearestBossMob:WaitForChild("Head").CFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Garrett II Boss Mobs 3 (Front Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbygarrettiibossmobs3_db",
    Callback = function(Value)
        Environment.autotpnearbygarrettiibossmobs3 = Value
        if not Value then return end
        while Environment.autotpnearbygarrettiibossmobs3 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local originalCanCollide = rootPart.CanCollide
                    rootPart.CanCollide = false
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                    rootPart.CanCollide = originalCanCollide
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Garrett II" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local offsetPosition = mobHRP.Position + (mobHRP.CFrame.LookVector * 5)
                    local targetCFrame = CFrame.new(offsetPosition, mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Garrett II Boss Mobs 4 (Under Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbygarrettiibossmobs4_db",
    Callback = function(Value)
        Environment.autotpnearbygarrettiibossmobs4 = Value
        if not Value then return end
        while Environment.autotpnearbygarrettiibossmobs4 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local originalCanCollide = rootPart.CanCollide
                    rootPart.CanCollide = false
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                    rootPart.CanCollide = originalCanCollide
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Garrett II" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.new(mobHRP.Position - Vector3.new(0, 5, 0), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Garrett III Boss Mobs (Back Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbygarrettiiibossmobs_db",
    Callback = function(Value)
        Environment.autotpnearbygarrettiiibossmobs = Value
        if not Value then return end
        while Environment.autotpnearbygarrettiiibossmobs do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local originalCanCollide = rootPart.CanCollide
                    rootPart.CanCollide = false
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                    rootPart.CanCollide = originalCanCollide
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Garrett III" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local offsetPosition = mobHRP.Position - (mobHRP.CFrame.LookVector * 5)
                    local targetCFrame = CFrame.new(offsetPosition, mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Garrett III Boss Mobs 2 (Head Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbygarrettiiibossmobs2_db",
    Callback = function(Value)
        Environment.autotpnearbygarrettiiibossmobs2 = Value
        if not Value then return end
        while Environment.autotpnearbygarrettiiibossmobs2 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local originalCanCollide = rootPart.CanCollide
                    rootPart.CanCollide = false
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                    rootPart.CanCollide = originalCanCollide
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Garrett III" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("Head").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    tweenTo(rootPart, nearestBossMob:WaitForChild("Head").CFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Garrett III Boss Mobs 3 (Front Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbygarrettiiibossmobs3_db",
    Callback = function(Value)
        Environment.autotpnearbygarrettiiibossmobs3 = Value
        if not Value then return end
        while Environment.autotpnearbygarrettiiibossmobs3 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local originalCanCollide = rootPart.CanCollide
                    rootPart.CanCollide = false
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                    rootPart.CanCollide = originalCanCollide
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Garrett III" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local offsetPosition = mobHRP.Position + (mobHRP.CFrame.LookVector * 5)
                    local targetCFrame = CFrame.new(offsetPosition, mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Garrett III Boss Mobs 4 (Under Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbygarrettiiibossmobs4_db",
    Callback = function(Value)
        Environment.autotpnearbygarrettiiibossmobs4 = Value
        if not Value then return end
        while Environment.autotpnearbygarrettiiibossmobs4 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local originalCanCollide = rootPart.CanCollide
                    rootPart.CanCollide = false
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                    rootPart.CanCollide = originalCanCollide
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Garrett III" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.new(mobHRP.Position - Vector3.new(0, 5, 0), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
local Tab = Window:MakeTab({
Name = "NPCS",
Icon = "rbxassetid://4483345998",
PremiumOnly = false
})
Tab:AddButton({
Name = "Interact With Guild Quest Giver",
Callback = function()
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Main.GuildQuest:WaitForChild("Head").CFrame
task.wait(0.75)
fireproximityprompt(Workspace.Misc.NPC.Main.GuildQuest.Head.DialogAttachment.DialogPrompt)
end
})
Tab:AddButton({
Name = "Interact With Guild Quest Giver 2",
Callback = function()
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Main.GuildQuest:WaitForChild("HumanoidRootPart").CFrame
task.wait(0.75)
fireproximityprompt(Workspace.Misc.NPC.Main.GuildQuest.Head.DialogAttachment.DialogPrompt)
end
})
Tab:AddButton({
Name = "West City Quest",
Callback = function()
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCMain1_WestCity:WaitForChild("Head").CFrame
end
})
Tab:AddButton({
Name = "Puriza Area Quest",
Callback = function()
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCMain2_PurizaArea:WaitForChild("Head").CFrame
end
})
Tab:AddButton({
Name = "Coolest Area Quest",
Callback = function()
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCMain3_CoolestArea:WaitForChild("Head").CFrame
end
})
Tab:AddButton({
Name = "Droids Quest",
Callback = function()
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCMain4_Droid1718Area:WaitForChild("Head").CFrame
end
})
Tab:AddButton({
Name = "Atom Area Quest",
Callback = function()
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCMain5_AtomArea:WaitForChild("Head").CFrame
end
})
Tab:AddButton({
Name = "Jinbu Area Quest",
Callback = function()
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCMain6_JinbuArea:WaitForChild("Head").CFrame
end
})
Tab:AddButton({
Name = "Veggy Area Quest",
Callback = function()
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCMain7_VeggyArea:WaitForChild("Head").CFrame
end
})
Tab:AddButton({
Name = "Black Karrot Area Quest",
Callback = function()
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCMain8_BlackKarrotArea:WaitForChild("Head").CFrame
end
})
Tab:AddButton({
Name = "Brawly Area Quest",
Callback = function()
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCMain9_BrawlyArea:WaitForChild("Head").CFrame
end
})
Tab:AddButton({
Name = "Jigray Area Quest",
Callback = function()
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCMain10_JigrayArea:WaitForChild("Head").CFrame
end
})
Tab:AddButton({
Name = "West City Quest 2",
Callback = function()
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCMain1_WestCity:WaitForChild("HumanoidRootPart").CFrame
end
})
Tab:AddButton({
Name = "Puriza Area Quest 2",
Callback = function()
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCMain2_PurizaArea:WaitForChild("HumanoidRootPart").CFrame
end
})
Tab:AddButton({
Name = "Coolest Area Quest 2",
Callback = function()
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCMain3_CoolestArea:WaitForChild("HumanoidRootPart").CFrame
end
})
Tab:AddButton({
Name = "Droids Quest 2",
Callback = function()
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCMain4_Droid1718Area:WaitForChild("HumanoidRootPart").CFrame
end
})
Tab:AddButton({
Name = "Atom Area Quest 2",
Callback = function()
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCMain5_AtomArea:WaitForChild("HumanoidRootPart").CFrame
end
})
Tab:AddButton({
Name = "Jinbu Area Quest 2",
Callback = function()
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCMain6_JinbuArea:WaitForChild("HumanoidRootPart").CFrame
end
})
Tab:AddButton({
Name = "Veggy Area Quest 2",
Callback = function()
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCMain7_VeggyArea:WaitForChild("HumanoidRootPart").CFrame
end
})
Tab:AddButton({
Name = "Black Karrot Area Quest 2",
Callback = function()
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCMain8_BlackKarrotArea:WaitForChild("HumanoidRootPart").CFrame
end
})
Tab:AddButton({
Name = "Brawly Area Quest 2",
Callback = function()
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCMain9_BrawlyArea:WaitForChild("HumanoidRootPart").CFrame
end
})
Tab:AddButton({
Name = "Jigray Area Quest 2",
Callback = function()
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCMain10_JigrayArea:WaitForChild("HumanoidRootPart").CFrame
end
})
local Tab = Window:MakeTab({
Name = "Teleports",
Icon = "http://www.roblox.com/asset/?id=4335489011",
PremiumOnly = false
})
local Section = Tab:AddSection({
Name = "Click the Buttons to Teleport"
})
Tab:AddButton({
Name = "Go To Quest Spawned Items",
Callback = function()
for _, v in Workspace.Misc.QuestProps:GetChildren() do
if v:IsA("Folder") then
for _, child in v:GetChildren() do
if child:IsA("Part") then
if child:FindFirstChildWhichIsA("Model") and child:FindFirstChildWhichIsA("Model").Parent.Name == child.Name then
local endTime = tick() + 2.5
while tick() < endTime do
Players.LocalPlayer.Character:FindFirstChild("HumanoidRootPart").CFrame = child.CFrame
task.wait()
end
end
end
end
end
end
end
})
Tab:AddButton({
Name = "Go To Kaioken Trainer",
Callback = function()
if Workspace.Misc.NPC.Main:FindFirstChild("KaiokenTrainerNPC") and Workspace.Misc.NPC.Main.KaiokenTrainerNPC:FindFirstChild("Head") then
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Main.KaiokenTrainerNPC:WaitForChild("Head").CFrame
end
end
})
Tab:AddButton({
Name = "Go To Kaioken Trainer 2",
Callback = function()
if Workspace.Misc.NPC.Main:FindFirstChild("KaiokenTrainerNPC") and Workspace.Misc.NPC.Main.KaiokenTrainerNPC:FindFirstChild("HumanoidRootPart") then
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Main.KaiokenTrainerNPC:WaitForChild("HumanoidRootPart").CFrame
end
end
})
Tab:AddButton({
Name = "Go To Quest Board",
Callback = function()
Players.LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(1330.0188, 597.434753, -2834.25732, 0.0495601445, 0, 0.998771191, -0, 1.00000012, -0, -0.998771191, 0, 0.0495601445)
end
})
Tab:AddButton({
Name = "World Teleporter",
Callback = function()
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = CFrame.new(1308.73767, 590.966797, -2994.11597, -0.242444679, -5.55411894e-08, -0.970165253, -3.81573484e-09, 1, -5.62956508e-08, 0.970165253, -9.94668792e-09, -0.242444679)
end
})
Tab:AddButton({
Name = "Ocean",
Callback = function()
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = CFrame.new(2078, 589, 1744)
end
})
Tab:AddButton({
Name = "Piccolo",
Callback = function()
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = CFrame.new(1276.79102, 591.04303, -2915.26123, -0.940263867, 3.25475753e-08, 0.5 - 0.340446591, 6.62011104e-08, 1, -8.72352217e-08, 0.340446591, -1.04562069e-07, -0.940263867)
end
})
Tab:AddButton({
Name = "Premium Fusion (Requires Roblox Premium)",
Callback = function()
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = CFrame.new(1369.20447, 590.932251, -2870.10815, -0.419576734, 1.09318696e-08, -0.907719851, -9.90776439e-09, 1, 1.66229004e-08, 0.907719851, 1.59680571e-08, -0.419576734)
end
})
Tab:AddButton({
Name = "Vegeta Fusion",
Callback = function()
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = CFrame.new(-639.25415, 988.875061, -2697.02563, -0.0209753737, -5.05985343e-09, 0.999779999, -2.22242824e-09, 1, 5.0143405e-09, -0.999779999, -2.11676165e-09, -0.0209753737)
end
})
Tab:AddButton({
Name = "Goku Fusion",
Callback = function()
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = CFrame.new(2417.25415, 569.399048, -4974.60449, 0.98227036, -1.31336657e-08, -0.187469915, 2.13354454e-08, 1, 4.17320862e-08, 0.187469915, -4.49919462e-08, 0.98227036)
end
})
local Tab = Window:MakeTab({
Name = "Worlds",
Icon = "rbxassetid://4483345998",
PremiumOnly = false
})
local Section = Tab:AddSection({
Name = "Click The Button To Be Teleported To That World"
})
Tab:AddButton({
Name = "Main World",
Callback = function()
local teleportservice = game:GetService("TeleportService")
local gameID = game.PlaceId
teleportservice:Teleport(3177438863)
end
})
Tab:AddButton({
Name = "Crimson Planet (30 Rebirths Required)",
Callback = function()
local teleportservice = game:GetService("TeleportService")
local gameID = game.PlaceId
teleportservice:Teleport(7040546583)
end
})
Tab:AddButton({
Name = "Planet Droid (150 Rebirths Required)",
Callback = function()
local teleportservice = game:GetService("TeleportService")
local gameID = game.PlaceId
teleportservice:Teleport(7888441249)
end
})
Tab:AddButton({
Name = "World Of Tournament (Script Wont Work Here!)",
Callback = function()
local teleportservice = game:GetService("TeleportService")
local gameID = game.PlaceId
teleportservice:Teleport(7379325123)
end
})
Tab:AddButton({
Name = "Battlegrounds (Script Wont Work Here!)",
Callback = function()
local teleportservice = game:GetService("TeleportService")
local gameID = game.PlaceId
teleportservice:Teleport(14516089806)
end
})
Tab:AddButton({
Name = "AFK World (Script Wont Work Here!)",
Callback = function()
local teleportservice = game:GetService("TeleportService")
local gameID = game.PlaceId
teleportservice:Teleport(18501029209)
end
})
Tab:AddButton({
Name = "Trade Hub (Script Wont Work Here!)",
Callback = function()
local teleportservice = game:GetService("TeleportService")
local gameID = game.PlaceId
teleportservice:Teleport(18408422178)
end
})
local Tab = Window:MakeTab({
Name = "Anti's",
Icon = "http://www.roblox.com/asset/?id=7734056608",
PremiumOnly = false
})
Tab:AddToggle({
Name = "Anti Admins (do task.wait()t use with anti kick!)",
Default = false,
Save = true,
Flag = "antiadmins_db",
Callback = function(Value)
Environment.AntiAdmins = Value
while Environment.AntiAdmins and task.wait() do
local AdminNames = {
"Jirenato",
"ReignImperatus",
"Zephyrus_Yi",
"ratedroeh",
"Natt_P5",
"O_ORui",
"BexDragoon",
"pokemonhaden",
"XADED4971",
"AltForTGBN",
"notrmdx",
"Npinoy"
}
for _, player in Players:GetPlayers() do
if table.find(AdminNames, player.Name) then
Players.LocalPlayer:Kick("Holy shit admin detected! You almost got banned there bud!")
end
end
task.wait(0.1)
end
end
})
Tab:AddButton({
    Name = "Anti Cheat Bypass (Bypass Anti Exploit Manager!)",
    Callback = function()
        local mt = getrawmetatable(game)
        setreadonly(mt, false)
        local oldNamecall = mt.__namecall
        mt.__namecall = newcclosure(function(self, ...)
            local args = {...}
            local method = getnamecallmethod()
            if method == "Kick" or method == "Destroy" then
                warn("[Bypass] Anti-cheat tried to kick you!")
                return false
            end
            return oldNamecall(self, unpack(args))
        end)
        local RunService = game:GetService("RunService")
        local Players = game:GetService("Players")
        local StarterGui = game:GetService("StarterGui")
        local HeartbeatConnection
        if HeartbeatConnection then HeartbeatConnection:Disconnect() end
        HeartbeatConnection = RunService.Heartbeat:Connect(function()
            sethiddenproperty(Players.LocalPlayer, "SimulationRadius", math.huge)
        end)
        local ReplicatedStorage = game:GetService("ReplicatedStorage")
        local BlockedRemotes = {"AntiCheatEvent1", "AntiCheatEvent2"}
        local oldFireServer = Instance.new("RemoteEvent").FireServer
        hookfunction(oldFireServer, function(self, ...)
            if table.find(BlockedRemotes, self.Name) then
                warn("[Bypass] Blocked remote:", self.Name)
                return false
            end
            return oldFireServer(self, ...)
        end)
        local function SpoofPlayerData()
            local Player = Players.LocalPlayer
            while task.wait(0.75) do
                if Player and Player.Character then
                    local Humanoid = Player.Character:FindFirstChildOfClass("Humanoid")
                    if Humanoid then
                        Humanoid.WalkSpeed = 16
                        Humanoid.JumpPower = 50
                    end
                end
            end
        end
        task.spawn(SpoofPlayerData)
        StarterGui:SetCore("SendNotification", {
            Title = "Bypass Active",
            Text = "Anti-Cheat successfully bypassed.",
            Duration = 5
        })
    end
})
Tab:AddToggle({
Name = "Anti Kick",
Default = false,
Save = true,
Flag = "antikick_db",
Callback = function(Value)
Environment.Anti = Value
if Value then
local Anti
Anti = hookmetamethod(game, "__namecall", function(self, ...)
if self == Players.LocalPlayer and getnamecallmethod():lower() == "kick" and Environment.Anti then
return warn("[ANTI-KICK] Client Tried To Call Kick Function On LocalPlayer")
end
return Anti(self, ...)
end)
else
if Anti then
Environment.Anti = false
hookmetamethod(game, "__namecall", Anti)
Anti = false
end
end
end
})
Tab:AddToggle({
Name = "Anti Destroy",
Default = false,
Save = true,
Flag = "antidestroy_db",
Callback = function(Value)
Environment.Anti = Value
if Value then
local Anti
Anti = hookmetamethod(game, "__namecall", function(self, ...)
if self == Players.LocalPlayer and getnamecallmethod():lower() == "destroy" and Environment.Anti then
return warn("[ANTI-DESTROY] Client Tried To Call Destroy Function On LocalPlayer")
end
return Anti(self, ...)
end)
else
if Anti then
Environment.Anti = false
hookmetamethod(game, "__namecall", Anti)
Anti = false
end
end
end
})
Tab:AddToggle({
Name = "Anti Afk",
Default = false,
Save = true,
Flag = "antiafk_db",
Callback = function(Value)
if Value then
if getconnections then
for _, v in getconnections(Players.LocalPlayer.Idled) do
if v["Disable"] then
v["Disable"](v)
elseif v["Disconnect"] then
v["Disconnect"](v)
end
end
else
local VirtualUser = game:GetService("VirtualUser")
Players.LocalPlayer.Idled:Connect(function()
VirtualUser:CaptureController()
VirtualUser:ClickButton2(Vector2.new())
end)
end
else
end
end
})
local Tab = Window:MakeTab({
Name = "Spawn Items",
Icon = "http://www.roblox.com/asset/?id=7733674079",
PremiumOnly = false
})
Tab:AddButton({
Name = "Grab Relics",
Callback = function()
for _, position in {
CFrame.new(-1259.17737, 963.225159, -3163.17065, 0.965929627, 0, 0.258804798, 0, 1, 0, -0.258804798, 0, 0.965929627),
CFrame.new(1871.38721, 673.225159, 193.938965, 1, 0, 0, 0, 1, 0, 0, 0, 1),
CFrame.new(3134.06689, 585.223816, -3038.94849, 1, 0, 0, 0, 1, 0, 0, 0, 1),
CFrame.new(5212.08105, 1323.2251, -2180.61816, 0.866007268, 0, 0.500031412, 0, 1, 0, -0.500031412, 0, 0.866007268),
CFrame.new(7854.40088, 611.025208, -2125.53979, 0.996191859, -0, -0.0871884301, 0, 1, -0, 0.0871884301, 0, 0.996191859),
CFrame.new(5983.10596, 572.225159, -3562.54883, 0.996191859, -0, -0.0871884301, 0, 1, -0, 0.0871884301, 0, 0.996191859)
} do
local endTime = tick() + 4.75
while tick() < endTime do
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = position
for _, part in Workspace:WaitForChild("PartStorage"):GetDescendants() do
if part:IsA("BasePart") then
for _, v in part:GetDescendants() do
if v:IsA("ProximityPrompt") and v.ObjectText and v.ObjectText:find("Token") then
firesignal(v.Triggered)
end
end
end
end
task.wait()
end
end
end
})
Tab:AddButton({
Name = "Grab Relics 2 (Remotes!)",
Callback = function()
for _, position in {
CFrame.new(-1259.17737, 963.225159, -3163.17065, 0.965929627, 0, 0.258804798, 0, 1, 0, -0.258804798, 0, 0.965929627),
CFrame.new(1871.38721, 673.225159, 193.938965, 1, 0, 0, 0, 1, 0, 0, 0, 1),
CFrame.new(3134.06689, 585.223816, -3038.94849, 1, 0, 0, 0, 1, 0, 0, 0, 1),
CFrame.new(5212.08105, 1323.2251, -2180.61816, 0.866007268, 0, 0.500031412, 0, 1, 0, -0.500031412, 0, 0.866007268),
CFrame.new(7854.40088, 611.025208, -2125.53979, 0.996191859, -0, -0.0871884301, 0, 1, -0, 0.0871884301, 0, 0.996191859),
CFrame.new(5983.10596, 572.225159, -3562.54883, 0.996191859, -0, -0.0871884301, 0, 1, -0, 0.0871884301, 0, 0.996191859)
} do
local endTime = tick() + 4.75
while tick() < endTime do
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = position
for _, v in Workspace.PartStorage:GetChildren() do
if string.match(v.Name, "^ItemDrop_%d+$") then
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("ItemDropService"):WaitForChild("RF"):WaitForChild("ClaimItem").InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("ItemDropService"):WaitForChild("RF"):WaitForChild("ClaimItem"), string.match(v.Name, "%d+"), "StandardWish"}))
end
end
task.wait()
end
end
end
})
Tab:AddButton({
Name = "Teleport To Relics",
Callback = function()
for _, position in {
CFrame.new(-1259.17737, 963.225159, -3163.17065, 0.965929627, 0, 0.258804798, 0, 1, 0, -0.258804798, 0, 0.965929627),
CFrame.new(1871.38721, 673.225159, 193.938965, 1, 0, 0, 0, 1, 0, 0, 0, 1),
CFrame.new(3134.06689, 585.223816, -3038.94849, 1, 0, 0, 0, 1, 0, 0, 0, 1),
CFrame.new(5212.08105, 1323.2251, -2180.61816, 0.866007268, 0, 0.500031412, 0, 1, 0, -0.500031412, 0, 0.866007268),
CFrame.new(7854.40088, 611.025208, -2125.53979, 0.996191859, -0, -0.0871884301, 0, 1, -0, 0.0871884301, 0, 0.996191859),
CFrame.new(5983.10596, 572.225159, -3562.54883, 0.996191859, -0, -0.0871884301, 0, 1, -0, 0.0871884301, 0, 0.996191859)
} do
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = position
task.wait(5)
end
end
})
Tab:AddButton({
Name = "Teleport To Relics 2 (Stay At Current Location!)",
Callback = function()
for _, position in {
CFrame.new(-1259.17737, 963.225159, -3163.17065, 0.965929627, 0, 0.258804798, 0, 1, 0, -0.258804798, 0, 0.965929627),
CFrame.new(1871.38721, 673.225159, 193.938965, 1, 0, 0, 0, 1, 0, 0, 0, 1),
CFrame.new(3134.06689, 585.223816, -3038.94849, 1, 0, 0, 0, 1, 0, 0, 0, 1),
CFrame.new(5212.08105, 1323.2251, -2180.61816, 0.866007268, 0, 0.500031412, 0, 1, 0, -0.500031412, 0, 0.866007268),
CFrame.new(7854.40088, 611.025208, -2125.53979, 0.996191859, -0, -0.0871884301, 0, 1, -0, 0.0871884301, 0, 0.996191859),
CFrame.new(5983.10596, 572.225159, -3562.54883, 0.996191859, -0, -0.0871884301, 0, 1, -0, 0.0871884301, 0, 0.996191859)
} do
local endTime = tick() + 5
while tick() < endTime do
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = position
task.wait(1.25)
end
end
end
})
Tab:AddToggle({
Name = "Auto Teleport To Relics",
Default = false,
Save = true,
Flag = "autorelics_db",
Callback = function(Value)
Environment.AutoRelics = Value
while Environment.AutoRelics and task.wait() do
for _, position in {
CFrame.new(-1259.17737, 963.225159, -3163.17065, 0.965929627, 0, 0.258804798, 0, 1, 0, -0.258804798, 0, 0.965929627),
CFrame.new(1871.38721, 673.225159, 193.938965, 1, 0, 0, 0, 1, 0, 0, 0, 1),
CFrame.new(3134.06689, 585.223816, -3038.94849, 1, 0, 0, 0, 1, 0, 0, 0, 1),
CFrame.new(5212.08105, 1323.2251, -2180.61816, 0.866007268, 0, 0.500031412, 0, 1, 0, -0.500031412, 0, 0.866007268),
CFrame.new(7854.40088, 611.025208, -2125.53979, 0.996191859, -0, -0.0871884301, 0, 1, -0, 0.0871884301, 0, 0.996191859),
CFrame.new(5983.10596, 572.225159, -3562.54883, 0.996191859, -0, -0.0871884301, 0, 1, -0, 0.0871884301, 0, 0.996191859)
} do
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = position
task.wait(5)
end
end
end
})
Tab:AddToggle({
Name = "Auto Teleport To Relics 2 (Stay At Current Location!)",
Default = false,
Save = true,
Flag = "autorelics_db",
Callback = function(Value)
Environment.AutoRelics = Value
while Environment.AutoRelics and task.wait() do
for _, position in {
CFrame.new(-1259.17737, 963.225159, -3163.17065, 0.965929627, 0, 0.258804798, 0, 1, 0, -0.258804798, 0, 0.965929627),
CFrame.new(1871.38721, 673.225159, 193.938965, 1, 0, 0, 0, 1, 0, 0, 0, 1),
CFrame.new(3134.06689, 585.223816, -3038.94849, 1, 0, 0, 0, 1, 0, 0, 0, 1),
CFrame.new(5212.08105, 1323.2251, -2180.61816, 0.866007268, 0, 0.500031412, 0, 1, 0, -0.500031412, 0, 0.866007268),
CFrame.new(7854.40088, 611.025208, -2125.53979, 0.996191859, -0, -0.0871884301, 0, 1, -0, 0.0871884301, 0, 0.996191859),
CFrame.new(5983.10596, 572.225159, -3562.54883, 0.996191859, -0, -0.0871884301, 0, 1, -0, 0.0871884301, 0, 0.996191859)
} do
local endTime = tick() + 5
while tick() < endTime do
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = position
task.wait(0.1)
end
end
end
end
})
Tab:AddToggle({
Name = "Auto Relics (Must Be Near Standard Wish Tokens!)",
Default = false,
Save = true,
Flag = "autotokens_db",
Callback = function(Value)
Environment.AutoTokens = Value
while Environment.AutoTokens and task.wait() do
for _, part in Workspace:WaitForChild("PartStorage"):GetDescendants() do
if part:IsA("BasePart") then
for _, v in part:GetDescendants() do
if v:IsA("ProximityPrompt") and v.ObjectText and v.ObjectText:find("Token") then
if Players.LocalPlayer.Character and Players.LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = part.CFrame + Vector3.new(0, 5, 0)
task.wait(0.75)
fireproximityprompt(v, 1)
task.wait(2)
end
break
end
end
end
end
end
end
})
Tab:AddToggle({
Name = "Auto Orbs (FP Must Be Near Orbs!)",
Default = false,
Save = true,
Flag = "autoorbs_db",
Callback = function(Value)
Environment.AutoOrbs = Value
while Environment.AutoOrbs and task.wait() do
for _, part in Workspace:WaitForChild("PartStorage"):GetDescendants() do
if part:IsA("BasePart") then
for _, v in part:GetDescendants() do
if v:IsA("ProximityPrompt") and v.ObjectText and v.ObjectText:find("Orb") then
if Players.LocalPlayer.Character and Players.LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = part.CFrame + Vector3.new(0, 5, 0)
task.wait(0.75)
fireproximityprompt(v, 1)
task.wait(2)
end
break
end
end
end
end
end
end
})
Tab:AddToggle({
Name = "Auto Orbs 2 (FS Must Be Near Orbs!)",
Default = false,
Save = true,
Flag = "autoorbs2_db",
Callback = function(Value)
Environment.AutoOrbs2 = Value
while Environment.AutoOrbs2 and task.wait() do
for _, part in Workspace:WaitForChild("PartStorage"):GetDescendants() do
if part:IsA("BasePart") then
for _, v in part:GetDescendants() do
if v:IsA("ProximityPrompt") and v.ObjectText and v.ObjectText:find("Orb") then
if Players.LocalPlayer.Character and Players.LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = part.CFrame + Vector3.new(0, 5, 0)
task.wait(0.75)
firesignal(v.Triggered)
task.wait(2)
end
break
end
end
end
end
end
end
})
Tab:AddToggle({
Name = "Auto Nearby Interact",
Default = false,
Save = true,
Flag = "autonearbyinteract_db",
Callback = function(Value)
Environment.AutoNearbyInteract = Value
while Environment.AutoNearbyInteract and task.wait() do
function fire()
if Players.LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
for _, v in Workspace:GetDescendants() do
if v:IsA("ProximityPrompt") then
if ((Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").Position - v.Parent.Position).Magnitude <= 10) then
fireproximityprompt(v)
end
end
end
end
end
fire()
end
end
})
Tab:AddToggle({
Name = "Auto Nearby Interact 2 (Maybe Use 1!)",
Default = false,
Save = true,
Flag = "autonearbyinteract_db",
Callback = function(Value)
Environment.AutoNearbyInteract = Value
while Environment.AutoNearbyInteract and task.wait() do
function fire()
if Players.LocalPlayer.Character:FindFirstChild("Head") then
for _, v in Workspace:GetDescendants() do
if v:IsA("ProximityPrompt") then
if ((Players.LocalPlayer.Character:WaitForChild("Head").Position - v.Parent.Position).Magnitude <= 10) then
fireproximityprompt(v)
end
end
end
end
end
fire()
end
end
})
Tab:AddButton({
Name = "Instant Nearby Interact",
Callback = function()
function fire()
if Players.LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
for _, v in Workspace:GetDescendants() do
if v:IsA("ProximityPrompt") then
if ((Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").Position - v.Parent.Position).Magnitude <= 10) then
fireproximityprompt(v)
end
end
end
end
end
fire()
end
})
Tab:AddButton({
Name = "Instant Nearby Interact 2 (Maybe use 1!)",
Callback = function()
function fire()
if Players.LocalPlayer.Character:FindFirstChild("Head") then
for _, v in Workspace:GetDescendants() do
if v:IsA("ProximityPrompt") then
if ((Players.LocalPlayer.Character:WaitForChild("Head").Position - v.Parent.Position).Magnitude <= 10) then
fireproximityprompt(v)
end
end
end
end
end
fire()
end
})
Tab:AddButton({
Name = "Wish Standard (Need Standard Tokens!)",
Callback = function()
ReplicatedStorage.Packages._Index:FindFirstChild("sleitnick_knit@1.4.7").knit.Services.WishService.RF.requestWishFromBanner.InvokeServer(unpack({ReplicatedStorage.Packages._Index:FindFirstChild("sleitnick_knit@1.4.7").knit.Services.WishService.RF.requestWishFromBanner, "Standard", 1, 1}))
end
})
Tab:AddButton({
Name = "Wish Standard (Need Premium Tokens!)",
Callback = function()
ReplicatedStorage.Packages._Index:FindFirstChild("sleitnick_knit@1.4.7").knit.Services.WishService.RF.requestWishFromBanner.InvokeServer(unpack({ReplicatedStorage.Packages._Index:FindFirstChild("sleitnick_knit@1.4.7").knit.Services.WishService.RF.requestWishFromBanner, "Standard", 2, 1}))
end
})
Tab:AddButton({
Name = "Wish Premium (Need Premium Tokens!)",
Callback = function()
ReplicatedStorage.Packages._Index:FindFirstChild("sleitnick_knit@1.4.7").knit.Services.WishService.RF.requestWishFromBanner.InvokeServer(unpack({ReplicatedStorage.Packages._Index:FindFirstChild("sleitnick_knit@1.4.7").knit.Services.WishService.RF.requestWishFromBanner, "Premium", 1, 1}))
end
})
Tab:AddButton({
Name = "Buy Premium Wish Standard (Need 50 Gold!)",
Callback = function()
ReplicatedStorage.Packages._Index:FindFirstChild("sleitnick_knit@1.4.7").knit.Services.WishService.RF.buyFromShop.InvokeServer(unpack({ReplicatedStorage.Packages._Index:FindFirstChild("sleitnick_knit@1.4.7").knit.Services.WishService.RF.buyFromShop, "Standard", 5}))
end
})
local Tab = Window:MakeTab({
Name = "Codes/Trades",
Icon = "rbxassetid://4483345998",
PremiumOnly = false,
})
Tab:AddLabel("I Have Added A Codes Channel For Users!")
Tab:AddLabel("Please Know That These Codes May Expire At Any Time!")
Tab:AddLabel("I Will Update This Whenever A New One Comes Out!")
Tab:AddParagraph("The Current Codes Is MOREFREECODES, TENFREEWISHES, PREMIUMSGALORE")
Tab:AddTextbox({
Name = "Codes (Redeem!)",
Default = "",
TextDisappear = false,
Callback = function(Value)
local args = {
[1] = {
["UniqueTag"] = "RedeemCode",
["InputText"] = Value,
["InputType"] = "Text",
["LeftButton"] = "Redeem",
["Description"] = "Redeem Code",
["Prompt"] = "RedeemCode",
["RightButton"] = "Cancel",
["Title"] = "Codes"
},
[2] = "Redeem"
}
ReplicatedStorage.Packages._Index:FindFirstChild("sleitnick_knit@1.4.7").knit.Services.PromptService.RE.Prompt.FireServer(unpack({ReplicatedStorage.Packages._Index:FindFirstChild("sleitnick_knit@1.4.7").knit.Services.PromptService.RE.Prompt, unpack(args)}))
end
})
Tab:AddTextbox({
Name = "Trades (Enter Player Username Here!)",
Default = "",
TextDisappear = false,
Callback = function(Value)
local player = Players:FindFirstChild(Value)
if player then
OrionLib:MakeNotification({
Name = "Player Found!",
Content = "Sending Trade!",
Image = "rbxassetid://4483345998",
Time = 5
})
local args = {
[1] = player
}
ReplicatedStorage.Packages._Index:FindFirstChild("sleitnick_knit@1.4.7").knit.Services.TradeService.RE.RequestTrade.FireServer(unpack({ReplicatedStorage.Packages._Index:FindFirstChild("sleitnick_knit@1.4.7").knit.Services.TradeService.RE.RequestTrade, unpack(args)}))
else
OrionLib:MakeNotification({
Name = "Player Not Found!",
Content = "That Player Does Not Exist. Please Try Again!",
Image = "rbxassetid://4483345998",
Time = 5
})
end
end
})
local Tab = Window:MakeTab({
Name = "Credits",
Icon = "http://www.roblox.com/asset/?id=4370318685",
PremiumOnly = false
})
Tab:AddLabel("Script is made by Hydrazia")
Tab:AddParagraph("Info", "Tap The Button To Copy My Social And Invite Links!")
Tab:AddParagraph("Info", "Script changelogs at our Telegram Channel. If you find any bugs, inform me.")
Tab:AddButton({
Name = "Youtube",
Callback = function()
setclipboard("https://m.youtube.com/channel/UCa1xzPElap7CUTGtj0QhdwA")
end
})
Tab:AddButton({
Name = "Scriptblox",
Callback = function()
setclipboard("https://scriptblox.com/u/Riotscripter")
end
})
Tab:AddButton({
Name = "Telegram Channel",
Callback = function()
setclipboard("https://t.me/Riotscripter")
end
})
Tab:AddBind({
Name = "UI Toggle Keybind",
Default = Enum.KeyCode.RightShift,
Hold = false,
Callback = function()
print("press")
end
})
OrionLib:Init()
elseif game.PlaceId == 7040546583 then -- World 2
if not game:IsLoaded() then
game.Loaded:Wait()
end
local OrionLib = loadstring(game:HttpGet("https://raw.githubusercontent.com/Hydrazia/Exploits/refs/heads/main/ORION"))()
OrionLib:MakeNotification({
Name = "Dragon Blox",
Content = "Scripted By Riotscripter The King Of Lua",
Image = "rbxassetid://4483345998",
Time = 5
})
local Window = OrionLib:MakeWindow({
Name = "RS | DB",
HidePremium = false,
SaveConfig = false,
ConfigFolder = "Dragon Blox",
IntroEnabled = true,
IntroText = "Dragon Blox Gui loaded world 2"
})
-- local mytable
local MyTable = {
["AutoPunch"] = false,
["AutoPunch2"] = false,
["AutoPunch3"] = false,
["AutoPunch4"] = false,
["AutoPunch5"] = false,
["AutoBlock"] = false,
["AutoAgility"] = false,
["AutoEnergy"] = false,
["AutoEnergyBall"] = false,
["AutoEnergyBall2"] = false,
["AutoTechnique"] = false,
["AutoTechnique2"] = false,
["AutoRebirth"] = false,
["AutoRebirth2"] = false,
["AutoRebirth3"] = false,
["AutoSpin"] = false,
["AutoDrops"] = false,
["AutoDrops2"] = false,
["AutoDrops3"] = false,
["AutoDrops4"] = false,
["AutoDrops5"] = false,
}
-- Bool Values
Environment.AutoPunch = false
Environment.AutoPunch2 = false
Environment.AutoPunch3 = false
Environment.AutoPunch4 = false
Environment.AutoPunch5 = false
Environment.AutoBlock = false
Environment.AutoAgility = false
Environment.AutoEnergy = false
Environment.AutoEnergyBall = false
Environment.AutoEnergyBall2 = false
Environment.AutoTechnique = false
Environment.AutoTechnique2 = false
Environment.AutoRebirth = false
Environment.AutoRebirth2 = false
Environment.AutoRebirth3 = false
Environment.AutoSpin = false
Environment.AutoDrops = false
Environment.AutoDrops2 = false
Environment.AutoDrops3 = false
Environment.AutoDrops4 = false
Environment.AutoDrops5 = false
-- locals
local Players = cloneref(game:GetService("Players"))
local TweenService = cloneref(game:GetService("TweenService"))
local Workspace = cloneref(game:GetService("Workspace"))
local ReplicatedStorage = cloneref(game:GetService("ReplicatedStorage"))
local VirtualInputManager = cloneref(game:GetService("VirtualInputManager"))
local TeleportService = cloneref(game:GetService("TeleportService"))
local RunService = cloneref(game:GetService("RunService"))
local StarterGui = cloneref(game:GetService("StarterGui"))
-- functions
local function autopunch()
    while Environment.AutoPunch do task.wait()
        ReplicatedStorage:WaitForChild("Remotes"):WaitForChild("SkillRemote").FireServer(unpack({ReplicatedStorage:WaitForChild("Remotes"):WaitForChild("SkillRemote"), {SkillId = "1", Began = true, ["Typ\208\181"] = 1}}))
    end
end
local function autopunch2()
    while Environment.AutoPunch2 do task.wait()
        require(ReplicatedStorage.Packages.Knit).GetController("SkillManager").TapInput(unpack({{SkillId = "1", Began = true}}))
    end
end
local function autopunch3()
    while Environment.AutoPunch3 do task.wait()
        require(ReplicatedStorage.Packages._Index["sleitnick_knit@1.4.7"].knit).GetController("SkillManager").TapInput(unpack({{SkillId = "1", Began = true}}))
    end
end
local function autopunch4()
    while Environment.AutoPunch4 do task.wait()
        require(ReplicatedStorage.Packages.Knit).GetController("SkillManager"):GetSkillRemote().FireServer(unpack({require(ReplicatedStorage.Packages.Knit).GetController("SkillManager"):GetSkillRemote(), {SkillId = "1", Began = true, ["Typ\208\181"] = 1}}))
    end
end
local function autopunch5()
    while Environment.AutoPunch5 do task.wait()
        require(ReplicatedStorage.Packages._Index["sleitnick_knit@1.4.7"].knit).GetController("SkillManager"):GetSkillRemote().FireServer(unpack({require(ReplicatedStorage.Packages._Index["sleitnick_knit@1.4.7"].knit).GetController("SkillManager"):GetSkillRemote(), {SkillId = "1", Began = true, ["Typ\208\181"] = 1}}))
    end
end
local function autoblock()
    while Environment.AutoBlock do
        VirtualInputManager:SendKeyEvent(true, Enum.KeyCode.B, false, game)
        task.wait()
    end
    VirtualInputManager:SendKeyEvent(false, Enum.KeyCode.B, false, game)
end
local function autoagility()
    while Environment.AutoAgility do task.wait()
        Players.LocalPlayer.Character.Humanoid:Move(Vector3.new(0, 0, -1), true)
    end
end
local function autoenergy()
    while Environment.AutoEnergy do
        VirtualInputManager:SendKeyEvent(true, Enum.KeyCode.C, false, game)
        task.wait()
    end
    VirtualInputManager:SendKeyEvent(false, Enum.KeyCode.C, false, game)
end
local function autoenergyball()
    while Environment.AutoEnergyBall do task.wait()
        require(ReplicatedStorage.Packages.Knit).GetController("SkillManager").TapInput(unpack({{SkillId = "101", Began = true}}))
    end
end
local function autoenergyball2()
    while Environment.AutoEnergyBall2 do task.wait()
        require(ReplicatedStorage.Packages._Index["sleitnick_knit@1.4.7"].knit).GetController("SkillManager").TapInput(unpack({{SkillId = "101", Began = true}}))
    end
end
local function autotechnique()
    while Environment.AutoTechnique do task.wait()
        require(ReplicatedStorage.Packages.Knit).GetController("SkillManager").TapInput(unpack({{SkillId = "8", Began = true}}))
    end
end
local function autotechnique2()
    while Environment.AutoTechnique2 do task.wait()
        require(ReplicatedStorage.Packages._Index["sleitnick_knit@1.4.7"].knit).GetController("SkillManager").TapInput(unpack({{SkillId = "8", Began = true}}))
    end
end
local function autorebirth()
    while Environment.AutoRebirth do task.wait()
        for _, rb in Players.LocalPlayer.PlayerGui.MainGui.Frame.Stats.AspectRatioFrame.StatHUD.Frame:GetChildren() do
            if rb:FindFirstChild("RebirthButton") and rb:FindFirstChild("RebirthButton").BackgroundTransparency ~= 1 then
                ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("PlayerLevelService"):WaitForChild("RF"):WaitForChild("RequestRebirth").InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("PlayerLevelService"):WaitForChild("RF"):WaitForChild("RequestRebirth"), true}))
            end
        end
    end
end
local function autorebirth2()
    while Environment.AutoRebirth2 do task.wait()
        for _, rb in Players.LocalPlayer.PlayerGui.MainGui.Frame.Stats.AspectRatioFrame.StatHUD.Frame:GetChildren() do
            if rb:FindFirstChild("RebirthButton") and rb:FindFirstChild("RebirthButton").BackgroundTransparency ~= 1 then
                require(ReplicatedStorage.Packages.Knit).GetService("PlayerLevelService"):RequestRebirth(true)
            end
        end
    end
end
local function autorebirth3()
    while Environment.AutoRebirth3 do task.wait()
        for _, rb in Players.LocalPlayer.PlayerGui.MainGui.Frame.Stats.AspectRatioFrame.StatHUD.Frame:GetChildren() do
            if rb:FindFirstChild("RebirthButton") and rb:FindFirstChild("RebirthButton").BackgroundTransparency ~= 1 then
                require(ReplicatedStorage.Packages._Index["sleitnick_knit@1.4.7"].knit).GetService("PlayerLevelService"):RequestRebirth(true)
            end
        end
    end
end
local function autodrops()
    while Environment.AutoDrops do
        task.wait()
        for _, v in ReplicatedStorage.Data.ItemDrops:GetChildren() do
            if v:GetAttribute("UserId") == Players.LocalPlayer.UserId and v:GetAttribute("ItemName") then
                local ops = Players.LocalPlayer.Character.HumanoidRootPart.Position
                local pos = v:GetAttribute("Pos")
                Players.LocalPlayer.Character:MoveTo(pos)
                task.wait(0.5)
                require(ReplicatedStorage.Packages.Knit).GetController("ItemDropController"):ClaimItem(v.Name, require(ReplicatedStorage.Packages.Knit).GetController("InventoryController"):GetItem(v:GetAttribute("ItemName")))
                task.wait(0.5)
                Players.LocalPlayer.Character:MoveTo(ops)
            end
        end
    end
end
local function autodrops2()
    while Environment.AutoDrops2 do
        task.wait()
        for _, v in ReplicatedStorage.Data.ItemDrops:GetChildren() do
            if v:GetAttribute("UserId") == Players.LocalPlayer.UserId and v:GetAttribute("ItemName") then
                local ops = Players.LocalPlayer.Character.HumanoidRootPart.Position
                local pos = v:GetAttribute("Pos")
                Players.LocalPlayer.Character:MoveTo(pos)
                task.wait(0.5)
                require(ReplicatedStorage.Packages._Index["sleitnick_knit@1.4.7"].knit).GetController("ItemDropController"):ClaimItem(v.Name, require(ReplicatedStorage.Packages._Index["sleitnick_knit@1.4.7"].knit).GetController("InventoryController"):GetItem(v:GetAttribute("ItemName")))
                task.wait(0.5)
                Players.LocalPlayer.Character:MoveTo(ops)
            end
        end
    end
end
local function autodrops3()
    while Environment.AutoDrops3 do
        task.wait()
        for _, v in ReplicatedStorage.Data.ItemDrops:GetChildren() do
            if v:GetAttribute("UserId") == Players.LocalPlayer.UserId and v:GetAttribute("ItemName") then
                local ops = Players.LocalPlayer.Character.HumanoidRootPart.Position
                local pos = v:GetAttribute("Pos")
                Players.LocalPlayer.Character:MoveTo(pos)
                task.wait(0.5)
                ReplicatedStorage.Packages._Index["sleitnick_knit@1.4.7"].knit.Services.ItemDropService.RF.ClaimItem.InvokeServer(unpack({ReplicatedStorage.Packages._Index["sleitnick_knit@1.4.7"].knit.Services.ItemDropService.RF.ClaimItem, v.Name, v:GetAttribute("ItemName")}))
                task.wait(0.5)
                Players.LocalPlayer.Character:MoveTo(ops)
            end
        end
    end
end
local function autodrops4()
    while Environment.AutoDrops4 do
        task.wait()
        for _, v in ReplicatedStorage.Data.ItemDrops:GetChildren() do
            if v:GetAttribute("UserId") == Players.LocalPlayer.UserId then
                local ops = Players.LocalPlayer.Character.HumanoidRootPart.Position
                Players.LocalPlayer.Character:MoveTo(v:GetAttribute("Pos"))
                task.wait(0.5)
                local drop = Workspace:FindFirstChild("PartStorage"):FindFirstChild("ItemDrop_"..v.Name)
                if drop and drop.Base and drop.Base:FindFirstChildOfClass("ProximityPrompt") then
                    fireproximityprompt(drop.Base.ProximityPrompt)
                end
                task.wait(0.5)
                Players.LocalPlayer.Character:MoveTo(ops)
            end
        end
    end
end
local function autodrops5()
    while Environment.AutoDrops5 do task.wait()
        for _, v in ReplicatedStorage.Data.ItemDrops:GetChildren() do
            if v:GetAttribute("UserId") == Players.LocalPlayer.UserId then
                local ops = Players.LocalPlayer.Character.HumanoidRootPart.Position
                Players.LocalPlayer.Character:MoveTo(v:GetAttribute("Pos"))
                task.wait(0.5)
                local drop = Workspace:FindFirstChild("PartStorage"):FindFirstChild("ItemDrop_"..v.Name)
                if drop and drop.Base and drop.Base:FindFirstChildOfClass("ProximityPrompt") then
                    firesignal(drop.Base.ProximityPrompt.Triggered)
                end
                task.wait(0.5)
                Players.LocalPlayer.Character:MoveTo(ops)
            end
        end
    end
end
local function autospin()
    while Environment.AutoSpin do task.wait()
        ReplicatedStorage.Packages._Index:FindFirstChild("sleitnick_knit@1.4.7").knit.Services.LootPoolService.RF.SpinRequest.InvokeServer(unpack({ReplicatedStorage.Packages._Index:FindFirstChild("sleitnick_knit@1.4.7").knit.Services.LootPoolService.RF.SpinRequest, "SpinMain"}))
    end
end
local Tab = Window:MakeTab({
Name = "AutoFarm",
Icon = "rbxassetid://4483345998",
PremiumOnly = false
})
local Section = Tab:AddSection({
Name = "Autofarm Stuff (Rejoin to Undo)"
})
Tab:AddToggle({
Name = "Auto Punch (Universal)",
Default = false,
Save = true,
Flag = "autopunch_db",
Callback = function(Value)
Environment.AutoPunch = Value
autopunch()
end
})
Tab:AddToggle({
Name = "Auto Punch 2 (Universal)",
Default = false,
Save = true,
Flag = "autopunch2_db",
Callback = function(Value)
Environment.AutoPunch2 = Value
autopunch2()
end
})
Tab:AddToggle({
Name = "Auto Punch 3 (Universal)",
Default = false,
Save = true,
Flag = "autopunch3_db",
Callback = function(Value)
Environment.AutoPunch3 = Value
autopunch3()
end
})
Tab:AddToggle({
Name = "Auto Punch 4 (Universal)",
Default = false,
Save = true,
Flag = "autopunch4_db",
Callback = function(Value)
Environment.AutoPunch4 = Value
autopunch4()
end
})
Tab:AddToggle({
Name = "Auto Punch 5 (Universal)",
Default = false,
Save = true,
Flag = "autopunch5_db",
Callback = function(Value)
Environment.AutoPunch5 = Value
autopunch5()
end
})
Tab:AddToggle({
Name = "Auto Energy (Charge Energy!)",
Default = false,
Save = true,
Flag = "autoenergy_db",
Callback = function(Value)
Environment.AutoEnergy = Value
autoenergy()
end
})
Tab:AddToggle({
Name = "Auto Energy (Charge Energy At 10%!)",
Default = false,
Save = true,
Flag = "autoenergyat10percent_db",
Callback = function(Value)
Environment.AutoEnergyAt10Percent = Value
if Value then
task.spawn(function()
while Environment.AutoEnergyAt10Percent do
local character = Workspace.Characters:FindFirstChild(Players.LocalPlayer.Name)
if character and character:FindFirstChild("Status") then
local status = character.Status
local energy = status:FindFirstChild("CurrentEnergy")
local maxEnergy = status:FindFirstChild("MaxEnergy")
if energy and maxEnergy and energy.Value < (maxEnergy.Value * 0.1) then
VirtualInputManager:SendKeyEvent(true, Enum.KeyCode.C, false, game)
repeat task.wait()
until not character or not character.Parent or not status or not energy or not maxEnergy or energy.Value >= (maxEnergy.Value * 0.5) or not Environment.AutoEnergyAt10Percent
VirtualInputManager:SendKeyEvent(false, Enum.KeyCode.C, false, game)
end
end
task.wait()
end
end)
end
end
})
Tab:AddToggle({
Name = "Auto Block",
Default = false,
Save = true,
Flag = "autoblock_db",
Callback = function(Value)
Environment.AutoBlock = Value
autoblock()
end
})
Tab:AddToggle({
Name = "Auto Agility",
Default = false,
Save = true,
Flag = "autoagility_db",
Callback = function(Value)
Environment.AutoAgility = Value
autoagility()
end
})
Tab:AddToggle({
Name = "Auto Energy Ball (Drains Ki)",
Default = false,
Save = true,
Flag = "autoenergyball_db",
Callback = function(Value)
Environment.AutoEnergyBall = Value
autoenergyball()
end
})
Tab:AddToggle({
Name = "Auto Energy Ball 2 (Drains Ki)",
Default = false,
Save = true,
Flag = "autoenergyball2_db",
Callback = function(Value)
Environment.AutoEnergyBall2 = Value
autoenergyball2()
end
})
Tab:AddToggle({
    Name = "Auto Lock On (Aimbot!)",
    Default = false,
    Save = true,
    Flag = "autolockon_db",
    Callback = function(v)
        Environment.AutoLockOn = v
        if v then 
            coroutine.wrap(function()
                local currentTarget, maxDist = nil, 50
                while Environment.AutoLockOn and task.wait(0.1) do
                    local chr = Players.LocalPlayer.Character
                    if not (chr and chr:FindFirstChild("HumanoidRootPart")) then continue end
                    local pos = chr:WaitForChild("HumanoidRootPart").Position
                    if currentTarget and (not currentTarget:IsDescendantOf(Workspace) or
                        (currentTarget.Humanoid.Health <= 0) or
                        (currentTarget:WaitForChild("HumanoidRootPart").Position - pos).Magnitude > maxDist) then
                        currentTarget = nil
                    end
                    if not currentTarget then
                        local closestDist = maxDist
                        for _,model in Workspace:GetDescendants() do
                            if model:IsA("Model") and model ~= chr then
                                local hum = model:FindFirstChildOfClass("Humanoid")
                                local hrp = model:FindFirstChild("HumanoidRootPart")
                                if hum and hrp and hum.Health > 0 then
                                    local dist = (hrp.Position - pos).Magnitude
                                    if dist < closestDist then
                                        closestDist = dist
                                        currentTarget = model
                                    end
                                end
                            end
                        end
                        if currentTarget then
                            VirtualInputManager:SendKeyEvent(true, Enum.KeyCode.Q, false, game)
                            task.wait(0.05)
                            VirtualInputManager:SendKeyEvent(false, Enum.KeyCode.Q, false, game)
                        end
                    end
                end
            end)() 
        end
    end
})
Tab:AddToggle({
Name = "Auto Rebirth",
Default = false,
Save = true,
Flag = "autorebirth_db",
Callback = function(Value)
Environment.AutoRebirth = Value
autorebirth()
end
})
Tab:AddToggle({
Name = "Auto Rebirth 2",
Default = false,
Save = true,
Flag = "autorebirth2_db",
Callback = function(Value)
Environment.AutoRebirth2 = Value
autorebirth2()
end
})
Tab:AddToggle({
Name = "Auto Rebirth 3",
Default = false,
Save = true,
Flag = "autorebirth3_db",
Callback = function(Value)
Environment.AutoRebirth3 = Value
autorebirth3()
end
})
Tab:AddToggle({
Name = "Auto Kaioken/Technique",
Default = false,
Save = true,
Flag = "autotechnique_db",
Callback = function(Value)
Environment.AutoTechnique = Value
autotechnique()
end
})
Tab:AddToggle({
Name = "Auto Kaioken/Technique 2",
Default = false,
Save = true,
Flag = "autotechnique2_db",
Callback = function(Value)
Environment.AutoTechnique2 = Value
autotechnique2()
end
})
Tab:AddToggle({
Name = "Auto Kaioken/Technique 3",
Default = false,
Save = true,
Flag = "autotechnique3_db",
Callback = function(Value)
Environment.AutoTechnique3 = Value
if Value then
coroutine.wrap(function()
while Environment.AutoTechnique3 do
VirtualInputManager:SendKeyEvent(true, Enum.KeyCode.M, false, game)
task.wait()
end
end)()
end
end
})
Tab:AddButton({
Name = "Upgrade Technique",
Callback = function()
if Workspace.Misc.NPC.Main:FindFirstChild("KaiokenTrainerNPC") and Workspace.Misc.NPC.Main.KaiokenTrainerNPC:FindFirstChild("Head") then
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Main.KaiokenTrainerNPC:WaitForChild("Head").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer").InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer"), "TechniqueKaiokenGiver", 1, Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Main"):WaitForChild("KaiokenTrainerNPC")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
end
})
Tab:AddButton({
Name = "Upgrade Technique 2",
Callback = function()
if Workspace.Misc.NPC.Main:FindFirstChild("KaiokenTrainerNPC") and Workspace.Misc.NPC.Main.KaiokenTrainerNPC:FindFirstChild("HumanoidRootPart") then
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Main.KaiokenTrainerNPC:WaitForChild("HumanoidRootPart").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer").InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer"), "TechniqueKaiokenGiver", 1, Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Main"):WaitForChild("KaiokenTrainerNPC")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
end
})
Tab:AddButton({
Name = "Claim Daily Reward",
Callback = function()
require(ReplicatedStorage.Packages.Knit).GetService("LoginRewardsService").RF.ClaimDay.InvokeServer(unpack({require(ReplicatedStorage.Packages.Knit).GetService("LoginRewardsService").RF.ClaimDay}))
end
})
Tab:AddButton({
Name = "Claim Daily Reward 2",
Callback = function()
require(ReplicatedStorage.Packages._Index["sleitnick_knit@1.4.7"].knit).GetService("LoginRewardsService").RF.ClaimDay.InvokeServer(unpack({require(ReplicatedStorage.Packages._Index["sleitnick_knit@1.4.7"].knit).GetService("LoginRewardsService").RF.ClaimDay}))
end
})
Tab:AddToggle({
Name = "Auto Claim Daily",
Default = false,
Save = true,
Flag = "autoclaimdaily_db",
Callback = function(Value)
Environment.AutoClaimDaily = Value
if Value then
coroutine.wrap(function()
while Environment.AutoClaimDaily do
require(ReplicatedStorage.Packages.Knit).GetService("LoginRewardsService").RF.ClaimDay.InvokeServer(unpack({require(ReplicatedStorage.Packages.Knit).GetService("LoginRewardsService").RF.ClaimDay}))
task.wait()
end
end)()
end
end
})
Tab:AddToggle({
Name = "Auto Claim Daily 2",
Default = false,
Save = true,
Flag = "autoclaimdaily2_db",
Callback = function(Value)
Environment.AutoClaimDaily2 = Value
if Value then
coroutine.wrap(function()
while Environment.AutoClaimDaily2 do
require(ReplicatedStorage.Packages._Index["sleitnick_knit@1.4.7"].knit).GetService("LoginRewardsService").RF.ClaimDay.InvokeServer(unpack({require(ReplicatedStorage.Packages._Index["sleitnick_knit@1.4.7"].knit).GetService("LoginRewardsService").RF.ClaimDay}))
task.wait()
end
end)()
end
end
})
Tab:AddToggle({
Name = "Auto Dash",
Default = false,
Save = true,
Flag = "autodash_db",
Callback = function(Value)
Environment.AutoDash = Value
if Value then
coroutine.wrap(function()
while Environment.AutoDash do
require(ReplicatedStorage.Packages.Knit).GetController("SkillManager").TapInput(unpack({{SkillId = "7", Began = true}}))
task.wait()
end
end)()
end
end
})
Tab:AddToggle({
Name = "Auto Dash 2",
Default = false,
Save = true,
Flag = "autodash2_db",
Callback = function(Value)
Environment.AutoDash2 = Value
if Value then
coroutine.wrap(function()
while Environment.AutoDash2 do
require(ReplicatedStorage.Packages._Index["sleitnick_knit@1.4.7"].knit).GetController("SkillManager").TapInput(unpack({{SkillId = "7", Began = true}}))
task.wait()
end
end)()
end
end
})
Tab:AddToggle({
Name = "Auto Spin",
Default = false,
Save = true,
Flag = "autospin_db",
Callback = function(Value)
Environment.AutoSpin = Value
autospin()
end
})
Tab:AddButton({
Name = "Activate/Deactivate Fusion",
Callback = function()
require(ReplicatedStorage.Packages.Knit).GetController("SkillManager").TapInput(unpack({{SkillId = "9", Began = true}}))
end
})
Tab:AddButton({
Name = "Activate/Deactivate Fusion 2",
Callback = function()
require(ReplicatedStorage.Packages._Index["sleitnick_knit@1.4.7"].knit).GetController("SkillManager").TapInput(unpack({{SkillId = "9", Began = true}}))
end
})
Tab:AddToggle({
Name = "Auto Grab Drops [Op]",
Default = false,
Save = true,
Flag = "autodrops_db",
Callback = function(Value)
Environment.AutoDrops = Value
autodrops()
end
})
Tab:AddToggle({
Name = "Auto Grab Drops 2 [Op]",
Default = false,
Save = true,
Flag = "autodrops2_db",
Callback = function(Value)
Environment.AutoDrops2 = Value
autodrops2()
end
})
Tab:AddToggle({
Name = "Auto Grab Drops 3 [Op]",
Default = false,
Save = true,
Flag = "autodrops3_db",
Callback = function(Value)
Environment.AutoDrops3 = Value
autodrops3()
end
})
Tab:AddToggle({
Name = "Auto Grab Drops 4 [Op]",
Default = false,
Save = true,
Flag = "autodrops4_db",
Callback = function(Value)
Environment.AutoDrops4 = Value
autodrops4()
end
})
Tab:AddToggle({
Name = "Auto Grab Drops 5 [Op]",
Default = false,
Save = true,
Flag = "autodrops5_db",
Callback = function(Value)
Environment.AutoDrops5 = Value
autodrops5()
end
})
Tab:AddButton({
Name = "Activate/Deactivate Sprint",
Callback = function()
ReplicatedStorage.Packages._Index:FindFirstChild("sleitnick_knit@1.4.7").knit.Services.FlightService.RE.Sprint.FireServer(unpack({ReplicatedStorage.Packages._Index:FindFirstChild("sleitnick_knit@1.4.7").knit.Services.FlightService.RE.Sprint}))
end
})
Tab:AddToggle({
    Name = "Auto Transform",
    Default = false,
    Save = true,
    Flag = "autotransform_db",
    Callback = function(Value)
        Environment.AutoTransform = Value
        if Value then                       task.spawn(function()while Environment.AutoTransform do
                    local char = Players.LocalPlayer.Character or Players.LocalPlayer.CharacterAdded:Wait()
                    local gauge = char:FindFirstChild("Status") and char.Status:FindFirstChild("FormGauge")
                    if gauge and gauge.Value >= 1 then
                        require(ReplicatedStorage.Packages.Knit).GetController("SkillManager").TapInput(unpack({{SkillId = "10", Began = true}}))
                        repeat
                            task.wait()
                            char = Players.LocalPlayer.Character
                            gauge = char and char:FindFirstChild("Status") and char.Status:FindFirstChild("FormGauge")
                            if not Environment.AutoTransform then break end
                        until not gauge or gauge.Value < 1
                    end
                    task.wait()
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Transform 2",
    Default = false,
    Save = true,
    Flag = "autotransform2_db",
    Callback = function(Value)
        Environment.AutoTransform2 = Value
        if Value then                       task.spawn(function()while Environment.AutoTransform2 do
                    local char = Players.LocalPlayer.Character or Players.LocalPlayer.CharacterAdded:Wait()
                    local gauge = char:FindFirstChild("Status") and char.Status:FindFirstChild("FormGauge")
                    if gauge and gauge.Value >= 1 then
                        require(ReplicatedStorage.Packages.Knit).GetController("SkillManager").TapInput(unpack({{SkillId = "10", Began = true}}))
                        repeat
                            task.wait()
                            char = Players.LocalPlayer.Character
                            gauge = char and char:FindFirstChild("Status") and char.Status:FindFirstChild("FormGauge")
                            if not Environment.AutoTransform2 then break end
                        until not gauge or gauge.Value < 1
                    end
                    task.wait()
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Transform (PC!)",
    Default = false,
    Save = true,
    Flag = "autotransform3_db",
    Callback = function(Value)
        Environment.AutoTransform3 = Value
        if Value then                       task.spawn(function()while Environment.AutoTransform3 do
                    local char = Players.LocalPlayer.Character or Players.LocalPlayer.CharacterAdded:Wait()
                    local gauge = char:FindFirstChild("Status") and char.Status:FindFirstChild("FormGauge")
                    if gauge and gauge.Value >= 1 then
                        VirtualInputManager:SendKeyEvent(true, Enum.KeyCode.Y, false, game)
                        repeat
                            task.wait()
                            char = Players.LocalPlayer.Character
                            gauge = char and char:FindFirstChild("Status") and char.Status:FindFirstChild("FormGauge")
                            if not Environment.AutoTransform3 then break end
                        until not gauge or gauge.Value < 1
                    end
                    task.wait()
                end
            end)
        end
    end
})
Tab:AddButton({
Name = "Transform/Untransform",
Callback = function()
require(ReplicatedStorage.Packages.Knit).GetController("SkillManager").TapInput(unpack({{SkillId = "10", Began = true}}))
end
})
Tab:AddButton({
Name = "Transform/Untransform 2",
Callback = function()
require(ReplicatedStorage.Packages._Index["sleitnick_knit@1.4.7"].knit).GetController("SkillManager").TapInput(unpack({{SkillId = "10", Began = true}}))
end
})
Tab:AddButton({
Name = "Transform/UnTransform 3",
Callback = function()
VirtualInputManager:SendKeyEvent(true, Enum.KeyCode.Y, false, game)
end
})
Tab:AddToggle({
Name = "Anti Death (Godmode!)",
Default = false,
Save = true,
Flag = "antideath_db",
Callback = function(Value)
Environment.AntiDeath = Value
while Environment.AntiDeath do task.wait()
if Players.LocalPlayer.Character and Players.LocalPlayer.Character:FindFirstChild("Humanoid") then
local Humanoid = Players.LocalPlayer.Character.Humanoid
if Humanoid.Health <= 0 then
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.CharacterAdded:Wait()
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
end
end
end
})
Tab:AddToggle({
Name = "Anti Death 2 (Godmode!)",
Default = false,
Save = true,
Flag = "antideath2_db",
Callback = function(Value)
Environment.AntiDeath2 = Value
while Environment.AntiDeath2 do
if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid") then
local Humanoid = LocalPlayer.Character.Humanoid
if Humanoid.Health <= 0 then
local RootPart = LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
if RootPart then
local OldCFrame = RootPart.CFrame
LocalPlayer.CharacterAdded:Wait()
local NewRootPart = LocalPlayer.Character:WaitForChild("HumanoidRootPart")
if NewRootPart then
TweenService:Create(
NewRootPart,
TweenInfo.new(2, Enum.EasingStyle.Linear),
{CFrame = OldCFrame}
):Play()
end
end
end
end
task.wait()
end
end
})
Tab:AddToggle({
Name = "Loopkill All (Client Sided!)",
Default = false,
Save = true,
Flag = "loopkillall_db",
Callback = function(Value)
Environment.loopkillall = Value
while Environment.loopkillall do
task.wait(1.25)
pcall(function()
sethiddenproperty(Players.LocalPlayer, "SimulationRadius", math.huge)
sethiddenproperty(Players.LocalPlayer, "MaxSimulationRadius", math.huge)
for _, obj in game.Workspace:GetDescendants() do
if obj:IsA("Humanoid") and obj.Parent.Name ~= Players.LocalPlayer.Name then
obj.Health = 0
end
end
end)
end
end
})
Tab:AddToggle({
Name = "Loopkill Self (Client Sided!)",
Default = false,
Save = true,
Flag = "loopkillself_db",
Callback = function(Value)
Environment.loopkillself = Value
while Environment.loopkillself do task.wait()
Players.LocalPlayer.Character:WaitForChild("Humanoid").Health = 0
end
end
})
Tab:AddToggle({
Name = "Loopkill Self 2 (Client Sided!)",
Default = false,
Save = true,
Flag = "loopkillself2_db",
Callback = function(Value)
Environment.loopkillself2 = Value
while Environment.loopkillself2 do task.wait()
Players.LocalPlayer.Character.Humanoid.Health = 0
end
end
})
Tab:AddToggle({
Name = "Loopkill Self 3 (Client Sided!)",
Default = false,
Save = true,
Flag = "loopkillself3_db",
Callback = function(Value)
Environment.loopkillself3 = Value
while Environment.loopkillself3 do task.wait()
Players.LocalPlayer.Character:FindFirstChild("Humanoid").Health = 0
end
end
})
Tab:AddToggle({
Name = "Auto Destroy Display Name",
Default = false,
Flag = "autodestroyname_db",
Save = true,
Callback = function(Value)
Environment.autodestroyname = Value
while Environment.autodestroyname do task.wait()
for _, v in Players.LocalPlayer.Character:WaitForChild("Head"):GetChildren() do
if v:IsA("BillboardGui") then
v:Destroy()
end
end
end
end
})
Tab:AddButton({
Name = "Destroy Username",
Callback = function()
for _, v in Players.LocalPlayer.Character:WaitForChild("Head"):GetChildren() do
if v:IsA("BillboardGui") then
v:Destroy()
end
end
end
})
Tab:AddToggle({
    Name = "Auto Nearby Mobs (Back Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbymobs_db",
    Callback = function(Value)
        Environment.autotpnearbymobs = Value
        if not Value then return end
        while Environment.autotpnearbymobs do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    local mobHRP = nearestMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.lookAt(mobHRP.Position + (mobHRP.CFrame.LookVector * -5), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Mobs 2 (Head Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbymobs2_db",
    Callback = function(Value)
        Environment.autotpnearbymobs2 = Value
        if not Value then return end
        while Environment.autotpnearbymobs2 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("Head").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    tweenTo(rootPart, nearestMob:WaitForChild("Head").CFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Mobs 3 (Front Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbymobs3_db",
    Callback = function(Value)
        Environment.autotpnearbymobs3 = Value
        if not Value then return end
        while Environment.autotpnearbymobs3 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    local mobHRP = nearestMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.lookAt(mobHRP.Position + (mobHRP.CFrame.LookVector * 5), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Mobs 4 (Under Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbymobs4_db",
    Callback = function(Value)
        Environment.autotpnearbymobs4 = Value
        if not Value then return end
        while Environment.autotpnearbymobs4 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    local mobHRP = nearestMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.new(mobHRP.Position - Vector3.new(0, 5, 0), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Boss Mobs (Back Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbybossmobs_db",
    Callback = function(Value)
        Environment.autotpnearbybossmobs = Value
        if not Value then return end
        while Environment.autotpnearbybossmobs do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v:IsA("Model") and v.Name ~= "Veggy" and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.lookAt(mobHRP.Position - (mobHRP.CFrame.LookVector * -5), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Boss Mobs 2 (Head Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbybossmobs2_db",
    Callback = function(Value)
        Environment.autotpnearbybossmobs2 = Value
        if not Value then return end
        while Environment.autotpnearbybossmobs2 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v:IsA("Model") and v.Name ~= "Veggy" and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("Head").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    tweenTo(rootPart, nearestBossMob:WaitForChild("Head").CFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Boss Mobs 3 (Front Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbybossmobs3_db",
    Callback = function(Value)
        Environment.autotpnearbybossmobs3 = Value
        if not Value then return end
        while Environment.autotpnearbybossmobs3 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v:IsA("Model") and v.Name ~= "Veggy" and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.lookAt(mobHRP.Position + (mobHRP.CFrame.LookVector * 5), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Boss Mobs 4 (Under Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbybossmobs4_db",
    Callback = function(Value)
        Environment.autotpnearbybossmobs4 = Value
        if not Value then return end
        while Environment.autotpnearbybossmobs4 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v:IsA("Model") and v.Name ~= "Veggy" and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.new(mobHRP.Position - Vector3.new(0, 5, 0), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddButton({
Name = "Enable Gamepasses (Visual!)",
Callback = function()
for _, v in Players.LocalPlayer.Stats.GamePasses:GetChildren() do
v.Value = true
end
end
})
Tab:AddButton({
Name = "Disable Gamepasses (Visual!)",
Callback = function()
for _, v in Players.LocalPlayer.Stats.GamePasses:GetChildren() do
v.Value = false
end
end
})
Tab:AddButton({
    Name = "Hitbox Players",
    Callback = function()
        HitboxSettings = {
            Enabled = true,
            Size = 50,
            Transparency = 0.3,
            Color = "Bright violet",
            Material = Enum.Material.Neon,
            Damage = 15
        }
        local function applyHitboxToPlayer(player)
            if player and player ~= Players.LocalPlayer and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                local hrp = player.Character:WaitForChild("HumanoidRootPart")
                pcall(function()
                    hrp.Size = Vector3.new(HitboxSettings.Size, HitboxSettings.Size, HitboxSettings.Size)
                    hrp.Transparency = HitboxSettings.Transparency
                    hrp.BrickColor = BrickColor.new(HitboxSettings.Color)
                    hrp.Material = HitboxSettings.Material
                    hrp.CanCollide = false
                end)
            end
        end
        for _, player in Players:GetPlayers() do
            applyHitboxToPlayer(player)
        end
        Players.PlayerAdded:Connect(function(player)
            player.CharacterAdded:Connect(function(character)
                applyHitboxToPlayer(player)
            end)
        end)
        Players.PlayerRemoving:Connect(function(player)
            if player and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                local hrp = player.Character:WaitForChild("HumanoidRootPart")
                pcall(function()
                    hrp.Size = Vector3.new(2, 2, 1)
                    hrp.Transparency = 0
                    hrp.BrickColor = BrickColor.new("Medium stone grey")
                    hrp.Material = Enum.Material.Plastic
                    hrp.CanCollide = true
                end)
            end
        end)
        StarterGui:SetCore("SendNotification", {
            Title = "Hitbox Script Loaded",
            Text = "Unique hitbox script activated.",
            Duration = 5
        })
        Players.LocalPlayer.CharacterAdded:Connect(function(character)
            character.ChildAdded:Connect(function(child)
                if child:IsA("Tool") or child:IsA("Accessory") then
                    if HitboxSettings.Enabled and child:FindFirstChild("Handle") then
                        local handle = child.Handle
                        handle.Touched:Connect(function(hit)
                            if hit.Parent and hit.Parent:FindFirstChild("Humanoid") and hit.Parent.Name ~= Players.LocalPlayer.Name then
                                hit.Parent.Humanoid:TakeDamage(HitboxSettings.Damage)
                            end
                        end)
                    end
                end
            end)
        end)
    end
})
Tab:AddButton({
Name = "Server Hop",
Callback = function()
TeleportService:Teleport(3177438863, Players.LocalPlayer);
end
})
Tab:AddButton({
Name = "Server Hop To Dead Server",
Callback =function()
local Http = game:GetService("HttpService")
local TPS = game:GetService("TeleportService")
local Api = "https://games.roblox.com/v1/games/"
local _place = game.PlaceId
local _servers = Api.._place.."/servers/Public?sortOrder=Asc&limit=100"
function ListServers(cursor)
local Raw = game:HttpGet(_servers .. ((cursor and "&cursor="..cursor) or ""))
return Http:JSONDecode(Raw)
end
local Server, Next; repeat
local Servers = ListServers(Next)
Server = Servers.data[1]
Next = Servers.nextPageCursor
until Server
TPS:TeleportToPlaceInstance(_place,Server.id,Players.LocalPlayer)
end
})
Tab:AddButton({
Name = "Server Hop If You Die",
Callback = function()
task.wait(0.75)
Players.LocalPlayer.Character.Humanoid.Died:Connect(function()
local x = {}
for _, v in game:GetService("HttpService"):JSONDecode(game:HttpGetAsync("https://games.roblox.com/v1/games/" .. game.PlaceId .. "/servers/Public?sortOrder=Asc&limit=100")).data do
if type(v) == "table" and v.maxPlayers > v.playing and v.id ~= game.JobId then
x[#x + 1] = v.id
end
end
if #x > 0 then
TeleportService:TeleportToPlaceInstance(game.PlaceId, x[math.random(1, #x)])
else
print("Server not found.")
end
end)
end
})
Tab:AddButton({
Name = "Leave Server",
Callback = function()
game:Shutdown();
end
})
Tab:AddButton({
Name = "Rejoin Server",
Callback = function()
TeleportService:TeleportToPlaceInstance(game.PlaceId, game.JobId, Players.LocalPlayer)
end
})
Tab:AddButton({
Name = "Unload Gui",
Callback = function()
OrionLib:Destroy()
end
})
local Tab = Window:MakeTab({
Name = "Mobs",
Icon = "rbxassetid://4483345998",
PremiumOnly = false
})
local Section = Tab:AddSection({
Name = "Mobs Select Tab (Only Select 1 At A Time!)"
})
Tab:AddLabel("Just Get The Quest To Farm Your Mob")
Tab:AddLabel("Gordo Army")
Tab:AddButton({
Name = "Get Gordo Army Quest",
Callback = function()
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCCrimson1:WaitForChild("Head").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7").knit.Services.DialogService.RF.Answer.InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7").knit.Services.DialogService.RF.Answer, "NPCQuest_Crimson1", 2, Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Quests"):WaitForChild("QuestNPCCrimson1")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
})
Tab:AddButton({
Name = "Get Gordo Army Quest 2",
Callback = function()
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCCrimson1:WaitForChild("HumanoidRootPart").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7").knit.Services.DialogService.RF.Answer.InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7").knit.Services.DialogService.RF.Answer, "NPCQuest_Crimson1", 2, Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Quests"):WaitForChild("QuestNPCCrimson1")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
})
Tab:AddToggle({
    Name = "Auto Nearby Gordo Army Mobs (Back Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbygordoarmymobs_db",
    Callback = function(Value)
        Environment.autotpnearbygordoarmymobs = Value
        if not Value then return end
        while Environment.autotpnearbygordoarmymobs do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then
                                part.CanCollide = false
                            end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Gordo Army" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    local mobHRP = nearestMob:WaitForChild("HumanoidRootPart")
                    local offsetPosition = mobHRP.Position - (mobHRP.CFrame.LookVector * 5)
                    local targetCFrame = CFrame.new(offsetPosition, mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Gordo Army Mobs 2 (Head Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbygordoarmymobs2_db",
    Callback = function(Value)
        Environment.autotpnearbygordoarmymobs2 = Value
        if not Value then return end
        while Environment.autotpnearbygordoarmymobs2 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then
                                part.CanCollide = false
                            end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Gordo Army" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("Head").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    tweenTo(rootPart, nearestMob:WaitForChild("Head").CFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Gordo Army Mobs 3 (Front Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbygordoarmymobs3_db",
    Callback = function(Value)
        Environment.autotpnearbygordoarmymobs3 = Value
        if not Value then return end
        while Environment.autotpnearbygordoarmymobs3 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then
                                part.CanCollide = false
                            end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Gordo Army" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    local mobHRP = nearestMob:WaitForChild("HumanoidRootPart")
                    local offsetPosition = mobHRP.Position + (mobHRP.CFrame.LookVector * 5)
                    local targetCFrame = CFrame.new(offsetPosition, mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Gordo Army Mobs 4 (Under Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbygordoarmymobs4_db",
    Callback = function(Value)
        Environment.autotpnearbygordoarmymobs4 = Value
        if not Value then return end
        while Environment.autotpnearbygordoarmymobs4 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then
                                part.CanCollide = false
                            end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Gordo Army" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    local mobHRP = nearestMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.new(mobHRP.Position - Vector3.new(0, 5, 0), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddLabel("Calci Army")
Tab:AddButton({
Name = "Get Calci Army Quest",
Callback = function()
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCCrimson2:WaitForChild("Head").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7").knit.Services.DialogService.RF.Answer.InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7").knit.Services.DialogService.RF.Answer, "NPCQuest_Crimson2", 2, Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Quests"):WaitForChild("QuestNPCCrimson2")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
})
Tab:AddButton({
Name = "Get Calci Army Quest 2",
Callback = function()
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCCrimson2:WaitForChild("HumanoidRootPart").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7").knit.Services.DialogService.RF.Answer.InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7").knit.Services.DialogService.RF.Answer, "NPCQuest_Crimson2", 2, Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Quests"):WaitForChild("QuestNPCCrimson2")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
})
Tab:AddToggle({
    Name = "Auto Nearby Calci Army Mobs (Back Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbycalciarmymobs_db",
    Callback = function(Value)
        Environment.autotpnearbycalciarmymobs = Value
        if not Value then return end
        while Environment.autotpnearbycalciarmymobs do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then
                                part.CanCollide = false
                            end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Calci Army" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    local mobHRP = nearestMob:WaitForChild("HumanoidRootPart")
                    local offsetPosition = mobHRP.Position - (mobHRP.CFrame.LookVector * 5)
                    local targetCFrame = CFrame.new(offsetPosition, mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Calci Army Mobs 2 (Head Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbycalciarmymobs2_db",
    Callback = function(Value)
        Environment.autotpnearbycalciarmymobs2 = Value
        if not Value then return end
        while Environment.autotpnearbycalciarmymobs2 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then
                                part.CanCollide = false
                            end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Calci Army" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("Head").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    tweenTo(rootPart, nearestMob:WaitForChild("Head").CFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Calci Army Mobs 3 (Front Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbycalciarmymobs3_db",
    Callback = function(Value)
        Environment.autotpnearbycalciarmymobs3 = Value
        if not Value then return end
        while Environment.autotpnearbycalciarmymobs3 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then
                                part.CanCollide = false
                            end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Calci Army" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    local mobHRP = nearestMob:WaitForChild("HumanoidRootPart")
                    local offsetPosition = mobHRP.Position + (mobHRP.CFrame.LookVector * 5)
                    local targetCFrame = CFrame.new(offsetPosition, mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Calci Army Mobs 4 (Under Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbycalciarmymobs4_db",
    Callback = function(Value)
        Environment.autotpnearbycalciarmymobs4 = Value
        if not Value then return end
        while Environment.autotpnearbycalciarmymobs4 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then
                                part.CanCollide = false
                            end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Calci Army" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    local mobHRP = nearestMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.new(mobHRP.Position - Vector3.new(0, 5, 0), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddLabel("Truffle Army")
Tab:AddButton({
Name = "Get Truffle Army Quest",
Callback = function()
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCCrimson3:WaitForChild("Head").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7").knit.Services.DialogService.RF.Answer.InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7").knit.Services.DialogService.RF.Answer, "NPCQuest_Crimson3", 2, Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Quests"):WaitForChild("QuestNPCCrimson3")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
})
Tab:AddButton({
Name = "Get Truffle Army Quest 2",
Callback = function()
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCCrimson3:WaitForChild("HumanoidRootPart").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7").knit.Services.DialogService.RF.Answer.InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7").knit.Services.DialogService.RF.Answer, "NPCQuest_Crimson3", 2, Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Quests"):WaitForChild("QuestNPCCrimson3")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
})
Tab:AddToggle({
    Name = "Auto Nearby Truffle Army Mobs (Back Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbytrufflearmymobs_db",
    Callback = function(Value)
        Environment.autotpnearbytrufflearmymobs = Value
        if not Value then return end
        while Environment.autotpnearbytrufflearmymobs do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then
                                part.CanCollide = false
                            end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Truffle Army" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    local mobHRP = nearestMob:WaitForChild("HumanoidRootPart")
                    local offsetPosition = mobHRP.Position - (mobHRP.CFrame.LookVector * 5)
                    local targetCFrame = CFrame.new(offsetPosition, mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Truffle Army Mobs 2 (Head Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbytrufflearmymobs2_db",
    Callback = function(Value)
        Environment.autotpnearbytrufflearmymobs2 = Value
        if not Value then return end
        while Environment.autotpnearbytrufflearmymobs2 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then
                                part.CanCollide = false
                            end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Truffle Army" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("Head").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    tweenTo(rootPart, nearestMob:WaitForChild("Head").CFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Truffle Army Mobs 3 (Front Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbytrufflearmymobs3_db",
    Callback = function(Value)
        Environment.autotpnearbytrufflearmymobs3 = Value
        if not Value then return end
        while Environment.autotpnearbytrufflearmymobs3 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then
                                part.CanCollide = false
                            end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Truffle Army" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    local mobHRP = nearestMob:WaitForChild("HumanoidRootPart")
                    local offsetPosition = mobHRP.Position + (mobHRP.CFrame.LookVector * 5)
                    local targetCFrame = CFrame.new(offsetPosition, mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Truffle Army Mobs 4 (Under Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbytrufflearmymobs4_db",
    Callback = function(Value)
        Environment.autotpnearbytrufflearmymobs4 = Value
        if not Value then return end
        while Environment.autotpnearbytrufflearmymobs4 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then
                                part.CanCollide = false
                            end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Truffle Army" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    local mobHRP = nearestMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.new(mobHRP.Position - Vector3.new(0, 5, 0), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddLabel("Nemba Army")
Tab:AddButton({
Name = "Get Nemba Army Quest",
Callback = function()
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCCrimson4:WaitForChild("Head").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7").knit.Services.DialogService.RF.Answer.InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7").knit.Services.DialogService.RF.Answer, "NPCQuest_Crimson4", 2, Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Quests"):WaitForChild("QuestNPCCrimson4")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
})
Tab:AddButton({
Name = "Get Nemba Army Quest 2",
Callback = function()
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCCrimson4:WaitForChild("HumanoidRootPart").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7").knit.Services.DialogService.RF.Answer.InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7").knit.Services.DialogService.RF.Answer, "NPCQuest_Crimson4", 2, Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Quests"):WaitForChild("QuestNPCCrimson4")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
})
Tab:AddToggle({
    Name = "Auto Nearby Nemba Army Mobs (Back Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbynembaarmymobs_db",
    Callback = function(Value)
        Environment.autotpnearbynembaarmymobs = Value
        if not Value then return end
        while Environment.autotpnearbynembaarmymobs do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then
                                part.CanCollide = false
                            end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Nemba Army" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    local mobHRP = nearestMob:WaitForChild("HumanoidRootPart")
                    local offsetPosition = mobHRP.Position - (mobHRP.CFrame.LookVector * 5)
                    local targetCFrame = CFrame.new(offsetPosition, mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Nemba Army Mobs 2 (Head Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbynembaarmymobs2_db",
    Callback = function(Value)
        Environment.autotpnearbynembaarmymobs2 = Value
        if not Value then return end
        while Environment.autotpnearbynembaarmymobs2 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then
                                part.CanCollide = false
                            end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Nemba Army" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("Head").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    tweenTo(rootPart, nearestMob:WaitForChild("Head").CFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Nemba Army Mobs 3 (Front Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbynembaarmymobs3_db",
    Callback = function(Value)
        Environment.autotpnearbynembaarmymobs3 = Value
        if not Value then return end
        while Environment.autotpnearbynembaarmymobs3 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then
                                part.CanCollide = false
                            end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Nemba Army" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    local mobHRP = nearestMob:WaitForChild("HumanoidRootPart")
                    local offsetPosition = mobHRP.Position + (mobHRP.CFrame.LookVector * 5)
                    local targetCFrame = CFrame.new(offsetPosition, mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Nemba Army Mobs 4 (Under Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbynembaarmymobs4_db",
    Callback = function(Value)
        Environment.autotpnearbynembaarmymobs4 = Value
        if not Value then return end
        while Environment.autotpnearbynembaarmymobs4 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then
                                part.CanCollide = false
                            end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Nemba Army" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    local mobHRP = nearestMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.new(mobHRP.Position - Vector3.new(0, 5, 0), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddLabel("Just Get The Quest To Farm Your Boss Mob")
Tab:AddLabel("Gordo")
Tab:AddButton({
Name = "Get Gordo Quest",
Callback = function()
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCCrimson1:WaitForChild("Head").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer").InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer"), "NPCQuest_Crimson1", 7, Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Quests"):WaitForChild("QuestNPCCrimson1")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
})
Tab:AddButton({
Name = "Get Gordo Quest 2",
Callback = function()
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCCrimson1:WaitForChild("HumanoidRootPart").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer").InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer"), "NPCQuest_Crimson1", 7, Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Quests"):WaitForChild("QuestNPCCrimson1")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
})
Tab:AddToggle({
    Name = "Auto Nearby Gordo Boss Mobs (Back Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbygordobossmobs_db",
    Callback = function(Value)
        Environment.autotpnearbygordobossmobs = Value
        if not Value then return end
        while Environment.autotpnearbygordobossmobs do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local originalCanCollide = rootPart.CanCollide
                    rootPart.CanCollide = false
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                    rootPart.CanCollide = originalCanCollide
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Gordo" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local offsetPosition = mobHRP.Position - (mobHRP.CFrame.LookVector * 5)
                    local targetCFrame = CFrame.new(offsetPosition, mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Gordo Boss Mobs 2 (Head Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbygordobossmobs2_db",
    Callback = function(Value)
        Environment.autotpnearbygordobossmobs2 = Value
        if not Value then return end
        while Environment.autotpnearbygordobossmobs2 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local originalCanCollide = rootPart.CanCollide
                    rootPart.CanCollide = false
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                    rootPart.CanCollide = originalCanCollide
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Gordo" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("Head").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    tweenTo(rootPart, nearestBossMob:WaitForChild("Head").CFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Gordo Boss Mobs 3 (Front Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbygordobossmobs3_db",
    Callback = function(Value)
        Environment.autotpnearbygordobossmobs3 = Value
        if not Value then return end
        while Environment.autotpnearbygordobossmobs3 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local originalCanCollide = rootPart.CanCollide
                    rootPart.CanCollide = false
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                    rootPart.CanCollide = originalCanCollide
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Gordo" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local offsetPosition = mobHRP.Position + (mobHRP.CFrame.LookVector * 5)
                    local targetCFrame = CFrame.new(offsetPosition, mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Gordo Boss Mobs 4 (Under Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbygordobossmobs4_db",
    Callback = function(Value)
        Environment.autotpnearbygordobossmobs4 = Value
        if not Value then return end
        while Environment.autotpnearbygordobossmobs4 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local originalCanCollide = rootPart.CanCollide
                    rootPart.CanCollide = false
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                    rootPart.CanCollide = originalCanCollide
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Gordo" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.new(mobHRP.Position - Vector3.new(0, 5, 0), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddLabel("Calci")
Tab:AddButton({
Name = "Get Calci Quest",
Callback = function()
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCCrimson2:WaitForChild("Head").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer").InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer"), "NPCQuest_Crimson2", 7, Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Quests"):WaitForChild("QuestNPCCrimson2")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
})
Tab:AddButton({
Name = "Get Calci Quest 2",
Callback = function()
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCCrimson2:WaitForChild("HumanoidRootPart").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer").InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer"), "NPCQuest_Crimson2", 7, Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Quests"):WaitForChild("QuestNPCCrimson2")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
})
Tab:AddToggle({
    Name = "Auto Nearby Calci Boss Mobs (Back Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbycalcibossmobs_db",
    Callback = function(Value)
        Environment.autotpnearbycalcibossmobs = Value
        if not Value then return end
        while Environment.autotpnearbycalcibossmobs do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local originalCanCollide = rootPart.CanCollide
                    rootPart.CanCollide = false
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                    rootPart.CanCollide = originalCanCollide
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Calci" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local offsetPosition = mobHRP.Position - (mobHRP.CFrame.LookVector * 5)
                    local targetCFrame = CFrame.new(offsetPosition, mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Calci Boss Mobs 2 (Head Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbycalcibossmobs2_db",
    Callback = function(Value)
        Environment.autotpnearbycalcibossmobs2 = Value
        if not Value then return end
        while Environment.autotpnearbycalcibossmobs2 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local originalCanCollide = rootPart.CanCollide
                    rootPart.CanCollide = false
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                    rootPart.CanCollide = originalCanCollide
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Calci" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("Head").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    tweenTo(rootPart, nearestBossMob:WaitForChild("Head").CFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Calci Boss Mobs 3 (Front Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbycalcibossmobs3_db",
    Callback = function(Value)
        Environment.autotpnearbycalcibossmobs3 = Value
        if not Value then return end
        while Environment.autotpnearbycalcibossmobs3 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local originalCanCollide = rootPart.CanCollide
                    rootPart.CanCollide = false
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                    rootPart.CanCollide = originalCanCollide
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Calci" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local offsetPosition = mobHRP.Position + (mobHRP.CFrame.LookVector * 5)
                    local targetCFrame = CFrame.new(offsetPosition, mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Calci Boss Mobs 4 (Under Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbycalcibossmobs4_db",
    Callback = function(Value)
        Environment.autotpnearbycalcibossmobs4 = Value
        if not Value then return end
        while Environment.autotpnearbycalcibossmobs4 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local originalCanCollide = rootPart.CanCollide
                    rootPart.CanCollide = false
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                    rootPart.CanCollide = originalCanCollide
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Calci" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.new(mobHRP.Position - Vector3.new(0, 5, 0), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddLabel("Truffle")
Tab:AddButton({
Name = "Get Truffle Quest",
Callback = function()
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCCrimson3:WaitForChild("Head").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer").InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer"), "NPCQuest_Crimson3", 7, Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Quests"):WaitForChild("QuestNPCCrimson3")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
})
Tab:AddButton({
Name = "Get Truffle Quest 2",
Callback = function()
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCCrimson3:WaitForChild("HumanoidRootPart").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer").InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer"), "NPCQuest_Crimson3", 7, Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Quests"):WaitForChild("QuestNPCCrimson3")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
})
Tab:AddToggle({
    Name = "Auto Nearby Truffle Boss Mobs (Back Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbytrufflebossmobs_db",
    Callback = function(Value)
        Environment.autotpnearbytrufflebossmobs = Value
        if not Value then return end
        while Environment.autotpnearbytrufflebossmobs do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local originalCanCollide = rootPart.CanCollide
                    rootPart.CanCollide = false
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                    rootPart.CanCollide = originalCanCollide
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Truffle" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local offsetPosition = mobHRP.Position - (mobHRP.CFrame.LookVector * 5)
                    local targetCFrame = CFrame.new(offsetPosition, mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Truffle Boss Mobs 2 (Head Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbytrufflebossmobs2_db",
    Callback = function(Value)
        Environment.autotpnearbytrufflebossmobs2 = Value
        if not Value then return end
        while Environment.autotpnearbytrufflebossmobs2 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local originalCanCollide = rootPart.CanCollide
                    rootPart.CanCollide = false
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                    rootPart.CanCollide = originalCanCollide
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Truffle" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("Head").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    tweenTo(rootPart, nearestBossMob:WaitForChild("Head").CFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Truffle Boss Mobs 3 (Front Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbytrufflebossmobs3_db",
    Callback = function(Value)
        Environment.autotpnearbytrufflebossmobs3 = Value
        if not Value then return end
        while Environment.autotpnearbytrufflebossmobs3 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local originalCanCollide = rootPart.CanCollide
                    rootPart.CanCollide = false
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                    rootPart.CanCollide = originalCanCollide
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Truffle" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local offsetPosition = mobHRP.Position + (mobHRP.CFrame.LookVector * 5)
                    local targetCFrame = CFrame.new(offsetPosition, mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Truffle Boss Mobs 4 (Under Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbytrufflebossmobs4_db",
    Callback = function(Value)
        Environment.autotpnearbytrufflebossmobs4 = Value
        if not Value then return end
        while Environment.autotpnearbytrufflebossmobs4 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local originalCanCollide = rootPart.CanCollide
                    rootPart.CanCollide = false
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                    rootPart.CanCollide = originalCanCollide
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Truffle" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.new(mobHRP.Position - Vector3.new(0, 5, 0), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddLabel("Nemba")
Tab:AddButton({
Name = "Get Nemba Quest",
Callback = function()
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCCrimson4:WaitForChild("Head").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer").InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer"), "NPCQuest_Crimson4", 7, Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Quests"):WaitForChild("QuestNPCCrimson4")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
})
Tab:AddButton({
Name = "Get Nemba Quest 2",
Callback = function()
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCCrimson4:WaitForChild("HumanoidRootPart").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer").InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer"), "NPCQuest_Crimson4", 7, Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Quests"):WaitForChild("QuestNPCCrimson4")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
})
Tab:AddToggle({
    Name = "Auto Nearby Nemba Boss Mobs (Back Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbynembabossmobs_db",
    Callback = function(Value)
        Environment.autotpnearbynembabossmobs = Value
        if not Value then return end
        while Environment.autotpnearbynembabossmobs do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local originalCanCollide = rootPart.CanCollide
                    rootPart.CanCollide = false
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                    rootPart.CanCollide = originalCanCollide
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Nemba" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local offsetPosition = mobHRP.Position - (mobHRP.CFrame.LookVector * 5)
                    local targetCFrame = CFrame.new(offsetPosition, mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Nemba Boss Mobs 2 (Head Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbynembabossmobs2_db",
    Callback = function(Value)
        Environment.autotpnearbynembabossmobs2 = Value
        if not Value then return end
        while Environment.autotpnearbynembabossmobs2 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local originalCanCollide = rootPart.CanCollide
                    rootPart.CanCollide = false
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                    rootPart.CanCollide = originalCanCollide
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Nemba" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("Head").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    tweenTo(rootPart, nearestBossMob:WaitForChild("Head").CFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Nemba Boss Mobs 3 (Front Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbynembabossmobs3_db",
    Callback = function(Value)
        Environment.autotpnearbynembabossmobs3 = Value
        if not Value then return end
        while Environment.autotpnearbynembabossmobs3 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local originalCanCollide = rootPart.CanCollide
                    rootPart.CanCollide = false
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                    rootPart.CanCollide = originalCanCollide
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Nemba" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local offsetPosition = mobHRP.Position + (mobHRP.CFrame.LookVector * 5)
                    local targetCFrame = CFrame.new(offsetPosition, mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Nemba Boss Mobs 4 (Under Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbynembabossmobs4_db",
    Callback = function(Value)
        Environment.autotpnearbynembabossmobs4 = Value
        if not Value then return end
        while Environment.autotpnearbynembabossmobs4 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local originalCanCollide = rootPart.CanCollide
                    rootPart.CanCollide = false
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                    rootPart.CanCollide = originalCanCollide
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Nemba" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.new(mobHRP.Position - Vector3.new(0, 5, 0), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddLabel("Elmano")
Tab:AddButton({
Name = "Get Elmano Quest",
Callback = function()
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCCrimson5:WaitForChild("Head").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer").InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer"), "NPCQuest_Crimson5", 2, Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Quests"):WaitForChild("QuestNPCCrimson5")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
})
Tab:AddButton({
Name = "Get Elmano Quest 2",
Callback = function()
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCCrimson5:WaitForChild("HumanoidRootPart").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer").InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer"), "NPCQuest_Crimson5", 2, Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Quests"):WaitForChild("QuestNPCCrimson5")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
})
Tab:AddToggle({
    Name = "Auto Nearby Elmano Boss Mobs (Back Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyelmanobossmobs_db",
    Callback = function(Value)
        Environment.autotpnearbyelmanobossmobs = Value
        if not Value then return end
        while Environment.autotpnearbyelmanobossmobs do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local originalCanCollide = rootPart.CanCollide
                    rootPart.CanCollide = false
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                    rootPart.CanCollide = originalCanCollide
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Elmano" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local offsetPosition = mobHRP.Position - (mobHRP.CFrame.LookVector * 5)
                    local targetCFrame = CFrame.new(offsetPosition, mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Elmano Boss Mobs 2 (Head Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyelmanobossmobs2_db",
    Callback = function(Value)
        Environment.autotpnearbyelmanobossmobs2 = Value
        if not Value then return end
        while Environment.autotpnearbyelmanobossmobs2 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local originalCanCollide = rootPart.CanCollide
                    rootPart.CanCollide = false
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                    rootPart.CanCollide = originalCanCollide
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Elmano" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("Head").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    tweenTo(rootPart, nearestBossMob:WaitForChild("Head").CFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Elmano Boss Mobs 3 (Front Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyelmanobossmobs3_db",
    Callback = function(Value)
        Environment.autotpnearbyelmanobossmobs3 = Value
        if not Value then return end
        while Environment.autotpnearbyelmanobossmobs3 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local originalCanCollide = rootPart.CanCollide
                    rootPart.CanCollide = false
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                    rootPart.CanCollide = originalCanCollide
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Elmano" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local offsetPosition = mobHRP.Position + (mobHRP.CFrame.LookVector * 5)
                    local targetCFrame = CFrame.new(offsetPosition, mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Elmano Boss Mobs 4 (Under Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyelmanobossmobs4_db",
    Callback = function(Value)
        Environment.autotpnearbyelmanobossmobs4 = Value
        if not Value then return end
        while Environment.autotpnearbyelmanobossmobs4 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local originalCanCollide = rootPart.CanCollide
                    rootPart.CanCollide = false
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                    rootPart.CanCollide = originalCanCollide
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Elmano" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.new(mobHRP.Position - Vector3.new(0, 5, 0), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddLabel("Veggy")
Tab:AddToggle({
    Name = "Auto Nearby Veggy Boss Mobs (Back Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyveggybossmobs_db",
    Callback = function(Value)
        Environment.autotpnearbyveggybossmobs = Value
        if not Value then return end
        while Environment.autotpnearbyveggybossmobs do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local originalCanCollide = rootPart.CanCollide
                    rootPart.CanCollide = false
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                    rootPart.CanCollide = originalCanCollide
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Veggy" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local offsetPosition = mobHRP.Position - (mobHRP.CFrame.LookVector * 5)
                    local targetCFrame = CFrame.new(offsetPosition, mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Veggy Boss Mobs 2 (Head Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyveggybossmobs2_db",
    Callback = function(Value)
        Environment.autotpnearbyveggybossmobs2 = Value
        if not Value then return end
        while Environment.autotpnearbyveggybossmobs2 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local originalCanCollide = rootPart.CanCollide
                    rootPart.CanCollide = false
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                    rootPart.CanCollide = originalCanCollide
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Veggy" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("Head").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    tweenTo(rootPart, nearestBossMob:WaitForChild("Head").CFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Veggy Boss Mobs 3 (Front Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyveggybossmobs3_db",
    Callback = function(Value)
        Environment.autotpnearbyveggybossmobs3 = Value
        if not Value then return end
        while Environment.autotpnearbyveggybossmobs3 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local originalCanCollide = rootPart.CanCollide
                    rootPart.CanCollide = false
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                    rootPart.CanCollide = originalCanCollide
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Veggy" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local offsetPosition = mobHRP.Position + (mobHRP.CFrame.LookVector * 5)
                    local targetCFrame = CFrame.new(offsetPosition, mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Veggy Boss Mobs 4 (Under Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyveggybossmobs4_db",
    Callback = function(Value)
        Environment.autotpnearbyveggybossmobs4 = Value
        if not Value then return end
        while Environment.autotpnearbyveggybossmobs4 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local originalCanCollide = rootPart.CanCollide
                    rootPart.CanCollide = false
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                    rootPart.CanCollide = originalCanCollide
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Veggy" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.new(mobHRP.Position - Vector3.new(0, 5, 0), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddLabel("Selliot I, Selliot II, Selliot III")
Tab:AddToggle({
    Name = "Auto Nearby Selliot I Boss Mobs (Back Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyselliotibossmobs_db",
    Callback = function(Value)
        Environment.autotpnearbyselliotibossmobs = Value
        if not Value then return end
        while Environment.autotpnearbyselliotibossmobs do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local originalCanCollide = rootPart.CanCollide
                    rootPart.CanCollide = false
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                    rootPart.CanCollide = originalCanCollide
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Selliot I" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local offsetPosition = mobHRP.Position - (mobHRP.CFrame.LookVector * 5)
                    local targetCFrame = CFrame.new(offsetPosition, mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Selliot I Boss Mobs 2 (Head Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyselliotibossmobs2_db",
    Callback = function(Value)
        Environment.autotpnearbyselliotibossmobs2 = Value
        if not Value then return end
        while Environment.autotpnearbyselliotibossmobs2 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local originalCanCollide = rootPart.CanCollide
                    rootPart.CanCollide = false
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                    rootPart.CanCollide = originalCanCollide
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Selliot I" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("Head").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    tweenTo(rootPart, nearestBossMob:WaitForChild("Head").CFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Selliot I Boss Mobs 3 (Front Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyselliotibossmobs3_db",
    Callback = function(Value)
        Environment.autotpnearbyselliotibossmobs3 = Value
        if not Value then return end
        while Environment.autotpnearbyselliotibossmobs3 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local originalCanCollide = rootPart.CanCollide
                    rootPart.CanCollide = false
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                    rootPart.CanCollide = originalCanCollide
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Selliot I" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local offsetPosition = mobHRP.Position + (mobHRP.CFrame.LookVector * 5)
                    local targetCFrame = CFrame.new(offsetPosition, mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Selliot I Boss Mobs 4 (Under Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyselliotibossmobs4_db",
    Callback = function(Value)
        Environment.autotpnearbyselliotibossmobs4 = Value
        if not Value then return end
        while Environment.autotpnearbyselliotibossmobs4 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local originalCanCollide = rootPart.CanCollide
                    rootPart.CanCollide = false
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                    rootPart.CanCollide = originalCanCollide
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Selliot I" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.new(mobHRP.Position - Vector3.new(0, 5, 0), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Selliot II Boss Mobs (Back Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyselliotiibossmobs_db",
    Callback = function(Value)
        Environment.autotpnearbyselliotiibossmobs = Value
        if not Value then return end
        while Environment.autotpnearbyselliotiibossmobs do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local originalCanCollide = rootPart.CanCollide
                    rootPart.CanCollide = false
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                    rootPart.CanCollide = originalCanCollide
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Selliot II" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local offsetPosition = mobHRP.Position - (mobHRP.CFrame.LookVector * 5)
                    local targetCFrame = CFrame.new(offsetPosition, mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Selliot II Boss Mobs 2 (Head Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyselliotiibossmobs2_db",
    Callback = function(Value)
        Environment.autotpnearbyselliotiibossmobs2 = Value
        if not Value then return end
        while Environment.autotpnearbyselliotiibossmobs2 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local originalCanCollide = rootPart.CanCollide
                    rootPart.CanCollide = false
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                    rootPart.CanCollide = originalCanCollide
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Selliot II" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("Head").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    tweenTo(rootPart, nearestBossMob:WaitForChild("Head").CFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Selliot II Boss Mobs 3 (Front Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyselliotiibossmobs3_db",
    Callback = function(Value)
        Environment.autotpnearbyselliotiibossmobs3 = Value
        if not Value then return end
        while Environment.autotpnearbyselliotiibossmobs3 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local originalCanCollide = rootPart.CanCollide
                    rootPart.CanCollide = false
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                    rootPart.CanCollide = originalCanCollide
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Selliot II" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local offsetPosition = mobHRP.Position + (mobHRP.CFrame.LookVector * 5)
                    local targetCFrame = CFrame.new(offsetPosition, mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Selliot II Boss Mobs 4 (Under Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyselliotiibossmobs4_db",
    Callback = function(Value)
        Environment.autotpnearbyselliotiibossmobs4 = Value
        if not Value then return end
        while Environment.autotpnearbyselliotiibossmobs4 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local originalCanCollide = rootPart.CanCollide
                    rootPart.CanCollide = false
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                    rootPart.CanCollide = originalCanCollide
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Selliot II" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.new(mobHRP.Position - Vector3.new(0, 5, 0), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Selliot III Boss Mobs (Back Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyselliotiiibossmobs_db",
    Callback = function(Value)
        Environment.autotpnearbyselliotiiibossmobs = Value
        if not Value then return end
        while Environment.autotpnearbyselliotiiibossmobs do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local originalCanCollide = rootPart.CanCollide
                    rootPart.CanCollide = false
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                    rootPart.CanCollide = originalCanCollide
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Selliot III" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local offsetPosition = mobHRP.Position - (mobHRP.CFrame.LookVector * 5)
                    local targetCFrame = CFrame.new(offsetPosition, mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Selliot III Boss Mobs 2 (Head Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyselliotiiibossmobs2_db",
    Callback = function(Value)
        Environment.autotpnearbyselliotiiibossmobs2 = Value
        if not Value then return end
        while Environment.autotpnearbyselliotiiibossmobs2 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local originalCanCollide = rootPart.CanCollide
                    rootPart.CanCollide = false
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                    rootPart.CanCollide = originalCanCollide
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Selliot III" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("Head").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    tweenTo(rootPart, nearestBossMob:WaitForChild("Head").CFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Selliot III Boss Mobs 3 (Front Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyselliotiiibossmobs3_db",
    Callback = function(Value)
        Environment.autotpnearbyselliotiiibossmobs3 = Value
        if not Value then return end
        while Environment.autotpnearbyselliotiiibossmobs3 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local originalCanCollide = rootPart.CanCollide
                    rootPart.CanCollide = false
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                    rootPart.CanCollide = originalCanCollide
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Selliot III" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local offsetPosition = mobHRP.Position + (mobHRP.CFrame.LookVector * 5)
                    local targetCFrame = CFrame.new(offsetPosition, mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Selliot III Boss Mobs 4 (Under Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyselliotiiibossmobs4_db",
    Callback = function(Value)
        Environment.autotpnearbyselliotiiibossmobs4 = Value
        if not Value then return end
        while Environment.autotpnearbyselliotiiibossmobs4 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local originalCanCollide = rootPart.CanCollide
                    rootPart.CanCollide = false
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                    rootPart.CanCollide = originalCanCollide
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Selliot III" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.new(mobHRP.Position - Vector3.new(0, 5, 0), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
local Tab = Window:MakeTab({
Name = "NPCS",
Icon = "rbxassetid://4483345998",
PremiumOnly = false
})
Tab:AddButton({
Name = "Interact With Guild Quest Giver",
Callback = function()
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Main.GuildQuest:WaitForChild("Head").CFrame
task.wait(0.75)
fireproximityprompt(Workspace.Misc.NPC.Main.GuildQuest.Head.DialogAttachment.DialogPrompt)
end
})
Tab:AddButton({
Name = "Interact With Guild Quest Giver 2",
Callback = function()
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Main.GuildQuest:WaitForChild("HumanoidRootPart").CFrame
task.wait(0.75)
fireproximityprompt(Workspace.Misc.NPC.Main.GuildQuest.Head.DialogAttachment.DialogPrompt)
end
})
Tab:AddButton({
Name = "Gordo Quest",
Callback = function()
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCCrimson1:WaitForChild("Head").CFrame
end
})
Tab:AddButton({
Name = "Calci Quest",
Callback = function()
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCCrimson2:WaitForChild("Head").CFrame
end
})
Tab:AddButton({
Name = "Truffle Quest",
Callback = function()
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCCrimson3:WaitForChild("Head").CFrame
end
})
Tab:AddButton({
Name = "Nemba Quest",
Callback = function()
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCCrimson4:WaitForChild("Head").CFrame
end
})
Tab:AddButton({
Name = "Elmano Quest",
Callback = function()
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCCrimson5:WaitForChild("Head").CFrame
end
})
Tab:AddButton({
Name = "Gordo Quest 2",
Callback = function()
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCCrimson1:WaitForChild("HumanoidRootPart").CFrame
end
})
Tab:AddButton({
Name = "Calci Quest 2",
Callback = function()
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCCrimson2:WaitForChild("HumanoidRootPart").CFrame
end
})
Tab:AddButton({
Name = "Truffle Quest 2",
Callback = function()
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCCrimson3:WaitForChild("HumanoidRootPart").CFrame
end
})
Tab:AddButton({
Name = "Nemba Quest 2",
Callback = function()
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCCrimson4:WaitForChild("HumanoidRootPart").CFrame
end
})
Tab:AddButton({
Name = "Elmano Quest 2",
Callback = function()
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quests.QuestNPCCrimson5:WaitForChild("HumanoidRootPart").CFrame
end
})
local Tab = Window:MakeTab({
Name = "Teleports",
Icon = "http://www.roblox.com/asset/?id=4335489011",
PremiumOnly = false
})
local Section = Tab:AddSection({
Name = "Click the Buttons to Teleport"
})
Tab:AddButton({
Name = "Go To Quest Spawned Items",
Callback = function()
for _, v in Workspace.Misc.QuestProps:GetChildren() do
if v:IsA("Folder") then
for _, child in v:GetChildren() do
if child:IsA("Part") then
if child:FindFirstChildWhichIsA("Model") and child:FindFirstChildWhichIsA("Model").Parent.Name == child.Name then
local endTime = tick() + 2.5
while tick() < endTime do
Players.LocalPlayer.Character:FindFirstChild("HumanoidRootPart").CFrame = child.CFrame
task.wait()
end
end
end
end
end
end
end
})
Tab:AddButton({
Name = "Go To Potential Unleashed Giver Spawns (Checks All Spawn Locations!)",
Callback = function()
for _, position in {
CFrame.new(-1693.3656, 303.052032, 1924.12268, 0.90629667, 0, 0.422642082, 0, 1, 0, -0.422642082, 0, 0.90629667),
CFrame.new(-1904.1593, 309.511108, -91.1748352, -1, 0, 0, 0, 1, 0, 0, 0, -1),
CFrame.new(-1498.99414, 398.052277, -1774, 0, 0, 1, 0, 1, -0, -1, 0, 0),
CFrame.new(398.183838, 148.855316, -17.7955437, 0, 0, -1, 0, 1, 0, 1, 0, 0),
CFrame.new(759.57959, 27.5262451, -1705.27344, -1, 0, 0, 0, 1, 0, 0, 0, -1)
} do
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = position
task.wait(5)
end
end
})
Tab:AddButton({
Name = "Go To Potential Unleashed Giver (Only Works If Spawned!)",
Callback = function()
if Workspace.Misc.NPC:FindFirstChild("TechniquePotentialUnleashedGiver") and Workspace.Misc.NPC.TechniquePotentialUnleashedGiver:FindFirstChild("Head") then
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.TechniquePotentialUnleashedGiver:WaitForChild("Head").CFrame
end
end
})
Tab:AddButton({
Name = "Go To Potential Unleashed Giver 2 (Only Works If Spawned!)",
Callback = function()
if Workspace.Misc.NPC:FindFirstChild("TechniquePotentialUnleashedGiver") and Workspace.Misc.NPC.TechniquePotentialUnleashedGiver:FindFirstChild("HumanoidRootPart") then
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.TechniquePotentialUnleashedGiver:WaitForChild("HumanoidRootPart").CFrame
end
end
})
Tab:AddButton({
Name = "Go To Quest Board",
Callback = function()
Players.LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(-248.435837, 33.3595467, -15.8113546, 0.690788031, 2.12935536e-08, 0.72305733, 4.67363837e-09, 1, -3.39143895e-08, -0.72305733, 2.6806962e-08, 0.690788031)
end
})
Tab:AddButton({
Name = "World Teleporter",
Callback = function()
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = CFrame.new(-266.618866, 33.4995346, 79.0342484, -0.616952956, 3.93932638e-08, 1 / 20.78700006, 5.72333221e-08, 1, -5.18805443e-09, -0.78700006, 4.18418402e-08, -0.616952956)
end
})
local Tab = Window:MakeTab({
Name = "Worlds",
Icon = "rbxassetid://4483345998",
PremiumOnly = false
})
local Section = Tab:AddSection({
Name = "Click The Button To Be Teleported To That World"
})
Tab:AddButton({
Name = "Main World",
Callback = function()
local teleportservice = game:GetService("TeleportService")
local gameID = game.PlaceId
teleportservice:Teleport(3177438863)
end
})
Tab:AddButton({
Name = "Crimson Planet (30 Rebirths Required)",
Callback = function()
local teleportservice = game:GetService("TeleportService")
local gameID = game.PlaceId
teleportservice:Teleport(7040546583)
end
})
Tab:AddButton({
Name = "Planet Droid (150 Rebirths Required)",
Callback = function()
local teleportservice = game:GetService("TeleportService")
local gameID = game.PlaceId
teleportservice:Teleport(7888441249)
end
})
Tab:AddButton({
Name = "World Of Tournament (Script Wont Work Here!)",
Callback = function()
local teleportservice = game:GetService("TeleportService")
local gameID = game.PlaceId
teleportservice:Teleport(7379325123)
end
})
Tab:AddButton({
Name = "Battlegrounds (Script Wont Work Here!)",
Callback = function()
local teleportservice = game:GetService("TeleportService")
local gameID = game.PlaceId
teleportservice:Teleport(14516089806)
end
})
Tab:AddButton({
Name = "AFK World (Script Wont Work Here!)",
Callback = function()
local teleportservice = game:GetService("TeleportService")
local gameID = game.PlaceId
teleportservice:Teleport(18501029209)
end
})
Tab:AddButton({
Name = "Trade Hub (Script Wont Work Here!)",
Callback = function()
local teleportservice = game:GetService("TeleportService")
local gameID = game.PlaceId
teleportservice:Teleport(18408422178)
end
})
local Tab = Window:MakeTab({
Name = "Anti's",
Icon = "http://www.roblox.com/asset/?id=7734056608",
PremiumOnly = false
})
Tab:AddToggle({
Name = "Anti Admins (do task.wait()t use with anti kick!)",
Default = false,
Save = true,
Flag = "antiadmins_db",
Callback = function(Value)
Environment.AntiAdmins = Value
while Environment.AntiAdmins and task.wait() do
local AdminNames = {
"Jirenato",
"ReignImperatus",
"Zephyrus_Yi",
"ratedroeh",
"Natt_P5",
"O_ORui",
"BexDragoon",
"pokemonhaden",
"XADED4971",
"AltForTGBN",
"notrmdx",
"Npinoy"
}
for _, player in Players:GetPlayers() do
if table.find(AdminNames, player.Name) then
Players.LocalPlayer:Kick("Holy shit admin detected! You almost got banned there bud!")
end
end
task.wait(0.1)
end
end
})
Tab:AddButton({
    Name = "Anti Cheat Bypass (Bypass Anti Exploit Manager!)",
    Callback = function()
        local mt = getrawmetatable(game)
        setreadonly(mt, false)
        local oldNamecall = mt.__namecall
        mt.__namecall = newcclosure(function(self, ...)
            local args = {...}
            local method = getnamecallmethod()
            if method == "Kick" or method == "Destroy" then
                warn("[Bypass] Anti-cheat tried to kick you!")
                return false
            end
            return oldNamecall(self, unpack(args))
        end)
        local RunService = game:GetService("RunService")
        local Players = game:GetService("Players")
        local StarterGui = game:GetService("StarterGui")
        local HeartbeatConnection
        if HeartbeatConnection then HeartbeatConnection:Disconnect() end
        HeartbeatConnection = RunService.Heartbeat:Connect(function()
            sethiddenproperty(Players.LocalPlayer, "SimulationRadius", math.huge)
        end)
        local ReplicatedStorage = game:GetService("ReplicatedStorage")
        local BlockedRemotes = {"AntiCheatEvent1", "AntiCheatEvent2"}
        local oldFireServer = Instance.new("RemoteEvent").FireServer
        hookfunction(oldFireServer, function(self, ...)
            if table.find(BlockedRemotes, self.Name) then
                warn("[Bypass] Blocked remote:", self.Name)
                return false
            end
            return oldFireServer(self, ...)
        end)
        local function SpoofPlayerData()
            local Player = Players.LocalPlayer
            while task.wait(0.75) do
                if Player and Player.Character then
                    local Humanoid = Player.Character:FindFirstChildOfClass("Humanoid")
                    if Humanoid then
                        Humanoid.WalkSpeed = 16
                        Humanoid.JumpPower = 50
                    end
                end
            end
        end
        task.spawn(SpoofPlayerData)
        StarterGui:SetCore("SendNotification", {
            Title = "Bypass Active",
            Text = "Anti-Cheat successfully bypassed.",
            Duration = 5
        })
    end
})
Tab:AddToggle({
Name = "Anti Kick",
Default = false,
Save = true,
Flag = "antikick_db",
Callback = function(Value)
Environment.Anti = Value
if Value then
local Anti
Anti = hookmetamethod(game, "__namecall", function(self, ...)
if self == Players.LocalPlayer and getnamecallmethod():lower() == "kick" and Environment.Anti then
return warn("[ANTI-KICK] Client Tried To Call Kick Function On LocalPlayer")
end
return Anti(self, ...)
end)
else
if Anti then
Environment.Anti = false
hookmetamethod(game, "__namecall", Anti)
Anti = false
end
end
end
})
Tab:AddToggle({
Name = "Anti Destroy",
Default = false,
Save = true,
Flag = "antidestroy_db",
Callback = function(Value)
Environment.Anti = Value
if Value then
local Anti
Anti = hookmetamethod(game, "__namecall", function(self, ...)
if self == Players.LocalPlayer and getnamecallmethod():lower() == "destroy" and Environment.Anti then
return warn("[ANTI-DESTROY] Client Tried To Call Destroy Function On LocalPlayer")
end
return Anti(self, ...)
end)
else
if Anti then
Environment.Anti = false
hookmetamethod(game, "__namecall", Anti)
Anti = false
end
end
end
})
Tab:AddToggle({
Name = "Anti Afk",
Default = false,
Save = true,
Flag = "antiafk_db",
Callback = function(Value)
if Value then
if getconnections then
for _, v in getconnections(Players.LocalPlayer.Idled) do
if v["Disable"] then
v["Disable"](v)
elseif v["Disconnect"] then
v["Disconnect"](v)
end
end
else
local VirtualUser = game:GetService("VirtualUser")
Players.LocalPlayer.Idled:Connect(function()
VirtualUser:CaptureController()
VirtualUser:ClickButton2(Vector2.new())
end)
end
else
end
end
})
local Tab = Window:MakeTab({
Name = "Spawn Items",
Icon = "http://www.roblox.com/asset/?id=7733674079",
PremiumOnly = false
})
Tab:AddButton({
Name = "Grab Relics",
Callback = function()
for _, position in {
CFrame.new(-2273.83179, 28.2519531, -2429.64062, 1, 0, 0, 0, 1, 0, 0, 0, 1),
CFrame.new(820.891541, 26.2505569, 1990.21655, 1, 0, 0, 0, 1, 0, 0, 0, 1),
CFrame.new(253.534485, 130.25, -770.795044, 0.955216229, 0, 0.295908749, 0, 1, 0, -0.295908749, 0, 0.955216229),
CFrame.new(-2107.59253, 26.2521133, 497.246094, 1, 0, 0, 0, 1, 0, 0, 0, 1),
CFrame.new(446.697571, 26.2511673, 736.373047, 0.881064892, 0, 0.472995371, 0, 1, 0, -0.472995371, 0, 0.881064892),
CFrame.new(-355.540985, 26.2507324, -2043.2832, 1, 0, 0, 0, 1, 0, 0, 0, 1)
} do
local endTime = tick() + 4.75
while tick() < endTime do
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = position
for _, part in Workspace:WaitForChild("PartStorage"):GetDescendants() do
if part:IsA("BasePart") then
for _, v in part:GetDescendants() do
if v:IsA("ProximityPrompt") and v.ObjectText and v.ObjectText:find("Token") then
firesignal(v.Triggered)
end
end
end
end
task.wait()
end
end
end
})
Tab:AddButton({
Name = "Grab Relics 2 (Remotes!)",
Callback = function()
for _, position in {
CFrame.new(-2273.83179, 28.2519531, -2429.64062, 1, 0, 0, 0, 1, 0, 0, 0, 1),
CFrame.new(820.891541, 26.2505569, 1990.21655, 1, 0, 0, 0, 1, 0, 0, 0, 1),
CFrame.new(253.534485, 130.25, -770.795044, 0.955216229, 0, 0.295908749, 0, 1, 0, -0.295908749, 0, 0.955216229),
CFrame.new(-2107.59253, 26.2521133, 497.246094, 1, 0, 0, 0, 1, 0, 0, 0, 1),
CFrame.new(446.697571, 26.2511673, 736.373047, 0.881064892, 0, 0.472995371, 0, 1, 0, -0.472995371, 0, 0.881064892),
CFrame.new(-355.540985, 26.2507324, -2043.2832, 1, 0, 0, 0, 1, 0, 0, 0, 1)
} do
local endTime = tick() + 4.75
while tick() < endTime do
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = position
for _, v in Workspace.PartStorage:GetChildren() do
if string.match(v.Name, "^ItemDrop_%d+$") then
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("ItemDropService"):WaitForChild("RF"):WaitForChild("ClaimItem").InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("ItemDropService"):WaitForChild("RF"):WaitForChild("ClaimItem"), string.match(v.Name, "%d+"), "StandardWish"}))
end
end
task.wait()
end
end
end
})
Tab:AddButton({
Name = "Teleport To Relics",
Callback = function()
for _, position in {
CFrame.new(-2273.83179, 28.2519531, -2429.64062, 1, 0, 0, 0, 1, 0, 0, 0, 1),
CFrame.new(820.891541, 26.2505569, 1990.21655, 1, 0, 0, 0, 1, 0, 0, 0, 1),
CFrame.new(253.534485, 130.25, -770.795044, 0.955216229, 0, 0.295908749, 0, 1, 0, -0.295908749, 0, 0.955216229),
CFrame.new(-2107.59253, 26.2521133, 497.246094, 1, 0, 0, 0, 1, 0, 0, 0, 1),
CFrame.new(446.697571, 26.2511673, 736.373047, 0.881064892, 0, 0.472995371, 0, 1, 0, -0.472995371, 0, 0.881064892),
CFrame.new(-355.540985, 26.2507324, -2043.2832, 1, 0, 0, 0, 1, 0, 0, 0, 1)
} do
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = position
task.wait(5)
end
end
})
Tab:AddButton({
Name = "Teleport To Relics 2 (Stay At Current Location!)",
Callback = function()
for _, position in {
CFrame.new(-2273.83179, 28.2519531, -2429.64062, 1, 0, 0, 0, 1, 0, 0, 0, 1),
CFrame.new(820.891541, 26.2505569, 1990.21655, 1, 0, 0, 0, 1, 0, 0, 0, 1),
CFrame.new(253.534485, 130.25, -770.795044, 0.955216229, 0, 0.295908749, 0, 1, 0, -0.295908749, 0, 0.955216229),
CFrame.new(-2107.59253, 26.2521133, 497.246094, 1, 0, 0, 0, 1, 0, 0, 0, 1),
CFrame.new(446.697571, 26.2511673, 736.373047, 0.881064892, 0, 0.472995371, 0, 1, 0, -0.472995371, 0, 0.881064892),
CFrame.new(-355.540985, 26.2507324, -2043.2832, 1, 0, 0, 0, 1, 0, 0, 0, 1)
} do
local endTime = tick() + 5
while tick() < endTime do
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = position
task.wait(1.25)
end
end
end
})
Tab:AddToggle({
Name = "Auto Teleport To Relics",
Default = false,
Save = true,
Flag = "autorelics_db",
Callback = function(Value)
Environment.AutoRelics = Value
while Environment.AutoRelics and task.wait() do
for _, position in {
CFrame.new(-2273.83179, 28.2519531, -2429.64062, 1, 0, 0, 0, 1, 0, 0, 0, 1),
CFrame.new(820.891541, 26.2505569, 1990.21655, 1, 0, 0, 0, 1, 0, 0, 0, 1),
CFrame.new(253.534485, 130.25, -770.795044, 0.955216229, 0, 0.295908749, 0, 1, 0, -0.295908749, 0, 0.955216229),
CFrame.new(-2107.59253, 26.2521133, 497.246094, 1, 0, 0, 0, 1, 0, 0, 0, 1),
CFrame.new(446.697571, 26.2511673, 736.373047, 0.881064892, 0, 0.472995371, 0, 1, 0, -0.472995371, 0, 0.881064892),
CFrame.new(-355.540985, 26.2507324, -2043.2832, 1, 0, 0, 0, 1, 0, 0, 0, 1)
} do
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = position
task.wait(5)
end
end
end
})
Tab:AddToggle({
Name = "Auto Teleport To Relics 2 (Stay At Current Location!)",
Default = false,
Save = true,
Flag = "autorelics_db",
Callback = function(Value)
Environment.AutoRelics = Value
while Environment.AutoRelics and task.wait() do
for _, position in {
CFrame.new(-2273.83179, 28.2519531, -2429.64062, 1, 0, 0, 0, 1, 0, 0, 0, 1),
CFrame.new(820.891541, 26.2505569, 1990.21655, 1, 0, 0, 0, 1, 0, 0, 0, 1),
CFrame.new(253.534485, 130.25, -770.795044, 0.955216229, 0, 0.295908749, 0, 1, 0, -0.295908749, 0, 0.955216229),
CFrame.new(-2107.59253, 26.2521133, 497.246094, 1, 0, 0, 0, 1, 0, 0, 0, 1),
CFrame.new(446.697571, 26.2511673, 736.373047, 0.881064892, 0, 0.472995371, 0, 1, 0, -0.472995371, 0, 0.881064892),
CFrame.new(-355.540985, 26.2507324, -2043.2832, 1, 0, 0, 0, 1, 0, 0, 0, 1)
} do
local endTime = tick() + 5
while tick() < endTime do
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = position
task.wait(0.1)
end
end
end
end
})
Tab:AddToggle({
Name = "Auto Relics (Must Be Near Standard Wish Tokens!)",
Default = false,
Save = true,
Flag = "autotokens_db",
Callback = function(Value)
Environment.AutoTokens = Value
while Environment.AutoTokens and task.wait() do
for _, part in Workspace:WaitForChild("PartStorage"):GetDescendants() do
if part:IsA("BasePart") then
for _, v in part:GetDescendants() do
if v:IsA("ProximityPrompt") and v.ObjectText and v.ObjectText:find("Token") then
if Players.LocalPlayer.Character and Players.LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = part.CFrame + Vector3.new(0, 5, 0)
task.wait(0.75)
fireproximityprompt(v, 1)
task.wait(2)
end
break
end
end
end
end
end
end
})
Tab:AddToggle({
Name = "Auto Orbs (FP Must Be Near Orbs!)",
Default = false,
Save = true,
Flag = "autoorbs_db",
Callback = function(Value)
Environment.AutoOrbs = Value
while Environment.AutoOrbs and task.wait() do
for _, part in Workspace:WaitForChild("PartStorage"):GetDescendants() do
if part:IsA("BasePart") then
for _, v in part:GetDescendants() do
if v:IsA("ProximityPrompt") and v.ObjectText and v.ObjectText:find("Orb") then
if Players.LocalPlayer.Character and Players.LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = part.CFrame + Vector3.new(0, 5, 0)
task.wait(0.75)
fireproximityprompt(v, 1)
task.wait(2)
end
break
end
end
end
end
end
end
})
Tab:AddToggle({
Name = "Auto Orbs 2 (FS Must Be Near Orbs!)",
Default = false,
Save = true,
Flag = "autoorbs2_db",
Callback = function(Value)
Environment.AutoOrbs2 = Value
while Environment.AutoOrbs2 and task.wait() do
for _, part in Workspace:WaitForChild("PartStorage"):GetDescendants() do
if part:IsA("BasePart") then
for _, v in part:GetDescendants() do
if v:IsA("ProximityPrompt") and v.ObjectText and v.ObjectText:find("Orb") then
if Players.LocalPlayer.Character and Players.LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = part.CFrame + Vector3.new(0, 5, 0)
task.wait(0.75)
firesignal(v.Triggered)
task.wait(2)
end
break
end
end
end
end
end
end
})
Tab:AddToggle({
Name = "Auto Nearby Interact",
Default = false,
Save = true,
Flag = "autonearbyinteract_db",
Callback = function(Value)
Environment.AutoNearbyInteract = Value
while Environment.AutoNearbyInteract and task.wait() do
function fire()
if Players.LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
for _, v in Workspace:GetDescendants() do
if v:IsA("ProximityPrompt") then
if ((Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").Position - v.Parent.Position).Magnitude <= 10) then
fireproximityprompt(v)
end
end
end
end
end
fire()
end
end
})
Tab:AddToggle({
Name = "Auto Nearby Interact 2 (Maybe Use 1!)",
Default = false,
Save = true,
Flag = "autonearbyinteract_db",
Callback = function(Value)
Environment.AutoNearbyInteract = Value
while Environment.AutoNearbyInteract and task.wait() do
function fire()
if Players.LocalPlayer.Character:FindFirstChild("Head") then
for _, v in Workspace:GetDescendants() do
if v:IsA("ProximityPrompt") then
if ((Players.LocalPlayer.Character:WaitForChild("Head").Position - v.Parent.Position).Magnitude <= 10) then
fireproximityprompt(v)
end
end
end
end
end
fire()
end
end
})
Tab:AddButton({
Name = "Instant Nearby Interact",
Callback = function()
function fire()
if Players.LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
for _, v in Workspace:GetDescendants() do
if v:IsA("ProximityPrompt") then
if ((Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").Position - v.Parent.Position).Magnitude <= 10) then
fireproximityprompt(v)
end
end
end
end
end
fire()
end
})
Tab:AddButton({
Name = "Instant Nearby Interact 2 (Maybe use 1!)",
Callback = function()
function fire()
if Players.LocalPlayer.Character:FindFirstChild("Head") then
for _, v in Workspace:GetDescendants() do
if v:IsA("ProximityPrompt") then
if ((Players.LocalPlayer.Character:WaitForChild("Head").Position - v.Parent.Position).Magnitude <= 10) then
fireproximityprompt(v)
end
end
end
end
end
fire()
end
})
Tab:AddButton({
Name = "Wish Standard (Need Standard Tokens!)",
Callback = function()
ReplicatedStorage.Packages._Index:FindFirstChild("sleitnick_knit@1.4.7").knit.Services.WishService.RF.requestWishFromBanner.InvokeServer(unpack({ReplicatedStorage.Packages._Index:FindFirstChild("sleitnick_knit@1.4.7").knit.Services.WishService.RF.requestWishFromBanner, "Standard", 1, 1}))
end
})
Tab:AddButton({
Name = "Wish Standard (Need Premium Tokens!)",
Callback = function()
ReplicatedStorage.Packages._Index:FindFirstChild("sleitnick_knit@1.4.7").knit.Services.WishService.RF.requestWishFromBanner.InvokeServer(unpack({ReplicatedStorage.Packages._Index:FindFirstChild("sleitnick_knit@1.4.7").knit.Services.WishService.RF.requestWishFromBanner, "Standard", 2, 1}))
end
})
Tab:AddButton({
Name = "Wish Premium (Need Premium Tokens!)",
Callback = function()
ReplicatedStorage.Packages._Index:FindFirstChild("sleitnick_knit@1.4.7").knit.Services.WishService.RF.requestWishFromBanner.InvokeServer(unpack({ReplicatedStorage.Packages._Index:FindFirstChild("sleitnick_knit@1.4.7").knit.Services.WishService.RF.requestWishFromBanner, "Premium", 1, 1}))
end
})
Tab:AddButton({
Name = "Buy Premium Wish Standard (Need 50 Gold!)",
Callback = function()
ReplicatedStorage.Packages._Index:FindFirstChild("sleitnick_knit@1.4.7").knit.Services.WishService.RF.buyFromShop.InvokeServer(unpack({ReplicatedStorage.Packages._Index:FindFirstChild("sleitnick_knit@1.4.7").knit.Services.WishService.RF.buyFromShop, "Standard", 5}))
end
})
local Tab = Window:MakeTab({
Name = "Codes/Trades",
Icon = "rbxassetid://4483345998",
PremiumOnly = false,
})
Tab:AddLabel("I Have Added A Codes Channel For Users!")
Tab:AddLabel("Please Know That These Codes May Expire At Any Time!")
Tab:AddLabel("I Will Update This Whenever A New One Comes Out!")
Tab:AddParagraph("The Current Codes Is MOREFREECODES, TENFREEWISHES, PREMIUMSGALORE")
Tab:AddTextbox({
Name = "Codes (Redeem!)",
Default = "",
TextDisappear = false,
Callback = function(Value)
local args = {
[1] = {
["UniqueTag"] = "RedeemCode",
["InputText"] = Value,
["InputType"] = "Text",
["LeftButton"] = "Redeem",
["Description"] = "Redeem Code",
["Prompt"] = "RedeemCode",
["RightButton"] = "Cancel",
["Title"] = "Codes"
},
[2] = "Redeem"
}
ReplicatedStorage.Packages._Index:FindFirstChild("sleitnick_knit@1.4.7").knit.Services.PromptService.RE.Prompt.FireServer(unpack({ReplicatedStorage.Packages._Index:FindFirstChild("sleitnick_knit@1.4.7").knit.Services.PromptService.RE.Prompt, unpack(args)}))
end
})
Tab:AddTextbox({
Name = "Trades (Enter Player Username Here!)",
Default = "",
TextDisappear = false,
Callback = function(Value)
local player = Players:FindFirstChild(Value)
if player then
OrionLib:MakeNotification({
Name = "Player Found!",
Content = "Sending Trade!",
Image = "rbxassetid://4483345998",
Time = 5
})
local args = {
[1] = player
}
ReplicatedStorage.Packages._Index:FindFirstChild("sleitnick_knit@1.4.7").knit.Services.TradeService.RE.RequestTrade.FireServer(unpack({ReplicatedStorage.Packages._Index:FindFirstChild("sleitnick_knit@1.4.7").knit.Services.TradeService.RE.RequestTrade, unpack(args)}))
else
OrionLib:MakeNotification({
Name = "Player Not Found!",
Content = "That Player Does Not Exist. Please Try Again!",
Image = "rbxassetid://4483345998",
Time = 5
})
end
end
})
local Tab = Window:MakeTab({
Name = "Credits",
Icon = "http://www.roblox.com/asset/?id=4370318685",
PremiumOnly = false
})
Tab:AddLabel("Script is made by Hydrazia")
Tab:AddParagraph("Info", "Tap The Button To Copy My Social And Invite Links!")
Tab:AddParagraph("Info", "Script changelogs at our Telegram Channel. If you find any bugs, inform me.")
Tab:AddButton({
Name = "Youtube",
Callback = function()
setclipboard("https://m.youtube.com/channel/UCa1xzPElap7CUTGtj0QhdwA")
end
})
Tab:AddButton({
Name = "Scriptblox",
Callback = function()
setclipboard("https://scriptblox.com/u/Riotscripter")
end
})
Tab:AddButton({
Name = "Telegram Channel",
Callback = function()
setclipboard("https://t.me/Riotscripter")
end
})
Tab:AddBind({
Name = "UI Toggle Keybind",
Default = Enum.KeyCode.RightShift,
Hold = false,
Callback = function()
print("press")
end
})
OrionLib:Init()
elseif game.PlaceId == 7888441249 then -- World 3
if not game:IsLoaded() then
game.Loaded:Wait()
end
local OrionLib = loadstring(game:HttpGet("https://raw.githubusercontent.com/Hydrazia/Exploits/refs/heads/main/ORION"))()
OrionLib:MakeNotification({
Name = "Dragon Blox",
Content = "Scripted By Riotscripter The King Of Lua",
Image = "rbxassetid://4483345998",
Time = 5
})
local Window = OrionLib:MakeWindow({
Name = "RS | DB",
HidePremium = false,
SaveConfig = false,
ConfigFolder = "Dragon Blox",
IntroEnabled = true,
IntroText = "Dragon Blox Gui loaded world 3"
})
-- local mytable
local MyTable = {
["AutoPunch"] = false,
["AutoPunch2"] = false,
["AutoPunch3"] = false,
["AutoPunch4"] = false,
["AutoPunch5"] = false,
["AutoBlock"] = false,
["AutoAgility"] = false,
["AutoEnergy"] = false,
["AutoEnergyBall"] = false,
["AutoEnergyBall2"] = false,
["AutoTechnique"] = false,
["AutoTechnique2"] = false,
["AutoRebirth"] = false,
["AutoRebirth2"] = false,
["AutoRebirth3"] = false,
["AutoSpin"] = false,
["AutoDrops"] = false,
["AutoDrops2"] = false,
["AutoDrops3"] = false,
["AutoDrops4"] = false,
["AutoDrops5"] = false,
}
-- Bool Values
Environment.AutoPunch = false
Environment.AutoPunch2 = false
Environment.AutoPunch3 = false
Environment.AutoPunch4 = false
Environment.AutoPunch5 = false
Environment.AutoBlock = false
Environment.AutoAgility = false
Environment.AutoEnergy = false
Environment.AutoEnergyBall = false
Environment.AutoEnergyBall2 = false
Environment.AutoTechnique = false
Environment.AutoTechnique2 = false
Environment.AutoRebirth = false
Environment.AutoRebirth2 = false
Environment.AutoRebirth3 = false
Environment.AutoSpin = false
Environment.AutoDrops = false
Environment.AutoDrops2 = false
Environment.AutoDrops3 = false
Environment.AutoDrops4 = false
Environment.AutoDrops5 = false
-- locals
local Players = cloneref(game:GetService("Players"))
local TweenService = cloneref(game:GetService("TweenService"))
local Workspace = cloneref(game:GetService("Workspace"))
local ReplicatedStorage = cloneref(game:GetService("ReplicatedStorage"))
local VirtualInputManager = cloneref(game:GetService("VirtualInputManager"))
local TeleportService = cloneref(game:GetService("TeleportService"))
local RunService = cloneref(game:GetService("RunService"))
local StarterGui = cloneref(game:GetService("StarterGui"))
-- functions
local function autopunch()
    while Environment.AutoPunch do task.wait()
        ReplicatedStorage:WaitForChild("Remotes"):WaitForChild("SkillRemote").FireServer(unpack({ReplicatedStorage:WaitForChild("Remotes"):WaitForChild("SkillRemote"), {SkillId = "1", Began = true, ["Typ\208\181"] = 1}}))
    end
end
local function autopunch2()
    while Environment.AutoPunch2 do task.wait()
        require(ReplicatedStorage.Packages.Knit).GetController("SkillManager").TapInput(unpack({{SkillId = "1", Began = true}}))
    end
end
local function autopunch3()
    while Environment.AutoPunch3 do task.wait()
        require(ReplicatedStorage.Packages._Index["sleitnick_knit@1.4.7"].knit).GetController("SkillManager").TapInput(unpack({{SkillId = "1", Began = true}}))
    end
end
local function autopunch4()
    while Environment.AutoPunch4 do task.wait()
        require(ReplicatedStorage.Packages.Knit).GetController("SkillManager"):GetSkillRemote().FireServer(unpack({require(ReplicatedStorage.Packages.Knit).GetController("SkillManager"):GetSkillRemote(), {SkillId = "1", Began = true, ["Typ\208\181"] = 1}}))
    end
end
local function autopunch5()
    while Environment.AutoPunch5 do task.wait()
        require(ReplicatedStorage.Packages._Index["sleitnick_knit@1.4.7"].knit).GetController("SkillManager"):GetSkillRemote().FireServer(unpack({require(ReplicatedStorage.Packages._Index["sleitnick_knit@1.4.7"].knit).GetController("SkillManager"):GetSkillRemote(), {SkillId = "1", Began = true, ["Typ\208\181"] = 1}}))
    end
end
local function autoblock()
    while Environment.AutoBlock do
        VirtualInputManager:SendKeyEvent(true, Enum.KeyCode.B, false, game)
        task.wait()
    end
    VirtualInputManager:SendKeyEvent(false, Enum.KeyCode.B, false, game)
end
local function autoagility()
    while Environment.AutoAgility do task.wait()
        Players.LocalPlayer.Character.Humanoid:Move(Vector3.new(0, 0, -1), true)
    end
end
local function autoenergy()
    while Environment.AutoEnergy do
        VirtualInputManager:SendKeyEvent(true, Enum.KeyCode.C, false, game)
        task.wait()
    end
    VirtualInputManager:SendKeyEvent(false, Enum.KeyCode.C, false, game)
end
local function autoenergyball()
    while Environment.AutoEnergyBall do task.wait()
        require(ReplicatedStorage.Packages.Knit).GetController("SkillManager").TapInput(unpack({{SkillId = "101", Began = true}}))
    end
end
local function autoenergyball2()
    while Environment.AutoEnergyBall2 do task.wait()
        require(ReplicatedStorage.Packages._Index["sleitnick_knit@1.4.7"].knit).GetController("SkillManager").TapInput(unpack({{SkillId = "101", Began = true}}))
    end
end
local function autotechnique()
    while Environment.AutoTechnique do task.wait()
        require(ReplicatedStorage.Packages.Knit).GetController("SkillManager").TapInput(unpack({{SkillId = "8", Began = true}}))
    end
end
local function autotechnique2()
    while Environment.AutoTechnique2 do task.wait()
        require(ReplicatedStorage.Packages._Index["sleitnick_knit@1.4.7"].knit).GetController("SkillManager").TapInput(unpack({{SkillId = "8", Began = true}}))
    end
end
local function autorebirth()
    while Environment.AutoRebirth do task.wait()
        for _, rb in Players.LocalPlayer.PlayerGui.MainGui.Frame.Stats.AspectRatioFrame.StatHUD.Frame:GetChildren() do
            if rb:FindFirstChild("RebirthButton") and rb:FindFirstChild("RebirthButton").BackgroundTransparency ~= 1 then
                ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("PlayerLevelService"):WaitForChild("RF"):WaitForChild("RequestRebirth").InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("PlayerLevelService"):WaitForChild("RF"):WaitForChild("RequestRebirth"), true}))
            end
        end
    end
end
local function autorebirth2()
    while Environment.AutoRebirth2 do task.wait()
        for _, rb in Players.LocalPlayer.PlayerGui.MainGui.Frame.Stats.AspectRatioFrame.StatHUD.Frame:GetChildren() do
            if rb:FindFirstChild("RebirthButton") and rb:FindFirstChild("RebirthButton").BackgroundTransparency ~= 1 then
                require(ReplicatedStorage.Packages.Knit).GetService("PlayerLevelService"):RequestRebirth(true)
            end
        end
    end
end
local function autorebirth3()
    while Environment.AutoRebirth3 do task.wait()
        for _, rb in Players.LocalPlayer.PlayerGui.MainGui.Frame.Stats.AspectRatioFrame.StatHUD.Frame:GetChildren() do
            if rb:FindFirstChild("RebirthButton") and rb:FindFirstChild("RebirthButton").BackgroundTransparency ~= 1 then
                require(ReplicatedStorage.Packages._Index["sleitnick_knit@1.4.7"].knit).GetService("PlayerLevelService"):RequestRebirth(true)
            end
        end
    end
end
local function autodrops()
    while Environment.AutoDrops do
        task.wait()
        for _, v in ReplicatedStorage.Data.ItemDrops:GetChildren() do
            if v:GetAttribute("UserId") == Players.LocalPlayer.UserId and v:GetAttribute("ItemName") then
                local ops = Players.LocalPlayer.Character.HumanoidRootPart.Position
                local pos = v:GetAttribute("Pos")
                Players.LocalPlayer.Character:MoveTo(pos)
                task.wait(0.5)
                require(ReplicatedStorage.Packages.Knit).GetController("ItemDropController"):ClaimItem(v.Name, require(ReplicatedStorage.Packages.Knit).GetController("InventoryController"):GetItem(v:GetAttribute("ItemName")))
                task.wait(0.5)
                Players.LocalPlayer.Character:MoveTo(ops)
            end
        end
    end
end
local function autodrops2()
    while Environment.AutoDrops2 do
        task.wait()
        for _, v in ReplicatedStorage.Data.ItemDrops:GetChildren() do
            if v:GetAttribute("UserId") == Players.LocalPlayer.UserId and v:GetAttribute("ItemName") then
                local ops = Players.LocalPlayer.Character.HumanoidRootPart.Position
                local pos = v:GetAttribute("Pos")
                Players.LocalPlayer.Character:MoveTo(pos)
                task.wait(0.5)
                require(ReplicatedStorage.Packages._Index["sleitnick_knit@1.4.7"].knit).GetController("ItemDropController"):ClaimItem(v.Name, require(ReplicatedStorage.Packages._Index["sleitnick_knit@1.4.7"].knit).GetController("InventoryController"):GetItem(v:GetAttribute("ItemName")))
                task.wait(0.5)
                Players.LocalPlayer.Character:MoveTo(ops)
            end
        end
    end
end
local function autodrops3()
    while Environment.AutoDrops3 do
        task.wait()
        for _, v in ReplicatedStorage.Data.ItemDrops:GetChildren() do
            if v:GetAttribute("UserId") == Players.LocalPlayer.UserId and v:GetAttribute("ItemName") then
                local ops = Players.LocalPlayer.Character.HumanoidRootPart.Position
                local pos = v:GetAttribute("Pos")
                Players.LocalPlayer.Character:MoveTo(pos)
                task.wait(0.5)
                ReplicatedStorage.Packages._Index["sleitnick_knit@1.4.7"].knit.Services.ItemDropService.RF.ClaimItem.InvokeServer(unpack({ReplicatedStorage.Packages._Index["sleitnick_knit@1.4.7"].knit.Services.ItemDropService.RF.ClaimItem, v.Name, v:GetAttribute("ItemName")}))
                task.wait(0.5)
                Players.LocalPlayer.Character:MoveTo(ops)
            end
        end
    end
end
local function autodrops4()
    while Environment.AutoDrops4 do
        task.wait()
        for _, v in ReplicatedStorage.Data.ItemDrops:GetChildren() do
            if v:GetAttribute("UserId") == Players.LocalPlayer.UserId then
                local ops = Players.LocalPlayer.Character.HumanoidRootPart.Position
                Players.LocalPlayer.Character:MoveTo(v:GetAttribute("Pos"))
                task.wait(0.5)
                local drop = Workspace:FindFirstChild("PartStorage"):FindFirstChild("ItemDrop_"..v.Name)
                if drop and drop.Base and drop.Base:FindFirstChildOfClass("ProximityPrompt") then
                    fireproximityprompt(drop.Base.ProximityPrompt)
                end
                task.wait(0.5)
                Players.LocalPlayer.Character:MoveTo(ops)
            end
        end
    end
end
local function autodrops5()
    while Environment.AutoDrops5 do task.wait()
        for _, v in ReplicatedStorage.Data.ItemDrops:GetChildren() do
            if v:GetAttribute("UserId") == Players.LocalPlayer.UserId then
                local ops = Players.LocalPlayer.Character.HumanoidRootPart.Position
                Players.LocalPlayer.Character:MoveTo(v:GetAttribute("Pos"))
                task.wait(0.5)
                local drop = Workspace:FindFirstChild("PartStorage"):FindFirstChild("ItemDrop_"..v.Name)
                if drop and drop.Base and drop.Base:FindFirstChildOfClass("ProximityPrompt") then
                    firesignal(drop.Base.ProximityPrompt.Triggered)
                end
                task.wait(0.5)
                Players.LocalPlayer.Character:MoveTo(ops)
            end
        end
    end
end
local function autospin()
    while Environment.AutoSpin do task.wait()
        ReplicatedStorage.Packages._Index:FindFirstChild("sleitnick_knit@1.4.7").knit.Services.LootPoolService.RF.SpinRequest.InvokeServer(unpack({ReplicatedStorage.Packages._Index:FindFirstChild("sleitnick_knit@1.4.7").knit.Services.LootPoolService.RF.SpinRequest, "SpinMain"}))
    end
end
local Tab = Window:MakeTab({
Name = "AutoFarm",
Icon = "rbxassetid://4483345998",
PremiumOnly = false
})
local Section = Tab:AddSection({
Name = "Autofarm Stuff (Rejoin to Undo)"
})
Tab:AddToggle({
Name = "Auto Punch (Universal)",
Default = false,
Save = true,
Flag = "autopunch_db",
Callback = function(Value)
Environment.AutoPunch = Value
autopunch()
end
})
Tab:AddToggle({
Name = "Auto Punch 2 (Universal)",
Default = false,
Save = true,
Flag = "autopunch2_db",
Callback = function(Value)
Environment.AutoPunch2 = Value
autopunch2()
end
})
Tab:AddToggle({
Name = "Auto Punch 3 (Universal)",
Default = false,
Save = true,
Flag = "autopunch3_db",
Callback = function(Value)
Environment.AutoPunch3 = Value
autopunch3()
end
})
Tab:AddToggle({
Name = "Auto Punch 4 (Universal)",
Default = false,
Save = true,
Flag = "autopunch4_db",
Callback = function(Value)
Environment.AutoPunch4 = Value
autopunch4()
end
})
Tab:AddToggle({
Name = "Auto Punch 5 (Universal)",
Default = false,
Save = true,
Flag = "autopunch5_db",
Callback = function(Value)
Environment.AutoPunch5 = Value
autopunch5()
end
})
Tab:AddToggle({
Name = "Auto Energy (Charge Energy!)",
Default = false,
Save = true,
Flag = "autoenergy_db",
Callback = function(Value)
Environment.AutoEnergy = Value
autoenergy()
end
})
Tab:AddToggle({
Name = "Auto Energy (Charge Energy At 10%!)",
Default = false,
Save = true,
Flag = "autoenergyat10percent_db",
Callback = function(Value)
Environment.AutoEnergyAt10Percent = Value
if Value then
task.spawn(function()
while Environment.AutoEnergyAt10Percent do
local character = Workspace.Characters:FindFirstChild(Players.LocalPlayer.Name)
if character and character:FindFirstChild("Status") then
local status = character.Status
local energy = status:FindFirstChild("CurrentEnergy")
local maxEnergy = status:FindFirstChild("MaxEnergy")
if energy and maxEnergy and energy.Value < (maxEnergy.Value * 0.1) then
VirtualInputManager:SendKeyEvent(true, Enum.KeyCode.C, false, game)
repeat task.wait()
until not character or not character.Parent or not status or not energy or not maxEnergy or energy.Value >= (maxEnergy.Value * 0.5) or not Environment.AutoEnergyAt10Percent
VirtualInputManager:SendKeyEvent(false, Enum.KeyCode.C, false, game)
end
end
task.wait()
end
end)
end
end
})
Tab:AddToggle({
Name = "Auto Block",
Default = false,
Save = true,
Flag = "autoblock_db",
Callback = function(Value)
Environment.AutoBlock = Value
autoblock()
end
})
Tab:AddToggle({
Name = "Auto Agility",
Default = false,
Save = true,
Flag = "autoagility_db",
Callback = function(Value)
Environment.AutoAgility = Value
autoagility()
end
})
Tab:AddToggle({
Name = "Auto Energy Ball (Drains Ki)",
Default = false,
Save = true,
Flag = "autoenergyball_db",
Callback = function(Value)
Environment.AutoEnergyBall = Value
autoenergyball()
end
})
Tab:AddToggle({
Name = "Auto Energy Ball 2 (Drains Ki)",
Default = false,
Save = true,
Flag = "autoenergyball2_db",
Callback = function(Value)
Environment.AutoEnergyBall2 = Value
autoenergyball2()
end
})
Tab:AddToggle({
    Name = "Auto Lock On (Aimbot!)",
    Default = false,
    Save = true,
    Flag = "autolockon_db",
    Callback = function(v)
        Environment.AutoLockOn = v
        if v then 
            coroutine.wrap(function()
                local currentTarget, maxDist = nil, 50
                while Environment.AutoLockOn and task.wait(0.1) do
                    local chr = Players.LocalPlayer.Character
                    if not (chr and chr:FindFirstChild("HumanoidRootPart")) then continue end
                    local pos = chr:WaitForChild("HumanoidRootPart").Position
                    if currentTarget and (not currentTarget:IsDescendantOf(Workspace) or
                        (currentTarget.Humanoid.Health <= 0) or
                        (currentTarget:WaitForChild("HumanoidRootPart").Position - pos).Magnitude > maxDist) then
                        currentTarget = nil
                    end
                    if not currentTarget then
                        local closestDist = maxDist
                        for _,model in Workspace:GetDescendants() do
                            if model:IsA("Model") and model ~= chr then
                                local hum = model:FindFirstChildOfClass("Humanoid")
                                local hrp = model:FindFirstChild("HumanoidRootPart")
                                if hum and hrp and hum.Health > 0 then
                                    local dist = (hrp.Position - pos).Magnitude
                                    if dist < closestDist then
                                        closestDist = dist
                                        currentTarget = model
                                    end
                                end
                            end
                        end
                        if currentTarget then
                            VirtualInputManager:SendKeyEvent(true, Enum.KeyCode.Q, false, game)
                            task.wait(0.05)
                            VirtualInputManager:SendKeyEvent(false, Enum.KeyCode.Q, false, game)
                        end
                    end
                end
            end)() 
        end
    end
})
Tab:AddToggle({
Name = "Auto Rebirth",
Default = false,
Save = true,
Flag = "autorebirth_db",
Callback = function(Value)
Environment.AutoRebirth = Value
autorebirth()
end
})
Tab:AddToggle({
Name = "Auto Rebirth 2",
Default = false,
Save = true,
Flag = "autorebirth2_db",
Callback = function(Value)
Environment.AutoRebirth2 = Value
autorebirth2()
end
})
Tab:AddToggle({
Name = "Auto Rebirth 3",
Default = false,
Save = true,
Flag = "autorebirth3_db",
Callback = function(Value)
Environment.AutoRebirth3 = Value
autorebirth3()
end
})
Tab:AddToggle({
Name = "Auto Kaioken/Technique",
Default = false,
Save = true,
Flag = "autotechnique_db",
Callback = function(Value)
Environment.AutoTechnique = Value
autotechnique()
end
})
Tab:AddToggle({
Name = "Auto Kaioken/Technique 2",
Default = false,
Save = true,
Flag = "autotechnique2_db",
Callback = function(Value)
Environment.AutoTechnique2 = Value
autotechnique2()
end
})
Tab:AddToggle({
Name = "Auto Kaioken/Technique 3",
Default = false,
Save = true,
Flag = "autotechnique3_db",
Callback = function(Value)
Environment.AutoTechnique3 = Value
if Value then
coroutine.wrap(function()
while Environment.AutoTechnique3 do
VirtualInputManager:SendKeyEvent(true, Enum.KeyCode.M, false, game)
task.wait()
end
end)()
end
end
})
Tab:AddButton({
Name = "Upgrade Technique",
Callback = function()
if Workspace.Misc.NPC.Main:FindFirstChild("KaiokenTrainerNPC") and Workspace.Misc.NPC.Main.KaiokenTrainerNPC:FindFirstChild("Head") then
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Main.KaiokenTrainerNPC:WaitForChild("Head").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer").InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer"), "TechniqueKaiokenGiver", 1, Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Main"):WaitForChild("KaiokenTrainerNPC")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
end
})
Tab:AddButton({
Name = "Upgrade Technique 2",
Callback = function()
if Workspace.Misc.NPC.Main:FindFirstChild("KaiokenTrainerNPC") and Workspace.Misc.NPC.Main.KaiokenTrainerNPC:FindFirstChild("HumanoidRootPart") then
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Main.KaiokenTrainerNPC:WaitForChild("HumanoidRootPart").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer").InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer"), "TechniqueKaiokenGiver", 1, Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Main"):WaitForChild("KaiokenTrainerNPC")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
end
})
Tab:AddButton({
Name = "Claim Daily Reward",
Callback = function()
require(ReplicatedStorage.Packages.Knit).GetService("LoginRewardsService").RF.ClaimDay.InvokeServer(unpack({require(ReplicatedStorage.Packages.Knit).GetService("LoginRewardsService").RF.ClaimDay}))
end
})
Tab:AddButton({
Name = "Claim Daily Reward 2",
Callback = function()
require(ReplicatedStorage.Packages._Index["sleitnick_knit@1.4.7"].knit).GetService("LoginRewardsService").RF.ClaimDay.InvokeServer(unpack({require(ReplicatedStorage.Packages._Index["sleitnick_knit@1.4.7"].knit).GetService("LoginRewardsService").RF.ClaimDay}))
end
})
Tab:AddToggle({
Name = "Auto Claim Daily",
Default = false,
Save = true,
Flag = "autoclaimdaily_db",
Callback = function(Value)
Environment.AutoClaimDaily = Value
if Value then
coroutine.wrap(function()
while Environment.AutoClaimDaily do
require(ReplicatedStorage.Packages.Knit).GetService("LoginRewardsService").RF.ClaimDay.InvokeServer(unpack({require(ReplicatedStorage.Packages.Knit).GetService("LoginRewardsService").RF.ClaimDay}))
task.wait()
end
end)()
end
end
})
Tab:AddToggle({
Name = "Auto Claim Daily 2",
Default = false,
Save = true,
Flag = "autoclaimdaily2_db",
Callback = function(Value)
Environment.AutoClaimDaily2 = Value
if Value then
coroutine.wrap(function()
while Environment.AutoClaimDaily2 do
require(ReplicatedStorage.Packages._Index["sleitnick_knit@1.4.7"].knit).GetService("LoginRewardsService").RF.ClaimDay.InvokeServer(unpack({require(ReplicatedStorage.Packages._Index["sleitnick_knit@1.4.7"].knit).GetService("LoginRewardsService").RF.ClaimDay}))
task.wait()
end
end)()
end
end
})
Tab:AddToggle({
Name = "Auto Dash",
Default = false,
Save = true,
Flag = "autodash_db",
Callback = function(Value)
Environment.AutoDash = Value
if Value then
coroutine.wrap(function()
while Environment.AutoDash do
require(ReplicatedStorage.Packages.Knit).GetController("SkillManager").TapInput(unpack({{SkillId = "7", Began = true}}))
task.wait()
end
end)()
end
end
})
Tab:AddToggle({
Name = "Auto Dash 2",
Default = false,
Save = true,
Flag = "autodash2_db",
Callback = function(Value)
Environment.AutoDash2 = Value
if Value then
coroutine.wrap(function()
while Environment.AutoDash2 do
require(ReplicatedStorage.Packages._Index["sleitnick_knit@1.4.7"].knit).GetController("SkillManager").TapInput(unpack({{SkillId = "7", Began = true}}))
task.wait()
end
end)()
end
end
})
Tab:AddToggle({
Name = "Auto Spin",
Default = false,
Save = true,
Flag = "autospin_db",
Callback = function(Value)
Environment.AutoSpin = Value
autospin()
end
})
Tab:AddButton({
Name = "Activate/Deactivate Fusion",
Callback = function()
require(ReplicatedStorage.Packages.Knit).GetController("SkillManager").TapInput(unpack({{SkillId = "9", Began = true}}))
end
})
Tab:AddButton({
Name = "Activate/Deactivate Fusion 2",
Callback = function()
require(ReplicatedStorage.Packages._Index["sleitnick_knit@1.4.7"].knit).GetController("SkillManager").TapInput(unpack({{SkillId = "9", Began = true}}))
end
})
Tab:AddToggle({
Name = "Auto Grab Drops [Op]",
Default = false,
Save = true,
Flag = "autodrops_db",
Callback = function(Value)
Environment.AutoDrops = Value
autodrops()
end
})
Tab:AddToggle({
Name = "Auto Grab Drops 2 [Op]",
Default = false,
Save = true,
Flag = "autodrops2_db",
Callback = function(Value)
Environment.AutoDrops2 = Value
autodrops2()
end
})
Tab:AddToggle({
Name = "Auto Grab Drops 3 [Op]",
Default = false,
Save = true,
Flag = "autodrops3_db",
Callback = function(Value)
Environment.AutoDrops3 = Value
autodrops3()
end
})
Tab:AddToggle({
Name = "Auto Grab Drops 4 [Op]",
Default = false,
Save = true,
Flag = "autodrops4_db",
Callback = function(Value)
Environment.AutoDrops4 = Value
autodrops4()
end
})
Tab:AddToggle({
Name = "Auto Grab Drops 5 [Op]",
Default = false,
Save = true,
Flag = "autodrops5_db",
Callback = function(Value)
Environment.AutoDrops5 = Value
autodrops5()
end
})
Tab:AddButton({
Name = "Activate/Deactivate Sprint",
Callback = function()
ReplicatedStorage.Packages._Index:FindFirstChild("sleitnick_knit@1.4.7").knit.Services.FlightService.RE.Sprint.FireServer(unpack({ReplicatedStorage.Packages._Index:FindFirstChild("sleitnick_knit@1.4.7").knit.Services.FlightService.RE.Sprint}))
end
})
Tab:AddToggle({
    Name = "Auto Transform",
    Default = false,
    Save = true,
    Flag = "autotransform_db",
    Callback = function(Value)
        Environment.AutoTransform = Value
        if Value then                       task.spawn(function()while Environment.AutoTransform do
                    local char = Players.LocalPlayer.Character or Players.LocalPlayer.CharacterAdded:Wait()
                    local gauge = char:FindFirstChild("Status") and char.Status:FindFirstChild("FormGauge")
                    if gauge and gauge.Value >= 1 then
                        require(ReplicatedStorage.Packages.Knit).GetController("SkillManager").TapInput(unpack({{SkillId = "10", Began = true}}))
                        repeat
                            task.wait()
                            char = Players.LocalPlayer.Character
                            gauge = char and char:FindFirstChild("Status") and char.Status:FindFirstChild("FormGauge")
                            if not Environment.AutoTransform then break end
                        until not gauge or gauge.Value < 1
                    end
                    task.wait()
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Transform 2",
    Default = false,
    Save = true,
    Flag = "autotransform2_db",
    Callback = function(Value)
        Environment.AutoTransform2 = Value
        if Value then                       task.spawn(function()while Environment.AutoTransform2 do
                    local char = Players.LocalPlayer.Character or Players.LocalPlayer.CharacterAdded:Wait()
                    local gauge = char:FindFirstChild("Status") and char.Status:FindFirstChild("FormGauge")
                    if gauge and gauge.Value >= 1 then
                        require(ReplicatedStorage.Packages.Knit).GetController("SkillManager").TapInput(unpack({{SkillId = "10", Began = true}}))
                        repeat
                            task.wait()
                            char = Players.LocalPlayer.Character
                            gauge = char and char:FindFirstChild("Status") and char.Status:FindFirstChild("FormGauge")
                            if not Environment.AutoTransform2 then break end
                        until not gauge or gauge.Value < 1
                    end
                    task.wait()
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Transform (PC!)",
    Default = false,
    Save = true,
    Flag = "autotransform3_db",
    Callback = function(Value)
        Environment.AutoTransform3 = Value
        if Value then                       task.spawn(function()while Environment.AutoTransform3 do
                    local char = Players.LocalPlayer.Character or Players.LocalPlayer.CharacterAdded:Wait()
                    local gauge = char:FindFirstChild("Status") and char.Status:FindFirstChild("FormGauge")
                    if gauge and gauge.Value >= 1 then
                        VirtualInputManager:SendKeyEvent(true, Enum.KeyCode.Y, false, game)
                        repeat
                            task.wait()
                            char = Players.LocalPlayer.Character
                            gauge = char and char:FindFirstChild("Status") and char.Status:FindFirstChild("FormGauge")
                            if not Environment.AutoTransform3 then break end
                        until not gauge or gauge.Value < 1
                    end
                    task.wait()
                end
            end)
        end
    end
})
Tab:AddButton({
Name = "Transform/Untransform",
Callback = function()
require(ReplicatedStorage.Packages.Knit).GetController("SkillManager").TapInput(unpack({{SkillId = "10", Began = true}}))
end
})
Tab:AddButton({
Name = "Transform/Untransform 2",
Callback = function()
require(ReplicatedStorage.Packages._Index["sleitnick_knit@1.4.7"].knit).GetController("SkillManager").TapInput(unpack({{SkillId = "10", Began = true}}))
end
})
Tab:AddButton({
Name = "Transform/UnTransform 3",
Callback = function()
VirtualInputManager:SendKeyEvent(true, Enum.KeyCode.Y, false, game)
end
})
Tab:AddToggle({
Name = "Anti Death (Godmode!)",
Default = false,
Save = true,
Flag = "antideath_db",
Callback = function(Value)
Environment.AntiDeath = Value
while Environment.AntiDeath do task.wait()
if Players.LocalPlayer.Character and Players.LocalPlayer.Character:FindFirstChild("Humanoid") then
local Humanoid = Players.LocalPlayer.Character.Humanoid
if Humanoid.Health <= 0 then
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.CharacterAdded:Wait()
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
end
end
end
})
Tab:AddToggle({
Name = "Anti Death 2 (Godmode!)",
Default = false,
Save = true,
Flag = "antideath2_db",
Callback = function(Value)
Environment.AntiDeath2 = Value
while Environment.AntiDeath2 do
if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid") then
local Humanoid = LocalPlayer.Character.Humanoid
if Humanoid.Health <= 0 then
local RootPart = LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
if RootPart then
local OldCFrame = RootPart.CFrame
LocalPlayer.CharacterAdded:Wait()
local NewRootPart = LocalPlayer.Character:WaitForChild("HumanoidRootPart")
if NewRootPart then
TweenService:Create(
NewRootPart,
TweenInfo.new(2, Enum.EasingStyle.Linear),
{CFrame = OldCFrame}
):Play()
end
end
end
end
task.wait()
end
end
})
Tab:AddToggle({
Name = "Loopkill All (Client Sided!)",
Default = false,
Save = true,
Flag = "loopkillall_db",
Callback = function(Value)
Environment.loopkillall = Value
while Environment.loopkillall do
task.wait(1.25)
pcall(function()
sethiddenproperty(Players.LocalPlayer, "SimulationRadius", math.huge)
sethiddenproperty(Players.LocalPlayer, "MaxSimulationRadius", math.huge)
for _, obj in game.Workspace:GetDescendants() do
if obj:IsA("Humanoid") and obj.Parent.Name ~= Players.LocalPlayer.Name then
obj.Health = 0
end
end
end)
end
end
})
Tab:AddToggle({
Name = "Loopkill Self (Client Sided!)",
Default = false,
Save = true,
Flag = "loopkillself_db",
Callback = function(Value)
Environment.loopkillself = Value
while Environment.loopkillself do task.wait()
Players.LocalPlayer.Character:WaitForChild("Humanoid").Health = 0
end
end
})
Tab:AddToggle({
Name = "Loopkill Self 2 (Client Sided!)",
Default = false,
Save = true,
Flag = "loopkillself2_db",
Callback = function(Value)
Environment.loopkillself2 = Value
while Environment.loopkillself2 do task.wait()
Players.LocalPlayer.Character.Humanoid.Health = 0
end
end
})
Tab:AddToggle({
Name = "Loopkill Self 3 (Client Sided!)",
Default = false,
Save = true,
Flag = "loopkillself3_db",
Callback = function(Value)
Environment.loopkillself3 = Value
while Environment.loopkillself3 do task.wait()
Players.LocalPlayer.Character:FindFirstChild("Humanoid").Health = 0
end
end
})
Tab:AddToggle({
Name = "Auto Destroy Display Name",
Default = false,
Flag = "autodestroyname_db",
Save = true,
Callback = function(Value)
Environment.autodestroyname = Value
while Environment.autodestroyname do task.wait()
for _, v in Players.LocalPlayer.Character:WaitForChild("Head"):GetChildren() do
if v:IsA("BillboardGui") then
v:Destroy()
end
end
end
end
})
Tab:AddButton({
Name = "Destroy Username",
Callback = function()
for _, v in Players.LocalPlayer.Character:WaitForChild("Head"):GetChildren() do
if v:IsA("BillboardGui") then
v:Destroy()
end
end
end
})
Tab:AddToggle({
    Name = "Auto Nearby Boss Mobs (Back Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbybossmobs_db",
    Callback = function(Value)
        Environment.autotpnearbybossmobs = Value
        if not Value then return end
        while Environment.autotpnearbybossmobs do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                if not rootPart then return end
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v:IsA("Model") and v.Name ~= "Karrot" and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.lookAt(mobHRP.Position - (mobHRP.CFrame.LookVector * -5), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Boss Mobs 2 (Head Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbybossmobs2_db",
    Callback = function(Value)
        Environment.autotpnearbybossmobs2 = Value
        if not Value then return end
        while Environment.autotpnearbybossmobs2 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                if not rootPart then return end
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v:IsA("Model") and v.Name ~= "Karrot" and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("Head").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    tweenTo(rootPart, nearestBossMob:WaitForChild("Head").CFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Boss Mobs 3 (Front Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbybossmobs3_db",
    Callback = function(Value)
        Environment.autotpnearbybossmobs3 = Value
        if not Value then return end
        while Environment.autotpnearbybossmobs3 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                if not rootPart then return end
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v:IsA("Model") and v.Name ~= "Karrot" and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.lookAt(mobHRP.Position + (mobHRP.CFrame.LookVector * 5), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Boss Mobs 4 (Under Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbybossmobs4_db",
    Callback = function(Value)
        Environment.autotpnearbybossmobs4 = Value
        if not Value then return end
        while Environment.autotpnearbybossmobs4 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then part.CanCollide = false end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                if not rootPart then return end
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v:IsA("Model") and v.Name ~= "Karrot" and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.new(mobHRP.Position - Vector3.new(0, 5, 0), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddButton({
Name = "Enable Gamepasses (Visual!)",
Callback = function()
for _, v in Players.LocalPlayer.Stats.GamePasses:GetChildren() do
v.Value = true
end
end
})
Tab:AddButton({
Name = "Disable Gamepasses (Visual!)",
Callback = function()
for _, v in Players.LocalPlayer.Stats.GamePasses:GetChildren() do
v.Value = false
end
end
})
Tab:AddButton({
    Name = "Hitbox Players",
    Callback = function()
        HitboxSettings = {
            Enabled = true,
            Size = 50,
            Transparency = 0.3,
            Color = "Bright violet",
            Material = Enum.Material.Neon,
            Damage = 15
        }
        local function applyHitboxToPlayer(player)
            if player and player ~= Players.LocalPlayer and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                local hrp = player.Character:WaitForChild("HumanoidRootPart")
                pcall(function()
                    hrp.Size = Vector3.new(HitboxSettings.Size, HitboxSettings.Size, HitboxSettings.Size)
                    hrp.Transparency = HitboxSettings.Transparency
                    hrp.BrickColor = BrickColor.new(HitboxSettings.Color)
                    hrp.Material = HitboxSettings.Material
                    hrp.CanCollide = false
                end)
            end
        end
        for _, player in Players:GetPlayers() do
            applyHitboxToPlayer(player)
        end
        Players.PlayerAdded:Connect(function(player)
            player.CharacterAdded:Connect(function(character)
                applyHitboxToPlayer(player)
            end)
        end)
        Players.PlayerRemoving:Connect(function(player)
            if player and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                local hrp = player.Character:WaitForChild("HumanoidRootPart")
                pcall(function()
                    hrp.Size = Vector3.new(2, 2, 1)
                    hrp.Transparency = 0
                    hrp.BrickColor = BrickColor.new("Medium stone grey")
                    hrp.Material = Enum.Material.Plastic
                    hrp.CanCollide = true
                end)
            end
        end)
        StarterGui:SetCore("SendNotification", {
            Title = "Hitbox Script Loaded",
            Text = "Unique hitbox script activated.",
            Duration = 5
        })
        Players.LocalPlayer.CharacterAdded:Connect(function(character)
            character.ChildAdded:Connect(function(child)
                if child:IsA("Tool") or child:IsA("Accessory") then
                    if HitboxSettings.Enabled and child:FindFirstChild("Handle") then
                        local handle = child.Handle
                        handle.Touched:Connect(function(hit)
                            if hit.Parent and hit.Parent:FindFirstChild("Humanoid") and hit.Parent.Name ~= Players.LocalPlayer.Name then
                                hit.Parent.Humanoid:TakeDamage(HitboxSettings.Damage)
                            end
                        end)
                    end
                end
            end)
        end)
    end
})
Tab:AddButton({
Name = "Server Hop",
Callback = function()
TeleportService:Teleport(3177438863, Players.LocalPlayer);
end
})
Tab:AddButton({
Name = "Server Hop To Dead Server",
Callback =function()
local Http = game:GetService("HttpService")
local TPS = game:GetService("TeleportService")
local Api = "https://games.roblox.com/v1/games/"
local _place = game.PlaceId
local _servers = Api.._place.."/servers/Public?sortOrder=Asc&limit=100"
function ListServers(cursor)
local Raw = game:HttpGet(_servers .. ((cursor and "&cursor="..cursor) or ""))
return Http:JSONDecode(Raw)
end
local Server, Next; repeat
local Servers = ListServers(Next)
Server = Servers.data[1]
Next = Servers.nextPageCursor
until Server
TPS:TeleportToPlaceInstance(_place,Server.id,Players.LocalPlayer)
end
})
Tab:AddButton({
Name = "Server Hop If You Die",
Callback = function()
task.wait(0.75)
Players.LocalPlayer.Character.Humanoid.Died:Connect(function()
local x = {}
for _, v in game:GetService("HttpService"):JSONDecode(game:HttpGetAsync("https://games.roblox.com/v1/games/" .. game.PlaceId .. "/servers/Public?sortOrder=Asc&limit=100")).data do
if type(v) == "table" and v.maxPlayers > v.playing and v.id ~= game.JobId then
x[#x + 1] = v.id
end
end
if #x > 0 then
TeleportService:TeleportToPlaceInstance(game.PlaceId, x[math.random(1, #x)])
else
print("Server not found.")
end
end)
end
})
Tab:AddButton({
Name = "Leave Server",
Callback = function()
game:Shutdown();
end
})
Tab:AddButton({
Name = "Rejoin Server",
Callback = function()
TeleportService:TeleportToPlaceInstance(game.PlaceId, game.JobId, Players.LocalPlayer)
end
})
Tab:AddButton({
Name = "Unload Gui",
Callback = function()
OrionLib:Destroy()
end
})
local Tab = Window:MakeTab({
Name = "Mobs",
Icon = "rbxassetid://4483345998",
PremiumOnly = false
})
local Section = Tab:AddSection({
Name = "Mobs Select Tab (Only Select 1 At A Time!)"
})
Tab:AddLabel("Just Get The Quest To Farm Your Mob")
Tab:AddLabel("Puriza X003 Army")
Tab:AddButton({
Name = "Get Puriza X003 Army Quest",
Callback = function()
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quest.QuestNPCDroid1_PurizaX003:WaitForChild("Head").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer").InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer"), "NPCQuest_Capital City",2,Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Quest"):WaitForChild("QuestNPCDroid1_PurizaX003")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
})
Tab:AddButton({
Name = "Get Puriza X003 Army Quest 2",
Callback = function()
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quest.QuestNPCDroid1_PurizaX003:WaitForChild("HumanoidRootPart").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer").InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer"), "NPCQuest_Capital City",2,Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Quest"):WaitForChild("QuestNPCDroid1_PurizaX003")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
})
Tab:AddToggle({
    Name = "Auto Nearby Puriza X003 Army Mobs (Back Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbypurizax003armymobs_db",
    Callback = function(Value)
        Environment.autotpnearbypurizax003armymobs = Value
        if not Value then return end
        while Environment.autotpnearbypurizax003armymobs do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then
                                part.CanCollide = false
                            end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Puriza X003 Army" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    local mobHRP = nearestMob:WaitForChild("HumanoidRootPart")
                    local offsetPosition = mobHRP.Position - (mobHRP.CFrame.LookVector * 5)
                    local targetCFrame = CFrame.new(offsetPosition, mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Puriza X003 Army Mobs 2 (Head Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbypurizax003armymobs2_db",
    Callback = function(Value)
        Environment.autotpnearbypurizax003armymobs2 = Value
        if not Value then return end
        while Environment.autotpnearbypurizax003armymobs2 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then
                                part.CanCollide = false
                            end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Puriza X003 Army" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("Head").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    tweenTo(rootPart, nearestMob:WaitForChild("Head").CFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Puriza X003 Army Mobs 3 (Front Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbypurizax003armymobs3_db",
    Callback = function(Value)
        Environment.autotpnearbypurizax003armymobs3 = Value
        if not Value then return end
        while Environment.autotpnearbypurizax003armymobs3 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then
                                part.CanCollide = false
                            end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Puriza X003 Army" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    local mobHRP = nearestMob:WaitForChild("HumanoidRootPart")
                    local offsetPosition = mobHRP.Position + (mobHRP.CFrame.LookVector * 5)
                    local targetCFrame = CFrame.new(offsetPosition, mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Puriza X003 Army Mobs 4 (Under Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbypurizax003armymobs4_db",
    Callback = function(Value)
        Environment.autotpnearbypurizax003armymobs4 = Value
        if not Value then return end
        while Environment.autotpnearbypurizax003armymobs4 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then
                                part.CanCollide = false
                            end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Puriza X003 Army" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    local mobHRP = nearestMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.new(mobHRP.Position - Vector3.new(0, 5, 0), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddLabel("Atom X002 Army")
Tab:AddButton({
Name = "Get Atom X002 Army Quest",
Callback = function()
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quest.QuestNPCDroid2_AtomX002:WaitForChild("Head").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7").knit.Services.DialogService.RF.Answer.InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7").knit.Services.DialogService.RF.Answer, "NPCQuest_Tribe Village",2,Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Quest"):WaitForChild("QuestNPCDroid2_AtomX002")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
})
Tab:AddButton({
Name = "Get Atom X002 Army Quest 2",
Callback = function()
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quest.QuestNPCDroid2_AtomX002:WaitForChild("HumanoidRootPart").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7").knit.Services.DialogService.RF.Answer.InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7").knit.Services.DialogService.RF.Answer, "NPCQuest_Tribe Village",2,Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Quest"):WaitForChild("QuestNPCDroid2_AtomX002")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
})
Tab:AddToggle({
    Name = "Auto Nearby Atom X002 Army Mobs (Back Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyatomx002armymobs_db",
    Callback = function(Value)
        Environment.autotpnearbyatomx002armymobs = Value
        if not Value then return end
        while Environment.autotpnearbyatomx002armymobs do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then
                                part.CanCollide = false
                            end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Atom X002 Army" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    local mobHRP = nearestMob:WaitForChild("HumanoidRootPart")
                    local offsetPosition = mobHRP.Position - (mobHRP.CFrame.LookVector * 5)
                    local targetCFrame = CFrame.new(offsetPosition, mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Atom X002 Army Mobs 2 (Head Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyatomx002armymobs2_db",
    Callback = function(Value)
        Environment.autotpnearbyatomx002armymobs2 = Value
        if not Value then return end
        while Environment.autotpnearbyatomx002armymobs2 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then
                                part.CanCollide = false
                            end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Atom X002 Army" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("Head").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    tweenTo(rootPart, nearestMob:WaitForChild("Head").CFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Atom X002 Army Mobs 3 (Front Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyatomx002armymobs3_db",
    Callback = function(Value)
        Environment.autotpnearbyatomx002armymobs3 = Value
        if not Value then return end
        while Environment.autotpnearbyatomx002armymobs3 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then
                                part.CanCollide = false
                            end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Atom X002 Army" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    local mobHRP = nearestMob:WaitForChild("HumanoidRootPart")
                    local offsetPosition = mobHRP.Position + (mobHRP.CFrame.LookVector * 5)
                    local targetCFrame = CFrame.new(offsetPosition, mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Atom X002 Army Mobs 4 (Under Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyatomx002armymobs4_db",
    Callback = function(Value)
        Environment.autotpnearbyatomx002armymobs4 = Value
        if not Value then return end
        while Environment.autotpnearbyatomx002armymobs4 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then
                                part.CanCollide = false
                            end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Atom X002 Army" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    local mobHRP = nearestMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.new(mobHRP.Position - Vector3.new(0, 5, 0), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddLabel("Great Droid Army")
Tab:AddLabel("The Quest For This Was Removed Sadly!")
Tab:AddToggle({
    Name = "Auto Nearby Great Droid Army Mobs (Back Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbygreatdroidarmymobs_db",
    Callback = function(Value)
        Environment.autotpnearbygreatdroidarmymobs = Value
        if not Value then return end
        while Environment.autotpnearbygreatdroidarmymobs do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then
                                part.CanCollide = false
                            end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Great Droid Army" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    local mobHRP = nearestMob:WaitForChild("HumanoidRootPart")
                    local offsetPosition = mobHRP.Position - (mobHRP.CFrame.LookVector * 5)
                    local targetCFrame = CFrame.new(offsetPosition, mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Great Droid Army Mobs 2 (Head Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbygreatdroidarmymobs2_db",
    Callback = function(Value)
        Environment.autotpnearbygreatdroidarmymobs2 = Value
        if not Value then return end
        while Environment.autotpnearbygreatdroidarmymobs2 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then
                                part.CanCollide = false
                            end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Great Droid Army" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("Head").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    tweenTo(rootPart, nearestMob:WaitForChild("Head").CFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Great Droid Army Mobs 3 (Front Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbygreatdroidarmymobs3_db",
    Callback = function(Value)
        Environment.autotpnearbygreatdroidarmymobs3 = Value
        if not Value then return end
        while Environment.autotpnearbygreatdroidarmymobs3 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then
                                part.CanCollide = false
                            end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Great Droid Army" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    local mobHRP = nearestMob:WaitForChild("HumanoidRootPart")
                    local offsetPosition = mobHRP.Position + (mobHRP.CFrame.LookVector * 5)
                    local targetCFrame = CFrame.new(offsetPosition, mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Great Droid Army Mobs 4 (Under Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbygreatdroidarmymobs4_db",
    Callback = function(Value)
        Environment.autotpnearbygreatdroidarmymobs4 = Value
        if not Value then return end
        while Environment.autotpnearbygreatdroidarmymobs4 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then
                                part.CanCollide = false
                            end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Great Droid Army" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    local mobHRP = nearestMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.new(mobHRP.Position - Vector3.new(0, 5, 0), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddLabel("Brawly X01 Army")
Tab:AddButton({
Name = "Get Brawly X01 Army Quest",
Callback = function()
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quest.QuestNPCDroid3_BrawlyX01:WaitForChild("Head").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7").knit.Services.DialogService.RF.Answer.InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7").knit.Services.DialogService.RF.Answer, "NPCQuest_Volcanic Island",2,Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Quest"):WaitForChild("QuestNPCDroid3_BrawlyX01")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
})
Tab:AddButton({
Name = "Get Brawly X01 Army Quest 2",
Callback = function()
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quest.QuestNPCDroid3_BrawlyX01:WaitForChild("HumanoidRootPart").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7").knit.Services.DialogService.RF.Answer.InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7").knit.Services.DialogService.RF.Answer, "NPCQuest_Volcanic Island",2,Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Quest"):WaitForChild("QuestNPCDroid3_BrawlyX01")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
})
Tab:AddToggle({
    Name = "Auto Nearby Brawly X01 Army Mobs (Back Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbybrawlyx01armymobs_db",
    Callback = function(Value)
        Environment.autotpnearbybrawlyx01armymobs = Value
        if not Value then return end
        while Environment.autotpnearbybrawlyx01armymobs do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then
                                part.CanCollide = false
                            end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Brawly X01 Army" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    local mobHRP = nearestMob:WaitForChild("HumanoidRootPart")
                    local offsetPosition = mobHRP.Position - (mobHRP.CFrame.LookVector * 5)
                    local targetCFrame = CFrame.new(offsetPosition, mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Brawly X01 Army Mobs 2 (Head Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbybrawlyx01armymobs2_db",
    Callback = function(Value)
        Environment.autotpnearbybrawlyx01armymobs2 = Value
        if not Value then return end
        while Environment.autotpnearbybrawlyx01armymobs2 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then
                                part.CanCollide = false
                            end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Brawly X01 Army" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("Head").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    tweenTo(rootPart, nearestMob:WaitForChild("Head").CFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Brawly X01 Army Mobs 3 (Front Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbybrawlyx01armymobs3_db",
    Callback = function(Value)
        Environment.autotpnearbybrawlyx01armymobs3 = Value
        if not Value then return end
        while Environment.autotpnearbybrawlyx01armymobs3 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then
                                part.CanCollide = false
                            end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Brawly X01 Army" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    local mobHRP = nearestMob:WaitForChild("HumanoidRootPart")
                    local offsetPosition = mobHRP.Position + (mobHRP.CFrame.LookVector * 5)
                    local targetCFrame = CFrame.new(offsetPosition, mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Brawly X01 Army Mobs 4 (Under Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbybrawlyx01armymobs4_db",
    Callback = function(Value)
        Environment.autotpnearbybrawlyx01armymobs4 = Value
        if not Value then return end
        while Environment.autotpnearbybrawlyx01armymobs4 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then
                                part.CanCollide = false
                            end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Brawly X01 Army" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    local mobHRP = nearestMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.new(mobHRP.Position - Vector3.new(0, 5, 0), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddLabel("Jigray X Army")
Tab:AddButton({
Name = "Get Jigray X Army Quest",
Callback = function()
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quest.QuestNPCDroid4_JigrayX:WaitForChild("Head").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer").InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer"), "NPCQuest_Misty Lake",2,Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Quest"):WaitForChild("QuestNPCDroid4_JigrayX")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
})
Tab:AddButton({
Name = "Get Jigray X Army Quest 2",
Callback = function()
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quest.QuestNPCDroid4_JigrayX:WaitForChild("HumanoidRootPart").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer").InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer"), "NPCQuest_Misty Lake",2,Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Quest"):WaitForChild("QuestNPCDroid4_JigrayX")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
})
Tab:AddToggle({
    Name = "Auto Nearby Jigray X Army Mobs (Back Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyjigrayxarmymobs_db",
    Callback = function(Value)
        Environment.autotpnearbyjigrayxarmymobs = Value
        if not Value then return end
        while Environment.autotpnearbyjigrayxarmymobs do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then
                                part.CanCollide = false
                            end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Jigray X Army" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    local mobHRP = nearestMob:WaitForChild("HumanoidRootPart")
                    local offsetPosition = mobHRP.Position - (mobHRP.CFrame.LookVector * 5)
                    local targetCFrame = CFrame.new(offsetPosition, mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Jigray X Army Mobs 2 (Head Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyjigrayxarmymobs2_db",
    Callback = function(Value)
        Environment.autotpnearbyjigrayxarmymobs2 = Value
        if not Value then return end
        while Environment.autotpnearbyjigrayxarmymobs2 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then
                                part.CanCollide = false
                            end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Jigray X Army" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("Head").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    tweenTo(rootPart, nearestMob:WaitForChild("Head").CFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Jigray X Army Mobs 3 (Front Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyjigrayxarmymobs3_db",
    Callback = function(Value)
        Environment.autotpnearbyjigrayxarmymobs3 = Value
        if not Value then return end
        while Environment.autotpnearbyjigrayxarmymobs3 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then
                                part.CanCollide = false
                            end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Jigray X Army" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    local mobHRP = nearestMob:WaitForChild("HumanoidRootPart")
                    local offsetPosition = mobHRP.Position + (mobHRP.CFrame.LookVector * 5)
                    local targetCFrame = CFrame.new(offsetPosition, mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Jigray X Army Mobs 4 (Under Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyjigrayxarmymobs4_db",
    Callback = function(Value)
        Environment.autotpnearbyjigrayxarmymobs4 = Value
        if not Value then return end
        while Environment.autotpnearbyjigrayxarmymobs4 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then
                                part.CanCollide = false
                            end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Jigray X Army" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    local mobHRP = nearestMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.new(mobHRP.Position - Vector3.new(0, 5, 0), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddLabel("Zero Army")
Tab:AddButton({
Name = "Get Zero Army Quest",
Callback = function()
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quest.QuestNPCDroid5_Zero:WaitForChild("Head").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer").InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer"), "NPCQuest_Droid Waste Island",7,Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Quest"):WaitForChild("QuestNPCDroid5_Zero")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
})
Tab:AddButton({
Name = "Get Zero Army Quest 2",
Callback = function()
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quest.QuestNPCDroid5_Zero:WaitForChild("HumanoidRootPart").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer").InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer"), "NPCQuest_Droid Waste Island",7,Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Quest"):WaitForChild("QuestNPCDroid5_Zero")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
})
Tab:AddToggle({
    Name = "Auto Nearby Zero Army Mobs (Back Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyzeroarmymobs_db",
    Callback = function(Value)
        Environment.autotpnearbyzeroarmymobs = Value
        if not Value then return end
        while Environment.autotpnearbyzeroarmymobs do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then
                                part.CanCollide = false
                            end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Zero Army" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    local mobHRP = nearestMob:WaitForChild("HumanoidRootPart")
                    local offsetPosition = mobHRP.Position - (mobHRP.CFrame.LookVector * 5)
                    local targetCFrame = CFrame.new(offsetPosition, mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Zero Army Mobs 2 (Head Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyzeroarmymobs2_db",
    Callback = function(Value)
        Environment.autotpnearbyzeroarmymobs2 = Value
        if not Value then return end
        while Environment.autotpnearbyzeroarmymobs2 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then
                                part.CanCollide = false
                            end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Zero Army" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("Head").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    tweenTo(rootPart, nearestMob:WaitForChild("Head").CFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Zero Army Mobs 3 (Front Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyzeroarmymobs3_db",
    Callback = function(Value)
        Environment.autotpnearbyzeroarmymobs3 = Value
        if not Value then return end
        while Environment.autotpnearbyzeroarmymobs3 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then
                                part.CanCollide = false
                            end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Zero Army" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    local mobHRP = nearestMob:WaitForChild("HumanoidRootPart")
                    local offsetPosition = mobHRP.Position + (mobHRP.CFrame.LookVector * 5)
                    local targetCFrame = CFrame.new(offsetPosition, mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Zero Army Mobs 4 (Under Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyzeroarmymobs4_db",
    Callback = function(Value)
        Environment.autotpnearbyzeroarmymobs4 = Value
        if not Value then return end
        while Environment.autotpnearbyzeroarmymobs4 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then
                                part.CanCollide = false
                            end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Mobs"):GetChildren() do
                    if v.Name == "Zero Army" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestMob = distance, v
                        end
                    end
                end
                if nearestMob then
                    local mobHRP = nearestMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.new(mobHRP.Position - Vector3.new(0, 5, 0), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddLabel("Just Get The Quest To Farm Your Boss Mob")
Tab:AddLabel("Puriza X003")
Tab:AddButton({
Name = "Get Puriza X003 Quest",
Callback = function()
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quest.QuestNPCDroid1_PurizaX003:WaitForChild("Head").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer").InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer"), "NPCQuest_Capital City", 7, Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Quest"):WaitForChild("QuestNPCDroid1_PurizaX003")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
})
Tab:AddButton({
Name = "Get Puriza X003 Quest 2",
Callback = function()
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quest.QuestNPCDroid1_PurizaX003:WaitForChild("HumanoidRootPart").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer").InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer"), "NPCQuest_Capital City", 7, Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Quest"):WaitForChild("QuestNPCDroid1_PurizaX003")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
})
Tab:AddToggle({
    Name = "Auto Nearby Puriza X003 Boss Mobs (Back Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbypurizax003bossmobs_db",
    Callback = function(Value)
        Environment.autotpnearbypurizax003bossmobs = Value
        if not Value then return end
        while Environment.autotpnearbypurizax003bossmobs do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then
                                part.CanCollide = false
                            end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Puriza X003" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local offsetPosition = mobHRP.Position - (mobHRP.CFrame.LookVector * 5)
                    local targetCFrame = CFrame.new(offsetPosition, mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Puriza X003 Boss Mobs 2 (Head Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbypurizax003bossmobs2_db",
    Callback = function(Value)
        Environment.autotpnearbypurizax003bossmobs2 = Value
        if not Value then return end
        while Environment.autotpnearbypurizax003bossmobs2 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then
                                part.CanCollide = false
                            end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Puriza X003" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("Head").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    tweenTo(rootPart, nearestBossMob:WaitForChild("Head").CFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Puriza X003 Boss Mobs 3 (Front Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbypurizax003bossmobs3_db",
    Callback = function(Value)
        Environment.autotpnearbypurizax003bossmobs3 = Value
        if not Value then return end
        while Environment.autotpnearbypurizax003bossmobs3 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then
                                part.CanCollide = false
                            end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Puriza X003" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local offsetPosition = mobHRP.Position + (mobHRP.CFrame.LookVector * 5)
                    local targetCFrame = CFrame.new(offsetPosition, mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Puriza X003 Boss Mobs 4 (Under Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbypurizax003bossmobs4_db",
    Callback = function(Value)
        Environment.autotpnearbypurizax003bossmobs4 = Value
        if not Value then return end
        while Environment.autotpnearbypurizax003bossmobs4 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then
                                part.CanCollide = false
                            end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Puriza X003" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.new(mobHRP.Position - Vector3.new(0, 5, 0), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddLabel("Atom X002")
Tab:AddButton({
Name = "Get Atom X002 Quest",
Callback = function()
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quest.QuestNPCDroid2_AtomX002:WaitForChild("Head").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer").InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer"), "NPCQuest_Tribe Village", 7, Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Quest"):WaitForChild("QuestNPCDroid2_AtomX002")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
})
Tab:AddButton({
Name = "Get Atom X002 Quest 2",
Callback = function()
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quest.QuestNPCDroid2_AtomX002:WaitForChild("HumanoidRootPart").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer").InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer"), "NPCQuest_Tribe Village", 7, Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Quest"):WaitForChild("QuestNPCDroid2_AtomX002")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
})
Tab:AddToggle({
    Name = "Auto Nearby Atom X002 Boss Mobs (Back Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyatomx002bossmobs_db",
    Callback = function(Value)
        Environment.autotpnearbyatomx002bossmobs = Value
        if not Value then return end
        while Environment.autotpnearbyatomx002bossmobs do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then
                                part.CanCollide = false
                            end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Atom X002" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local offsetPosition = mobHRP.Position - (mobHRP.CFrame.LookVector * 5)
                    local targetCFrame = CFrame.new(offsetPosition, mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Atom X002 Boss Mobs 2 (Head Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyatomx002bossmobs2_db",
    Callback = function(Value)
        Environment.autotpnearbyatomx002bossmobs2 = Value
        if not Value then return end
        while Environment.autotpnearbyatomx002bossmobs2 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then
                                part.CanCollide = false
                            end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Atom X002" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("Head").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    tweenTo(rootPart, nearestBossMob:WaitForChild("Head").CFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Atom X002 Boss Mobs 3 (Front Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyatomx002bossmobs3_db",
    Callback = function(Value)
        Environment.autotpnearbyatomx002bossmobs3 = Value
        if not Value then return end
        while Environment.autotpnearbyatomx002bossmobs3 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then
                                part.CanCollide = false
                            end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Atom X002" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local offsetPosition = mobHRP.Position + (mobHRP.CFrame.LookVector * 5)
                    local targetCFrame = CFrame.new(offsetPosition, mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Atom X002 Boss Mobs 4 (Under Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyatomx002bossmobs4_db",
    Callback = function(Value)
        Environment.autotpnearbyatomx002bossmobs4 = Value
        if not Value then return end
        while Environment.autotpnearbyatomx002bossmobs4 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then
                                part.CanCollide = false
                            end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Atom X002" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.new(mobHRP.Position - Vector3.new(0, 5, 0), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddLabel("Brawly X001")
Tab:AddButton({
Name = "Get Brawly X001 Quest",
Callback = function()
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quest.QuestNPCDroid3_BrawlyX01:WaitForChild("Head").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer").InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer"), "NPCQuest_Volcanic Island", 7, Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Quest"):WaitForChild("QuestNPCDroid3_BrawlyX01")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
})
Tab:AddButton({
Name = "Get Brawly X001 Quest 2",
Callback = function()
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quest.QuestNPCDroid3_BrawlyX01:WaitForChild("HumanoidRootPart").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer").InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer"), "NPCQuest_Volcanic Island", 7, Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Quest"):WaitForChild("QuestNPCDroid3_BrawlyX01")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
})
Tab:AddToggle({
    Name = "Auto Nearby Brawly X01 Boss Mobs (Back Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbybrawlyx001bossmobs_db",
    Callback = function(Value)
        Environment.autotpnearbybrawlyx001bossmobs = Value
        if not Value then return end
        while Environment.autotpnearbybrawlyx001bossmobs do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then
                                part.CanCollide = false
                            end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Brawly X01" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local offsetPosition = mobHRP.Position - (mobHRP.CFrame.LookVector * 5)
                    local targetCFrame = CFrame.new(offsetPosition, mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Brawly X01 Boss Mobs 2 (Head Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbybrawlyx001bossmobs2_db",
    Callback = function(Value)
        Environment.autotpnearbybrawlyx001bossmobs2 = Value
        if not Value then return end
        while Environment.autotpnearbybrawlyx001bossmobs2 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then
                                part.CanCollide = false
                            end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Brawly X01" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("Head").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    tweenTo(rootPart, nearestBossMob:WaitForChild("Head").CFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Brawly X01 Boss Mobs 3 (Front Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbybrawlyx001bossmobs3_db",
    Callback = function(Value)
        Environment.autotpnearbybrawlyx001bossmobs3 = Value
        if not Value then return end
        while Environment.autotpnearbybrawlyx001bossmobs3 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then
                                part.CanCollide = false
                            end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Brawly X01" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local offsetPosition = mobHRP.Position + (mobHRP.CFrame.LookVector * 5)
                    local targetCFrame = CFrame.new(offsetPosition, mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Brawly X01 Boss Mobs 4 (Under Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbybrawlyx001bossmobs4_db",
    Callback = function(Value)
        Environment.autotpnearbybrawlyx001bossmobs4 = Value
        if not Value then return end
        while Environment.autotpnearbybrawlyx001bossmobs4 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then
                                part.CanCollide = false
                            end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Brawly X01" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.new(mobHRP.Position - Vector3.new(0, 5, 0), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddLabel("Jigray X")
Tab:AddButton({
Name = "Get Jigray X Quest",
Callback = function()
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quest.QuestNPCDroid4_JigrayX:WaitForChild("Head").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer").InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer"), "NPCQuest_Misty Lake", 7, Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Quest"):WaitForChild("QuestNPCDroid4_JigrayX")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
})
Tab:AddButton({
Name = "Get Jigray X Quest 2",
Callback = function()
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quest.QuestNPCDroid4_JigrayX:WaitForChild("HumanoidRootPart").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer").InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer"), "NPCQuest_Misty Lake", 7, Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Quest"):WaitForChild("QuestNPCDroid4_JigrayX")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
})
Tab:AddToggle({
    Name = "Auto Nearby Jigray X Boss Mobs (Back Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyjigrayxbossmobs_db",
    Callback = function(Value)
        Environment.autotpnearbyjigrayxbossmobs = Value
        if not Value then return end
        while Environment.autotpnearbyjigrayxbossmobs do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then
                                part.CanCollide = false
                            end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Jigray X" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local offsetPosition = mobHRP.Position - (mobHRP.CFrame.LookVector * 5)
                    local targetCFrame = CFrame.new(offsetPosition, mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Jigray X Boss Mobs 2 (Head Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyjigrayxbossmobs2_db",
    Callback = function(Value)
        Environment.autotpnearbyjigrayxbossmobs2 = Value
        if not Value then return end
        while Environment.autotpnearbyjigrayxbossmobs2 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then
                                part.CanCollide = false
                            end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Jigray X" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("Head").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    tweenTo(rootPart, nearestBossMob:WaitForChild("Head").CFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Jigray X Boss Mobs 3 (Front Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyjigrayxbossmobs3_db",
    Callback = function(Value)
        Environment.autotpnearbyjigrayxbossmobs3 = Value
        if not Value then return end
        while Environment.autotpnearbyjigrayxbossmobs3 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then
                                part.CanCollide = false
                            end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Jigray X" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local offsetPosition = mobHRP.Position + (mobHRP.CFrame.LookVector * 5)
                    local targetCFrame = CFrame.new(offsetPosition, mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Jigray X Boss Mobs 4 (Under Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyjigrayxbossmobs4_db",
    Callback = function(Value)
        Environment.autotpnearbyjigrayxbossmobs4 = Value
        if not Value then return end
        while Environment.autotpnearbyjigrayxbossmobs4 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then
                                part.CanCollide = false
                            end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Jigray X" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.new(mobHRP.Position - Vector3.new(0, 5, 0), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddLabel("Zero")
Tab:AddButton({
Name = "Get Zero Quest",
Callback = function()
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quest.QuestNPCDroid5_Zero:WaitForChild("Head").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer").InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer"), "NPCQuest_Droid Waste Island", 2, Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Quest"):WaitForChild("QuestNPCDroid5_Zero")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
})
Tab:AddButton({
Name = "Get Zero Quest 2",
Callback = function()
local OldCFrame = Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quest.QuestNPCDroid5_Zero:WaitForChild("HumanoidRootPart").CFrame
task.wait(0.75)
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer").InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("DialogService"):WaitForChild("RF"):WaitForChild("Answer"), "NPCQuest_Droid Waste Island", 2, Workspace:WaitForChild("Misc"):WaitForChild("NPC"):WaitForChild("Quest"):WaitForChild("QuestNPCDroid5_Zero")}))
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = OldCFrame
end
})
Tab:AddToggle({
    Name = "Auto Nearby Zero Boss Mobs (Back Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyzerobossmobs_db",
    Callback = function(Value)
        Environment.autotpnearbyzerobossmobs = Value
        if not Value then return end
        while Environment.autotpnearbyzerobossmobs do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then
                                part.CanCollide = false
                            end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Zero" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local offsetPosition = mobHRP.Position - (mobHRP.CFrame.LookVector * 5)
                    local targetCFrame = CFrame.new(offsetPosition, mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Zero Boss Mobs 2 (Head Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyzerobossmobs2_db",
    Callback = function(Value)
        Environment.autotpnearbyzerobossmobs2 = Value
        if not Value then return end
        while Environment.autotpnearbyzerobossmobs2 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then
                                part.CanCollide = false
                            end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Zero" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("Head").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    tweenTo(rootPart, nearestBossMob:WaitForChild("Head").CFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Zero Boss Mobs 3 (Front Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyzerobossmobs3_db",
    Callback = function(Value)
        Environment.autotpnearbyzerobossmobs3 = Value
        if not Value then return end
        while Environment.autotpnearbyzerobossmobs3 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then
                                part.CanCollide = false
                            end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Zero" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local offsetPosition = mobHRP.Position + (mobHRP.CFrame.LookVector * 5)
                    local targetCFrame = CFrame.new(offsetPosition, mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Zero Boss Mobs 4 (Under Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbyzerobossmobs4_db",
    Callback = function(Value)
        Environment.autotpnearbyzerobossmobs4 = Value
        if not Value then return end
        while Environment.autotpnearbyzerobossmobs4 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then
                                part.CanCollide = false
                            end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Zero" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.new(mobHRP.Position - Vector3.new(0, 5, 0), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddLabel("Karrot")
Tab:AddToggle({
    Name = "Auto Nearby Karrot Boss Mobs (Back Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbykarrotbossmobs_db",
    Callback = function(Value)
        Environment.autotpnearbykarrotbossmobs = Value
        if not Value then return end
        while Environment.autotpnearbykarrotbossmobs do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then
                                part.CanCollide = false
                            end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Karrot" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local offsetPosition = mobHRP.Position - (mobHRP.CFrame.LookVector * 5)
                    local targetCFrame = CFrame.new(offsetPosition, mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Karrot Boss Mobs 2 (Head Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbykarrotbossmobs2_db",
    Callback = function(Value)
        Environment.autotpnearbykarrotbossmobs2 = Value
        if not Value then return end
        while Environment.autotpnearbykarrotbossmobs2 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then
                                part.CanCollide = false
                            end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Karrot" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("Head").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    tweenTo(rootPart, nearestBossMob:WaitForChild("Head").CFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Karrot Boss Mobs 3 (Front Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbykarrotbossmobs3_db",
    Callback = function(Value)
        Environment.autotpnearbykarrotbossmobs3 = Value
        if not Value then return end
        while Environment.autotpnearbykarrotbossmobs3 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then
                                part.CanCollide = false
                            end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Karrot" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local offsetPosition = mobHRP.Position + (mobHRP.CFrame.LookVector * 5)
                    local targetCFrame = CFrame.new(offsetPosition, mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Karrot Boss Mobs 4 (Under Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbykarrotbossmobs4_db",
    Callback = function(Value)
        Environment.autotpnearbykarrotbossmobs4 = Value
        if not Value then return end
        while Environment.autotpnearbykarrotbossmobs4 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then
                                part.CanCollide = false
                            end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Karrot" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.new(mobHRP.Position - Vector3.new(0, 5, 0), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddLabel("Garriot")
Tab:AddToggle({
    Name = "Auto Nearby Garriot Boss Mobs (Back Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbygarriotbossmobs_db",
    Callback = function(Value)
        Environment.autotpnearbygarriotbossmobs = Value
        if not Value then return end
        while Environment.autotpnearbygarriotbossmobs do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then
                                part.CanCollide = false
                            end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Garriot" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local offsetPosition = mobHRP.Position - (mobHRP.CFrame.LookVector * 5)
                    local targetCFrame = CFrame.new(offsetPosition, mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Garriot Boss Mobs 2 (Head Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbygarriotbossmobs2_db",
    Callback = function(Value)
        Environment.autotpnearbygarriotbossmobs2 = Value
        if not Value then return end
        while Environment.autotpnearbygarriotbossmobs2 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then
                                part.CanCollide = false
                            end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Garriot" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("Head").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    tweenTo(rootPart, nearestBossMob:WaitForChild("Head").CFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Garriot Boss Mobs 3 (Front Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbygarriotbossmobs3_db",
    Callback = function(Value)
        Environment.autotpnearbygarriotbossmobs3 = Value
        if not Value then return end
        while Environment.autotpnearbygarriotbossmobs3 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then
                                part.CanCollide = false
                            end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Garriot" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local offsetPosition = mobHRP.Position + (mobHRP.CFrame.LookVector * 5)
                    local targetCFrame = CFrame.new(offsetPosition, mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
Tab:AddToggle({
    Name = "Auto Nearby Garriot Boss Mobs 4 (Under Position!)",
    Default = false,
    Save = true,
    Flag = "autotpnearbygarriotbossmobs4_db",
    Callback = function(Value)
        Environment.autotpnearbygarriotbossmobs4 = Value
        if not Value then return end
        while Environment.autotpnearbygarriotbossmobs4 do task.wait()
                        task.spawn(function()
                local function tweenTo(rootPart, targetCFrame, speed)
                    if not rootPart then return end
                    local character = Players.LocalPlayer.Character
                    if character then
                        for _, part in character:GetDescendants() do
                            if part:IsA("BasePart") then
                                part.CanCollide = false
                            end
                        end
                    end
                    pcall(function()
                        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
                        local tweenTime = distance / speed
                        local tweenInfo = TweenInfo.new(tweenTime, Enum.EasingStyle.Linear)
                        local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = targetCFrame})
                        tween:Play()
                        task.wait(tweenTime)
                    end)
                end
                local nearestBossMob, nearestDistance = nil, math.huge
                local character = Players.LocalPlayer.Character
                local rootPart = character and character:FindFirstChild("HumanoidRootPart")
                for _, v in Workspace["World Mobs"]:FindFirstChild("Boss Mobs"):GetChildren() do
                    if v.Name == "Garriot" and v:IsA("Model") and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChildWhichIsA("Humanoid") and v:FindFirstChildWhichIsA("Humanoid").Health > 0 then
                        local distance = (rootPart.Position - v:WaitForChild("HumanoidRootPart").Position).Magnitude
                        if distance < nearestDistance then
                            nearestDistance, nearestBossMob = distance, v
                        end
                    end
                end
                if nearestBossMob then
                    local mobHRP = nearestBossMob:WaitForChild("HumanoidRootPart")
                    local targetCFrame = CFrame.new(mobHRP.Position - Vector3.new(0, 5, 0), mobHRP.Position)
                    tweenTo(rootPart, targetCFrame, 550 + 50)
                end
            end)
        end
    end
})
local Tab = Window:MakeTab({
Name = "NPCS",
Icon = "rbxassetid://4483345998",
PremiumOnly = false
})
Tab:AddButton({
Name = "Interact With Guild Quest Giver",
Callback = function()
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Main.GuildQuest:WaitForChild("Head").CFrame
task.wait(0.75)
fireproximityprompt(Workspace.Misc.NPC.Main.GuildQuest.Head.DialogAttachment.DialogPrompt)
end
})
Tab:AddButton({
Name = "Interact With Guild Quest Giver 2",
Callback = function()
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Main.GuildQuest:WaitForChild("HumanoidRootPart").CFrame
task.wait(0.75)
fireproximityprompt(Workspace.Misc.NPC.Main.GuildQuest.Head.DialogAttachment.DialogPrompt)
end
})
Tab:AddButton({
Name = "Puriza X003 Quest",
Callback = function()
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quest.QuestNPCDroid1_PurizaX003:WaitForChild("Head").CFrame
end
})
Tab:AddButton({
Name = "Atom X002 Quest",
Callback = function()
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quest.QuestNPCDroid2_AtomX002:WaitForChild("Head").CFrame
end
})
Tab:AddButton({
Name = "Brawly X001 Quest",
Callback = function()
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quest.QuestNPCDroid3_BrawlyX01:WaitForChild("Head").CFrame
end
})
Tab:AddButton({
Name = "Jigray X Quest",
Callback = function()
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quest.QuestNPCDroid4_JigrayX:WaitForChild("Head").CFrame
end
})
Tab:AddButton({
Name = "Zero Quest",
Callback = function()
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quest.QuestNPCDroid5_Zero:WaitForChild("Head").CFrame
end
})
Tab:AddButton({
Name = "Puriza X003 Quest 2",
Callback = function()
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quest.QuestNPCDroid1_PurizaX003:WaitForChild("HumanoidRootPart").CFrame
end
})
Tab:AddButton({
Name = "Atom X002 Quest 2",
Callback = function()
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quest.QuestNPCDroid2_AtomX002:WaitForChild("HumanoidRootPart").CFrame
end
})
Tab:AddButton({
Name = "Brawly X001 Quest 2",
Callback = function()
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quest.QuestNPCDroid3_BrawlyX01:WaitForChild("HumanoidRootPart").CFrame
end
})
Tab:AddButton({
Name = "Jigray X Quest 2",
Callback = function()
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quest.QuestNPCDroid4_JigrayX:WaitForChild("HumanoidRootPart").CFrame
end
})
Tab:AddButton({
Name = "Zero Quest 2",
Callback = function()
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = Workspace.Misc.NPC.Quest.QuestNPCDroid5_Zero:WaitForChild("HumanoidRootPart").CFrame
end
})
local Tab = Window:MakeTab({
Name = "Teleports",
Icon = "http://www.roblox.com/asset/?id=4335489011",
PremiumOnly = false
})
local Section = Tab:AddSection({
Name = "Click the Buttons to Teleport"
})
Tab:AddButton({
Name = "Go To Quest Spawned Items",
Callback = function()
for _, v in Workspace.Misc.QuestProps:GetChildren() do
if v:IsA("Folder") then
for _, child in v:GetChildren() do
if child:IsA("Part") then
if child:FindFirstChildWhichIsA("Model") and child:FindFirstChildWhichIsA("Model").Parent.Name == child.Name then
local endTime = tick() + 2.5
while tick() < endTime do
Players.LocalPlayer.Character:FindFirstChild("HumanoidRootPart").CFrame = child.CFrame
task.wait()
end
end
end
end
end
end
end
})
Tab:AddButton({
Name = "Go To Quest Board",
Callback = function()
Players.LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(1056.73523, 832.668213, 1592.24915, -0.984455705, 6.18853733e-08, -0.175633132, 4.11718553e-08, 1, 1.21580186e-07, 0.175633132, 1.12459162e-07, -0.984455705)
end
})
Tab:AddButton({
Name = "World Teleporter",
Callback = function()
task.wait(0.75)
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = CFrame.new(1042.92664, 832.733704, 1388.02917, 0.861865282, -3.41834581e-11, -0.507137299, -7.3925871e-10, 1, -1.32375377e-09, 0.507137299, 1.51580304e-09, 0.861865282)
end
})
local Tab = Window:MakeTab({
Name = "Worlds",
Icon = "rbxassetid://4483345998",
PremiumOnly = false
})
local Section = Tab:AddSection({
Name = "Click The Button To Be Teleported To That World"
})
Tab:AddButton({
Name = "Main World",
Callback = function()
local teleportservice = game:GetService("TeleportService")
local gameID = game.PlaceId
teleportservice:Teleport(3177438863)
end
})
Tab:AddButton({
Name = "Crimson Planet (30 Rebirths Required)",
Callback = function()
local teleportservice = game:GetService("TeleportService")
local gameID = game.PlaceId
teleportservice:Teleport(7040546583)
end
})
Tab:AddButton({
Name = "Planet Droid (150 Rebirths Required)",
Callback = function()
local teleportservice = game:GetService("TeleportService")
local gameID = game.PlaceId
teleportservice:Teleport(7888441249)
end
})
Tab:AddButton({
Name = "World Of Tournament (Script Wont Work Here!)",
Callback = function()
local teleportservice = game:GetService("TeleportService")
local gameID = game.PlaceId
teleportservice:Teleport(7379325123)
end
})
Tab:AddButton({
Name = "Battlegrounds (Script Wont Work Here!)",
Callback = function()
local teleportservice = game:GetService("TeleportService")
local gameID = game.PlaceId
teleportservice:Teleport(14516089806)
end
})
Tab:AddButton({
Name = "AFK World (Script Wont Work Here!)",
Callback = function()
local teleportservice = game:GetService("TeleportService")
local gameID = game.PlaceId
teleportservice:Teleport(18501029209)
end
})
Tab:AddButton({
Name = "Trade Hub (Script Wont Work Here!)",
Callback = function()
local teleportservice = game:GetService("TeleportService")
local gameID = game.PlaceId
teleportservice:Teleport(18408422178)
end
})
local Tab = Window:MakeTab({
Name = "Anti's",
Icon = "http://www.roblox.com/asset/?id=7734056608",
PremiumOnly = false
})
Tab:AddToggle({
Name = "Anti Admins (do task.wait()t use with anti kick!)",
Default = false,
Save = true,
Flag = "antiadmins_db",
Callback = function(Value)
Environment.AntiAdmins = Value
while Environment.AntiAdmins and task.wait() do
local AdminNames = {
"Jirenato",
"ReignImperatus",
"Zephyrus_Yi",
"ratedroeh",
"Natt_P5",
"O_ORui",
"BexDragoon",
"pokemonhaden",
"XADED4971",
"AltForTGBN",
"notrmdx",
"Npinoy"
}
for _, player in Players:GetPlayers() do
if table.find(AdminNames, player.Name) then
Players.LocalPlayer:Kick("Holy shit admin detected! You almost got banned there bud!")
end
end
task.wait(0.1)
end
end
})
Tab:AddButton({
    Name = "Anti Cheat Bypass (Bypass Anti Exploit Manager!)",
    Callback = function()
        local mt = getrawmetatable(game)
        setreadonly(mt, false)
        local oldNamecall = mt.__namecall
        mt.__namecall = newcclosure(function(self, ...)
            local args = {...}
            local method = getnamecallmethod()
            if method == "Kick" or method == "Destroy" then
                warn("[Bypass] Anti-cheat tried to kick you!")
                return false
            end
            return oldNamecall(self, unpack(args))
        end)
        local RunService = game:GetService("RunService")
        local Players = game:GetService("Players")
        local StarterGui = game:GetService("StarterGui")
        local HeartbeatConnection
        if HeartbeatConnection then HeartbeatConnection:Disconnect() end
        HeartbeatConnection = RunService.Heartbeat:Connect(function()
            sethiddenproperty(Players.LocalPlayer, "SimulationRadius", math.huge)
        end)
        local ReplicatedStorage = game:GetService("ReplicatedStorage")
        local BlockedRemotes = {"AntiCheatEvent1", "AntiCheatEvent2"}
        local oldFireServer = Instance.new("RemoteEvent").FireServer
        hookfunction(oldFireServer, function(self, ...)
            if table.find(BlockedRemotes, self.Name) then
                warn("[Bypass] Blocked remote:", self.Name)
                return false
            end
            return oldFireServer(self, ...)
        end)
        local function SpoofPlayerData()
            local Player = Players.LocalPlayer
            while task.wait(0.75) do
                if Player and Player.Character then
                    local Humanoid = Player.Character:FindFirstChildOfClass("Humanoid")
                    if Humanoid then
                        Humanoid.WalkSpeed = 16
                        Humanoid.JumpPower = 50
                    end
                end
            end
        end
        task.spawn(SpoofPlayerData)
        StarterGui:SetCore("SendNotification", {
            Title = "Bypass Active",
            Text = "Anti-Cheat successfully bypassed.",
            Duration = 5
        })
    end
})
Tab:AddToggle({
Name = "Anti Kick",
Default = false,
Save = true,
Flag = "antikick_db",
Callback = function(Value)
Environment.Anti = Value
if Value then
local Anti
Anti = hookmetamethod(game, "__namecall", function(self, ...)
if self == Players.LocalPlayer and getnamecallmethod():lower() == "kick" and Environment.Anti then
return warn("[ANTI-KICK] Client Tried To Call Kick Function On LocalPlayer")
end
return Anti(self, ...)
end)
else
if Anti then
Environment.Anti = false
hookmetamethod(game, "__namecall", Anti)
Anti = false
end
end
end
})
Tab:AddToggle({
Name = "Anti Destroy",
Default = false,
Save = true,
Flag = "antidestroy_db",
Callback = function(Value)
Environment.Anti = Value
if Value then
local Anti
Anti = hookmetamethod(game, "__namecall", function(self, ...)
if self == Players.LocalPlayer and getnamecallmethod():lower() == "destroy" and Environment.Anti then
return warn("[ANTI-DESTROY] Client Tried To Call Destroy Function On LocalPlayer")
end
return Anti(self, ...)
end)
else
if Anti then
Environment.Anti = false
hookmetamethod(game, "__namecall", Anti)
Anti = false
end
end
end
})
Tab:AddToggle({
Name = "Anti Afk",
Default = false,
Save = true,
Flag = "antiafk_db",
Callback = function(Value)
if Value then
if getconnections then
for _, v in getconnections(Players.LocalPlayer.Idled) do
if v["Disable"] then
v["Disable"](v)
elseif v["Disconnect"] then
v["Disconnect"](v)
end
end
else
local VirtualUser = game:GetService("VirtualUser")
Players.LocalPlayer.Idled:Connect(function()
VirtualUser:CaptureController()
VirtualUser:ClickButton2(Vector2.new())
end)
end
else
end
end
})
local Tab = Window:MakeTab({
Name = "Spawn Items",
Icon = "http://www.roblox.com/asset/?id=7733674079",
PremiumOnly = false
})
Tab:AddButton({
Name = "Grab Relics",
Callback = function()
for _, position in {
CFrame.new(1231.59375, 486.045441, 346.93631, 1, 0, 0, 0, 1, 0, 0, 0, 1),
CFrame.new(-1140.06934, 446.829285, 2510.25269, 1, 0, 0, 0, 1, 0, 0, 0, 1),
CFrame.new(3695.47021, 0.24999994, -3538.87207, 1, 0, 0, 0, 1, 0, 0, 0, 1),
CFrame.new(458.673889, 131.248169, -8390.77539, 0.996191859, 0, 0.0871884301, 0, 1, 0, -0.0871884301, 0, 0.996191859),
CFrame.new(-3459.78662, 95.2498169, -4689.10986, 1, 0, 0, 0, 1, 0, 0, 0, 1),
CFrame.new(5087.55859, 96.24617, -4636.09521, 1, 0, 0, 0, 1, 0, 0, 0, 1)
} do
local endTime = tick() + 4.75
while tick() < endTime do
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = position
for _, part in Workspace:WaitForChild("PartStorage"):GetDescendants() do
if part:IsA("BasePart") then
for _, v in part:GetDescendants() do
if v:IsA("ProximityPrompt") and v.ObjectText and v.ObjectText:find("Token") then
firesignal(v.Triggered)
end
end
end
end
task.wait()
end
end
end
})
Tab:AddButton({
Name = "Grab Relics 2 (Remotes!)",
Callback = function()
for _, position in {
CFrame.new(1231.59375, 486.045441, 346.93631, 1, 0, 0, 0, 1, 0, 0, 0, 1),
CFrame.new(-1140.06934, 446.829285, 2510.25269, 1, 0, 0, 0, 1, 0, 0, 0, 1),
CFrame.new(3695.47021, 0.24999994, -3538.87207, 1, 0, 0, 0, 1, 0, 0, 0, 1),
CFrame.new(458.673889, 131.248169, -8390.77539, 0.996191859, 0, 0.0871884301, 0, 1, 0, -0.0871884301, 0, 0.996191859),
CFrame.new(-3459.78662, 95.2498169, -4689.10986, 1, 0, 0, 0, 1, 0, 0, 0, 1),
CFrame.new(5087.55859, 96.24617, -4636.09521, 1, 0, 0, 0, 1, 0, 0, 0, 1)
} do
local endTime = tick() + 4.75
while tick() < endTime do
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = position
for _, v in Workspace.PartStorage:GetChildren() do
if string.match(v.Name, "^ItemDrop_%d+$") then
ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("ItemDropService"):WaitForChild("RF"):WaitForChild("ClaimItem").InvokeServer(unpack({ReplicatedStorage:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.4.7"):WaitForChild("knit"):WaitForChild("Services"):WaitForChild("ItemDropService"):WaitForChild("RF"):WaitForChild("ClaimItem"), string.match(v.Name, "%d+"), "StandardWish"}))
end
end
task.wait()
end
end
end
})
Tab:AddButton({
Name = "Teleport To Relics",
Callback = function()
for _, position in {
CFrame.new(1231.59375, 486.045441, 346.93631, 1, 0, 0, 0, 1, 0, 0, 0, 1),
CFrame.new(-1140.06934, 446.829285, 2510.25269, 1, 0, 0, 0, 1, 0, 0, 0, 1),
CFrame.new(3695.47021, 0.24999994, -3538.87207, 1, 0, 0, 0, 1, 0, 0, 0, 1),
CFrame.new(458.673889, 131.248169, -8390.77539, 0.996191859, 0, 0.0871884301, 0, 1, 0, -0.0871884301, 0, 0.996191859),
CFrame.new(-3459.78662, 95.2498169, -4689.10986, 1, 0, 0, 0, 1, 0, 0, 0, 1),
CFrame.new(5087.55859, 96.24617, -4636.09521, 1, 0, 0, 0, 1, 0, 0, 0, 1)
} do
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = position
task.wait(5)
end
end
})
Tab:AddButton({
Name = "Teleport To Relics 2 (Stay At Current Location!)",
Callback = function()
for _, position in {
CFrame.new(1231.59375, 486.045441, 346.93631, 1, 0, 0, 0, 1, 0, 0, 0, 1),
CFrame.new(-1140.06934, 446.829285, 2510.25269, 1, 0, 0, 0, 1, 0, 0, 0, 1),
CFrame.new(3695.47021, 0.24999994, -3538.87207, 1, 0, 0, 0, 1, 0, 0, 0, 1),
CFrame.new(458.673889, 131.248169, -8390.77539, 0.996191859, 0, 0.0871884301, 0, 1, 0, -0.0871884301, 0, 0.996191859),
CFrame.new(-3459.78662, 95.2498169, -4689.10986, 1, 0, 0, 0, 1, 0, 0, 0, 1),
CFrame.new(5087.55859, 96.24617, -4636.09521, 1, 0, 0, 0, 1, 0, 0, 0, 1)
} do
local endTime = tick() + 5
while tick() < endTime do
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = position
task.wait(1.25)
end
end
end
})
Tab:AddToggle({
Name = "Auto Teleport To Relics",
Default = false,
Save = true,
Flag = "autorelics_db",
Callback = function(Value)
Environment.AutoRelics = Value
while Environment.AutoRelics and task.wait() do
for _, position in {
CFrame.new(1231.59375, 486.045441, 346.93631, 1, 0, 0, 0, 1, 0, 0, 0, 1),
CFrame.new(-1140.06934, 446.829285, 2510.25269, 1, 0, 0, 0, 1, 0, 0, 0, 1),
CFrame.new(3695.47021, 0.24999994, -3538.87207, 1, 0, 0, 0, 1, 0, 0, 0, 1),
CFrame.new(458.673889, 131.248169, -8390.77539, 0.996191859, 0, 0.0871884301, 0, 1, 0, -0.0871884301, 0, 0.996191859),
CFrame.new(-3459.78662, 95.2498169, -4689.10986, 1, 0, 0, 0, 1, 0, 0, 0, 1),
CFrame.new(5087.55859, 96.24617, -4636.09521, 1, 0, 0, 0, 1, 0, 0, 0, 1)
} do
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = position
task.wait(5)
end
end
end
})
Tab:AddToggle({
Name = "Auto Teleport To Relics 2 (Stay At Current Location!)",
Default = false,
Save = true,
Flag = "autorelics_db",
Callback = function(Value)
Environment.AutoRelics = Value
while Environment.AutoRelics and task.wait() do
for _, position in {
CFrame.new(1231.59375, 486.045441, 346.93631, 1, 0, 0, 0, 1, 0, 0, 0, 1),
CFrame.new(-1140.06934, 446.829285, 2510.25269, 1, 0, 0, 0, 1, 0, 0, 0, 1),
CFrame.new(3695.47021, 0.24999994, -3538.87207, 1, 0, 0, 0, 1, 0, 0, 0, 1),
CFrame.new(458.673889, 131.248169, -8390.77539, 0.996191859, 0, 0.0871884301, 0, 1, 0, -0.0871884301, 0, 0.996191859),
CFrame.new(-3459.78662, 95.2498169, -4689.10986, 1, 0, 0, 0, 1, 0, 0, 0, 1),
CFrame.new(5087.55859, 96.24617, -4636.09521, 1, 0, 0, 0, 1, 0, 0, 0, 1)
} do
local endTime = tick() + 5
while tick() < endTime do
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = position
task.wait(0.1)
end
end
end
end
})
Tab:AddToggle({
Name = "Auto Relics (Must Be Near Standard Wish Tokens!)",
Default = false,
Save = true,
Flag = "autotokens_db",
Callback = function(Value)
Environment.AutoTokens = Value
while Environment.AutoTokens and task.wait() do
for _, part in Workspace:WaitForChild("PartStorage"):GetDescendants() do
if part:IsA("BasePart") then
for _, v in part:GetDescendants() do
if v:IsA("ProximityPrompt") and v.ObjectText and v.ObjectText:find("Token") then
if Players.LocalPlayer.Character and Players.LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = part.CFrame + Vector3.new(0, 5, 0)
task.wait(0.75)
fireproximityprompt(v, 1)
task.wait(2)
end
break
end
end
end
end
end
end
})
Tab:AddToggle({
Name = "Auto Orbs (FP Must Be Near Orbs!)",
Default = false,
Save = true,
Flag = "autoorbs_db",
Callback = function(Value)
Environment.AutoOrbs = Value
while Environment.AutoOrbs and task.wait() do
for _, part in Workspace:WaitForChild("PartStorage"):GetDescendants() do
if part:IsA("BasePart") then
for _, v in part:GetDescendants() do
if v:IsA("ProximityPrompt") and v.ObjectText and v.ObjectText:find("Orb") then
if Players.LocalPlayer.Character and Players.LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = part.CFrame + Vector3.new(0, 5, 0)
task.wait(0.75)
fireproximityprompt(v, 1)
task.wait(2)
end
break
end
end
end
end
end
end
})
Tab:AddToggle({
Name = "Auto Orbs 2 (FS Must Be Near Orbs!)",
Default = false,
Save = true,
Flag = "autoorbs2_db",
Callback = function(Value)
Environment.AutoOrbs2 = Value
while Environment.AutoOrbs2 and task.wait() do
for _, part in Workspace:WaitForChild("PartStorage"):GetDescendants() do
if part:IsA("BasePart") then
for _, v in part:GetDescendants() do
if v:IsA("ProximityPrompt") and v.ObjectText and v.ObjectText:find("Orb") then
if Players.LocalPlayer.Character and Players.LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").CFrame = part.CFrame + Vector3.new(0, 5, 0)
task.wait(0.75)
firesignal(v.Triggered)
task.wait(2)
end
break
end
end
end
end
end
end
})
Tab:AddToggle({
Name = "Auto Nearby Interact",
Default = false,
Save = true,
Flag = "autonearbyinteract_db",
Callback = function(Value)
Environment.AutoNearbyInteract = Value
while Environment.AutoNearbyInteract and task.wait() do
function fire()
if Players.LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
for _, v in Workspace:GetDescendants() do
if v:IsA("ProximityPrompt") then
if ((Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").Position - v.Parent.Position).Magnitude <= 10) then
fireproximityprompt(v)
end
end
end
end
end
fire()
end
end
})
Tab:AddToggle({
Name = "Auto Nearby Interact 2 (Maybe Use 1!)",
Default = false,
Save = true,
Flag = "autonearbyinteract_db",
Callback = function(Value)
Environment.AutoNearbyInteract = Value
while Environment.AutoNearbyInteract and task.wait() do
function fire()
if Players.LocalPlayer.Character:FindFirstChild("Head") then
for _, v in Workspace:GetDescendants() do
if v:IsA("ProximityPrompt") then
if ((Players.LocalPlayer.Character:WaitForChild("Head").Position - v.Parent.Position).Magnitude <= 10) then
fireproximityprompt(v)
end
end
end
end
end
fire()
end
end
})
Tab:AddButton({
Name = "Instant Nearby Interact",
Callback = function()
function fire()
if Players.LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
for _, v in Workspace:GetDescendants() do
if v:IsA("ProximityPrompt") then
if ((Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart").Position - v.Parent.Position).Magnitude <= 10) then
fireproximityprompt(v)
end
end
end
end
end
fire()
end
})
Tab:AddButton({
Name = "Instant Nearby Interact 2 (Maybe use 1!)",
Callback = function()
function fire()
if Players.LocalPlayer.Character:FindFirstChild("Head") then
for _, v in Workspace:GetDescendants() do
if v:IsA("ProximityPrompt") then
if ((Players.LocalPlayer.Character:WaitForChild("Head").Position - v.Parent.Position).Magnitude <= 10) then
fireproximityprompt(v)
end
end
end
end
end
fire()
end
})
Tab:AddButton({
Name = "Wish Standard (Need Standard Tokens!)",
Callback = function()
ReplicatedStorage.Packages._Index:FindFirstChild("sleitnick_knit@1.4.7").knit.Services.WishService.RF.requestWishFromBanner.InvokeServer(unpack({ReplicatedStorage.Packages._Index:FindFirstChild("sleitnick_knit@1.4.7").knit.Services.WishService.RF.requestWishFromBanner, "Standard", 1, 1}))
end
})
Tab:AddButton({
Name = "Wish Standard (Need Premium Tokens!)",
Callback = function()
ReplicatedStorage.Packages._Index:FindFirstChild("sleitnick_knit@1.4.7").knit.Services.WishService.RF.requestWishFromBanner.InvokeServer(unpack({ReplicatedStorage.Packages._Index:FindFirstChild("sleitnick_knit@1.4.7").knit.Services.WishService.RF.requestWishFromBanner, "Standard", 2, 1}))
end
})
Tab:AddButton({
Name = "Wish Premium (Need Premium Tokens!)",
Callback = function()
ReplicatedStorage.Packages._Index:FindFirstChild("sleitnick_knit@1.4.7").knit.Services.WishService.RF.requestWishFromBanner.InvokeServer(unpack({ReplicatedStorage.Packages._Index:FindFirstChild("sleitnick_knit@1.4.7").knit.Services.WishService.RF.requestWishFromBanner, "Premium", 1, 1}))
end
})
Tab:AddButton({
Name = "Buy Premium Wish Standard (Need 50 Gold!)",
Callback = function()
ReplicatedStorage.Packages._Index:FindFirstChild("sleitnick_knit@1.4.7").knit.Services.WishService.RF.buyFromShop.InvokeServer(unpack({ReplicatedStorage.Packages._Index:FindFirstChild("sleitnick_knit@1.4.7").knit.Services.WishService.RF.buyFromShop, "Standard", 5}))
end
})
local Tab = Window:MakeTab({
Name = "Codes/Trades",
Icon = "rbxassetid://4483345998",
PremiumOnly = false,
})
Tab:AddLabel("I Have Added A Codes Channel For Users!")
Tab:AddLabel("Please Know That These Codes May Expire At Any Time!")
Tab:AddLabel("I Will Update This Whenever A New One Comes Out!")
Tab:AddParagraph("The Current Codes Is MOREFREECODES, TENFREEWISHES, PREMIUMSGALORE")
Tab:AddTextbox({
Name = "Codes (Redeem!)",
Default = "",
TextDisappear = false,
Callback = function(Value)
local args = {
[1] = {
["UniqueTag"] = "RedeemCode",
["InputText"] = Value,
["InputType"] = "Text",
["LeftButton"] = "Redeem",
["Description"] = "Redeem Code",
["Prompt"] = "RedeemCode",
["RightButton"] = "Cancel",
["Title"] = "Codes"
},
[2] = "Redeem"
}
ReplicatedStorage.Packages._Index:FindFirstChild("sleitnick_knit@1.4.7").knit.Services.PromptService.RE.Prompt.FireServer(unpack({ReplicatedStorage.Packages._Index:FindFirstChild("sleitnick_knit@1.4.7").knit.Services.PromptService.RE.Prompt, unpack(args)}))
end
})
Tab:AddTextbox({
Name = "Trades (Enter Player Username Here!)",
Default = "",
TextDisappear = false,
Callback = function(Value)
local player = Players:FindFirstChild(Value)
if player then
OrionLib:MakeNotification({
Name = "Player Found!",
Content = "Sending Trade!",
Image = "rbxassetid://4483345998",
Time = 5
})
local args = {
[1] = player
}
ReplicatedStorage.Packages._Index:FindFirstChild("sleitnick_knit@1.4.7").knit.Services.TradeService.RE.RequestTrade.FireServer(unpack({ReplicatedStorage.Packages._Index:FindFirstChild("sleitnick_knit@1.4.7").knit.Services.TradeService.RE.RequestTrade, unpack(args)}))
else
OrionLib:MakeNotification({
Name = "Player Not Found!",
Content = "That Player Does Not Exist. Please Try Again!",
Image = "rbxassetid://4483345998",
Time = 5
})
end
end
})
local Tab = Window:MakeTab({
Name = "Credits",
Icon = "http://www.roblox.com/asset/?id=4370318685",
PremiumOnly = false
})
Tab:AddLabel("Script is made by Hydrazia")
Tab:AddParagraph("Info", "Tap The Button To Copy My Social And Invite Links!")
Tab:AddParagraph("Info", "Script changelogs at our Telegram Channel. If you find any bugs, inform me.")
Tab:AddButton({
Name = "Youtube",
Callback = function()
setclipboard("https://m.youtube.com/channel/UCa1xzPElap7CUTGtj0QhdwA")
end
})
Tab:AddButton({
Name = "Scriptblox",
Callback = function()
setclipboard("https://scriptblox.com/u/Riotscripter")
end
})
Tab:AddButton({
Name = "Telegram Channel",
Callback = function()
setclipboard("https://t.me/Riotscripter")
end
})
Tab:AddBind({
Name = "UI Toggle Keybind",
Default = Enum.KeyCode.RightShift,
Hold = false,
Callback = function()
print("press")
end
})
OrionLib:Init()
end
